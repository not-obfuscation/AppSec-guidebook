# Подраздел «Cryptographic Failures» (A04:2025) — отчёт

Миссия `appsec-stage1-crypto`. Досье —
`~/.supermanager/missions/appsec-stage1-crypto/dossier.md`. Семь тем
замороженного оглавления написаны, проверены и закоммичены.

## 1. Что написано

Семь тем подраздела 1.11 по замороженному оглавлению `STAGE1-TOC.md`
(места 50 и 74–79), каждая проходит валидатор.

| место | order | id | ур. | слов (тело) |
|---:|---:|---|:-:|---:|
| 50 | 500 | `hmac-vs-signature` | L2 | 1125 |
| 74 | 740 | `plaintext-data` | L2 | 1083 |
| 75 | 750 | `weak-algorithms` | L2 | 1211 |
| 76 | 760 | `crypto-misuse` | L2 | 1298 |
| 77 | 770 | `padding-oracle` | L3 | 329 |
| 78 | 780 | `secure-random` | L2 | 993 |
| 79 | 790 | `secrets-in-code` | L2 | 1090 |

Все L2 в норме 800–1700 слов, L3 в норме 300–500. Стержень подраздела —
разница шифрования, целостности и подписи — несёт `hmac-vs-signature`; на неё
переведены ссылки вперёд из `jwt-basics` и `saml`, и она поставлена их
предпосылкой по оглавлению.

Заведены при первой теме подраздела:

- категория `CRYPTO` в `taxonomy.yaml` (`C-TAX-CATEGORY` зелёный);
- лаба `lab-crypto-misuse` в `labs.yaml` (`pilot/lab/crypto-misuse`, формат
  «почини»);
- правило SAST `aes-mode-ecb-or-static-iv` (`pilot/semgrep/`, два
  идентификатора: `aes-mode-ecb` и `aes-static-iv`).

Восемь ссылок вперёд из ранее написанных тем переведены с номеров плана на
слаги: `tls-and-proxy` (три), `password-storage`, `jwt-basics` (две), `saml`,
`source-config-exposure`. Этим же закрыто сиротство четырёх новых тем; три
остальные (`crypto-misuse`, `padding-oracle`, `secure-random`) держатся на
входящих ссылках внутри подраздела.

## 2. Чем мерялось

Все числа сняты локально, стенды и вывод — в
`.smgr/appsec-stage1-crypto/exp/` (двенадцать стендов, `e01`…`e12`, каждый с
`*.out.txt`).

```text
make check          0 ошибок, 0 предупреждений, 5 снято — пройдено
make lint-selftest  177 из 177 — пройдено
make links          0 ошибок, 0 предупреждений, 3 снято — пройдено (с сетью)
make site           96 страниц, 11768 внутренних ссылок — пройдено
semgrep check.py    18 правил из 18 — ПРАВИЛО СВЕРЕНО (новое: aes-mode)
```

Стенды, на которых стоят числа тем (Python 3.14.7, `cryptography` 50.0.0,
OpenSSL 3.6.3, sqlite 3.53.4):

- `e01-ecb` — карта блоков ECB совпадает с открытым текстом: 5 различных из 32
  против 32 из 32 у CBC.
- `e02-nonce` — повтор nonce в CTR: 53 совпавших байта из 60 восстановлены без
  ключа.
- `e03-cbc-iv` — постоянный IV даёт одинаковый первый блок; правка 4 байт IV
  меняет `user` на `root` без ошибки.
- `e04-random` — перебор зерна за 3601 попытку; восстановление состояния
  MT19937 по 624 значениям с точным предсказанием следующих пяти; UUID v1
  повторяет узел, v4 даёт 122 случайных бита.
- `e05-hmac` — подмена суммы платежа тремя байтами AES-CTR; отказ
  HMAC-SHA256 и AES-GCM; проверка Ed25519 на исходном и подменённом.
- `e06-compare` — сравнение с ранним выходом: рост 190→611 нс;
  `compare_digest` — ровно 80 нс на всех точках (20 000 замеров).
- `e07-padding-oracle` — 25 байт восстановлены по одному биту за 4151
  обращение при пределе 8192.
- `e08-hashrate` — SHA-256 быстрее MD5 (5,26 против 4,43 млн/с); pbkdf2 600k
  медленнее md5 в 255 000 раз.
- `e09-length-ext` — достраивание `sha256(secret‖msg)` без ключа; HMAC устоял.
- `e10-secrets` — ключ, «удалённый» правкой файла, найден по объектам
  репозитория.
- `e11-plaintext` — пароль прочитан промежуточным узлом; номер карты виден в
  файле базы, при `secure_delete=0` остаётся после DELETE.
- `e12-openssl` — набор по умолчанию чист (только AEAD в TLS 1.3), но 6 из 27
  наборов TLS 1.2 без forward secrecy; `des-ecb`/`des-ede3-cbc` доступны.

Лаба `lab-crypto-misuse`: три опыта эксплойта детерминированы, `hack.py`
возвращает 1 на `code.py` (три обхода: ECB, повтор nonce, податливость CTR),
0 на `solution.py`; `tests.py` зелёный на обоих. Правило SAST: две находки на
`code.py`, ноль на `solution.py`.

## 3. Что найдено попутно

Расхождений источников со сводом или между собой в этом подразделе не
встретилось: криптографические стандарты стабильнее продуктовой документации.
Отмечу проверенное лично, что расходится с сетевым фольклором и вынесено в
тексты как ловушки:

- **SHA-256 быстрее MD5** на этой машине (процессорные инструкции для SHA-2).
  Из скорости стойкость не следует; для паролей плоха любая быстрая функция.
- **IV не секретен** — прямо сказано в SP 800-38A § 5.3. Попытка «спрятать» IV
  константой превращает законное свойство в дефект (стенд `e03`).
- **RFC 9846 (июль 2026) отменяет и RFC 8446, и RFC 5246** — то есть
  спецификация TLS 1.2 отозвана самой серией. Учтено в `plaintext-data`.
- **UUID — уникальность, не непредсказуемость.** ASVS оговаривает это отдельно
  (`v5.0-11.5.1`); v4 даёт 122 случайных бита, ниже нормы 128.
- **Удаление секрета правкой файла ничего не отзывает** (стенд `e10`): порядок
  «сначала отзыв и смена, потом уборка».

## 4. Заявки

**Т-1. Расхождение нумерации OWASP: две темы легли в две категории каждая.**
Это девятый и десятый случаи расхождения по этапу 1; ни один не двигает тему с
места (свод 4.1, п. 3). CWE→категория сверены по спискам «List of Mapped CWEs»
десяти страниц Top 10:2025 (копии в `.smgr/appsec-stage1-crypto/src/A*.txt`).

- `plaintext-data`: передача открытым текстом — CWE-319 (A04:2025), хранение
  открытым текстом — CWE-312, а CWE-312 стоит в списке A06:2025 Insecure
  Design, не в A04. Единого дома у темы нет; объявлены обе категории
  `['A04:2025', 'A06:2025']`.
- `secrets-in-code`: зашитый криптографический ключ — CWE-321 (A04:2025),
  зашитые учётные данные — CWE-798, а CWE-798 стоит в списке A07:2025
  Authentication Failures. Объявлены обе `['A04:2025', 'A07:2025']`.

Остальные пять тем целиком в A04:2025 (все их CWE в списке этой категории).

**Т-2. Уровни и объём.** Замечаний к уровням оглавления нет. `padding-oracle`
на L3 «обзорно» — как помечено планом; механизм по спецификации, прогон снят
на локальном стенде (сеть не участвует, как требует досье).

**Т-3. Глоссарий не пополнялся** — то же решение, что у миссий токенов, файлов,
логики и продвинутых тем. Термины подраздела (HMAC, TLS, CSPRNG, соль,
энтропия) уже заведены; новые понятия («аутентифицированный режим»,
«одноразовое значение», ECB, IV, nonce) раскрываются в прозе при первом
употреблении, и `G-FIRST` на корпусе молчит. Терминов по-прежнему 100.

## 5. Что осталось долгом

По замороженному оглавлению `STAGE1-TOC.md` следующий и последний подраздел
этапа 1 — 1.12 «Mishandling of Exceptional Conditions» (A10:2025), места 80–83:
`fail-open-vs-fail-closed` (L2), `error-message-leaks` (L3),
`unhandled-exceptions-dos` (L2), `partial-state-recovery` (L2). Категории на
подраздел 1.12 в `code_categories` ещё нет — завести с первой темой. По
`error-message-leaks` в оглавлении стоит пометка о возможном слиянии с
`information-disclosure` (до решения оператора — обзорно, L3).

Тем в проекте стало 90, правил SAST 18, лаб 11.

ПОДРАЗДЕЛ CRYPTO СДАН
