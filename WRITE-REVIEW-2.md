# Независимая приёмка восьми оставшихся тем этапа 0

Миссия `appsec-review-8`, 22–23 августа 2026. Принимаются восемь тем этапа 0,
написанные после первой приёмки и не проверявшиеся никем: `t-0-0-04`…`t-0-0-11`.
Заодно проверяется отчёт автора `WRITE-0B.md` — как утверждение о работе, а не
как её описание.

Мерило то же, что и в первой приёмке: `PLAYBOOK.md` — часть 13 (чеклист
приёмки), 11.2 (Definition of done), 3.1–3.2 (уровни), часть 4 (скелет),
части 5–7 (педагогика, редполитика, листинги и визуальное), 9.2–9.6.
Строгость — та же. Первая приёмка дала 21 находку и две правки фактических
ошибок на месте; этот заход даёт 26 находок и три правки на месте.

**Метод.** Читались сам текст тем и сами первоисточники. Отчёт автора открыт
после того, как обе таблицы были заполнены, и его расхождения с моими статусами
вынесены отдельной колонкой в § 4. Каждое число в этом отчёте получено командой,
запущенной в этом заходе; там, где проверить нечем, стоит «не подтверждено», а не
плюс и не минус.

---

## 1. Что именно принимается

Восьмёрка определена по факту, а не по названию миссии. В плане этапа 0
одиннадцать тем (`tools/gen_topics.py --check` — «topics.yaml соответствует
плану»); три из них — `t-0-0-01` `http-basics`, `t-0-0-02` `url-and-encoding`,
`t-0-0-03` `cookies` — прошли приёмку в `WRITE-REVIEW.md`. Остальные восемь идут
подряд от `t-0-0-04` до `t-0-0-11`, и у каждой в файле стоит `reviewed:
2026-08-22`, то есть автор объявил их готовыми. Это и есть набор.

| # | `plan_id` | Файл | Уровень | Режим | Тело | Проза |
|---|---|---|---|---|---|---|
| 1 | `t-0-0-04` | `content/stage-0/sessions-vs-tokens.md` | L2 | концепт | 1866 | 1327 |
| 2 | `t-0-0-05` | `content/stage-0/same-origin-policy.md` | L2 | концепт | 1793 | 1365 |
| 3 | `t-0-0-06` | `content/stage-0/cors.md` | L2 | концепт | 1764 | 1297 |
| 4 | `t-0-0-07` | `content/stage-0/csp.md` | L2 | концепт | 1878 | 1349 |
| 5 | `t-0-0-08` | `content/stage-0/security-headers.md` | **L3** | концепт | 726 | 521 |
| 6 | `t-0-0-09` | `content/stage-0/tls-and-proxy.md` | L2 | концепт | 1894 | 1261 |
| 7 | `t-0-0-10` | `content/stage-0/app-architecture.md` | L2 | концепт | 2003 | 1374 |
| 8 | `t-0-0-11` | `content/stage-0/rest-and-graphql.md` | L2 | концепт | 1959 | 1370 |

«Тело» — слова после шапки, «проза» — тело без листингов, `<details>`, таблиц,
схем и блока 14; счёт `tools/wordcount.py`. В таблицах ниже темы обозначаются
двузначным хвостом `plan_id`: 04, 05, …, 11.

---

## 2. Границы этого захода

Что я делаю: проверяю факты по первоисточникам, гоняю оба чеклиста пункт за
пунктом, измеряю то, что измеримо, и исправляю на месте фактические ошибки —
со ссылкой на источник и записью в § 9. Всё остальное идёт списком находок.
Приёмка не переписывает текст.

Чего я не делаю, и это решено оператором заранее, а не мной:

- **Норму объёма не подгоняю.** Семь тем превышают норму L2, восьмая — норму L3;
  я меряю и пишу цифру (§ 6.1, Ф-42). Резать текст или править норму —
  открытый вопрос оператора, и решать его правкой чужого текста нельзя.
- **Уровни `same-origin-policy` и `security-headers` не ставлю и не меняю.**
  Они ждут личного опыта оператора. Замечания по ним — в § 12, статусами в
  таблицах эти две темы не наказываются за уровень.
- **`tools/`, конфиги проверок, `INFRA.md`, `SCOPE.md`, `STYLE.md`, глоссарий и
  сборку сайта не трогаю** — в этом же каталоге параллельно работает миссия
  `appsec-infra`. Её инструменты я запускал только на чтение; что стоило бы в
  них добавить — списком в § 11, без правок.
- **`sources.yaml` не переписываю.** Добавление записи допустимо, но там, где
  запись нужна (Ф-25), она осмысленна только вместе с правкой текста темы,
  которую делать не мне. Поэтому это условие вердикта, а не моя правка.

**Легенда статусов** — та же, что в первой приёмке:

**+** выполнено · **−** не выполнено · **±** выполнено частично ·
**н/п** проверить нечем · **н/д** неприменимо · **обвязка** — пункт закрывается
не текстом, а инструментом, и в этом заходе закрыт им.

Отдельно про «обвязку». К моменту приёмки параллельная миссия собрала Vale,
markdownlint, стилевой и глоссарный линтеры и запускалку `tools/check.py`. Там,
где пункт чеклиста требует чистого прогона, я ставлю не плюс автора, а результат
прогона (§ 6.3). Линк-чекера в наборе нет — двадцать пять живых адресов
проверены руками (§ 6.4).

**Чего этот заход не проверял.** Исполняемость примеров: исполняемых примеров в
восьмёрке нет, все листинги помечены как фрагмент или псевдокод, CI отсутствует.
Чтение вслух и перечитывание через сутки: темы написаны и приняты в один день,
проверить нечем — это минус по факту, а не претензия к автору. Вёрстка,
навигация, виджет обратной связи: движка сайта нет.

---

## 3. Чеклист приёмки страницы (часть 13), 33 пункта × 8 тем

Столбцы — хвосты `plan_id`: 04 `sessions-vs-tokens`, 05 `same-origin-policy`,
06 `cors`, 07 `csp`, 08 `security-headers` (L3), 09 `tls-and-proxy`,
10 `app-architecture`, 11 `rest-and-graphql`.

| № | Пункт (раздел свода) | 04 | 05 | 06 | 07 | 08 | 09 | 10 | 11 | Находка |
|--:|---|:--:|:--:|:--:|:--:|:--:|:--:|:--:|:--:|---|
| | **Структура** | | | | | | | | | |
| 1 | Уровень проставлен, состав блоков соответствует (4) | ± | ± | ± | ± | ± | ± | ± | ± | Ф-42: состав верен у всех восьми, объём — нет |
| 2 | Порядок блоков не нарушен; отклонения — только удаления (4) | + | + | + | + | + | + | + | + | замерено: порядок возрастающий у всех восьми |
| 3 | Есть «Коротко», предпосылки, «Проверь себя», «Дальше», дата (4) | + | + | + | + | − | + | + | + | у 08 нет блока 12; см. системную находку С-05 |
| 4 | Режим не смешан (9.2) | ± | ± | ± | ± | + | ± | ± | ± | противоречие свода, С-08 (как и в первой приёмке) |
| 5 | Страница самодостаточна для пришедшего из поиска (9.5) | + | + | + | + | + | + | + | + | — |
| 6 | Оглавление читается как конспект (6.1) | − | − | − | − | − | − | − | − | Ф-20 первой приёмки не снята; С-11 |
| | **Обучение** | | | | | | | | | |
| 7 | Первый пример разобран, второй с пропусками, третий свой (5.1) | ± | ± | ± | ± | н/д | ± | ± | ± | Ф-21 первой приёмки: на L2 трёх примеров нет по составу блоков |
| 8 | Ответы недоступны до попытки (5.2) | + | + | + | + | н/д | + | + | + | `<details>` во всех семи |
| 9 | Есть 1–2 вопроса на возврат к прошлым темам (5.2) | + | + | + | + | н/д | + | + | + | буква пункта соблюдена; буква 5.2 п. 10 — нет, Ф-44 |
| 10 | Есть блок «Ловушка» (5.3) | + | + | + | + | н/д | + | + | + | — |
| 11 | Критерии проверки — списком наблюдаемых признаков (5.6) | + | + | + | + | н/д | + | + | + | «Критерий выполнения» в семи темах |
| 12 | Задача стоит в реалистичном контексте (5.6) | + | + | + | + | н/д | + | + | + | — |
| | **Язык** | | | | | | | | | |
| 13 | «вы» со строчной, повелительное, настоящее (6.1) | + | + | + | + | + | + | + | + | заглавное «Вы» — 0 совпадений |
| 14 | Хеджирование сохранено (6.2) | + | + | ± | + | + | − | + | + | Ф-29 (06), Ф-28 (09) |
| 15 | Терминология по глоссарию, первое употребление раскрыто (6.3) | ± | ± | + | + | ± | + | + | + | Ф-46: G-CANON 0, G-FIRST 4 |
| 16 | Запрещённых конструкций нет (6.5) | + | + | + | + | + | + | − | + | Ф-40 (10). У 06 «простой запрос» снят исключением |
| 17 | За каждой аналогией — «где ломается» (6.6) | н/д | н/д | н/д | н/д | н/д | н/д | н/д | н/д | аналогий нет ни одной |
| 18 | Каждое упрощение помечено (6.6) | + | + | + | + | ± | + | + | + | Ф-38 (08): у транскрипта нет маркера примера |
| | **Код и визуальное** | | | | | | | | | |
| 19 | Листинги ≤76 символов и ≤25 строк, подсвечено ≤5 (7.1) | + | + | + | + | + | + | + | + | замерено: ≤73 симв., ≤10 строк, подсветки нет |
| 20 | Пояснения — выносками, не абзацем после блока (7.1) | + | + | + | + | н/д | + | + | + | нумерованные выноски во всех семи |
| 21 | Уязвимый блок помечен маркером в первой строке (7.2) | + | + | + | + | н/д | + | + | + | замерено: 8 блоков `УЯЗВИМО`, все с маркером в первой строке |
| 22 | Плейсхолдеры вместо правдоподобных значений (7.1) | + | ± | ± | + | + | + | + | + | С-07: `company.com`, `attacker.test`, `.invalid` взяты из первоисточников |
| 23 | HTTP — транскриптом, не скриншотом (7.3) | + | + | ± | + | ± | + | + | + | Ф-37 (06), Ф-38 (08). Скриншотов нет ни одного |
| 24 | У схемы есть текстовое описание; цвет не единственный носитель (7.4) | + | н/д | н/д | н/д | н/д | н/д | н/д | н/д | схема одна на восемь тем: Ф-33 |
| 25 | Врезок ≤5, схем ≤3, скриншот ≤1 (7.6, 7.7) | + | + | + | + | + | + | + | + | замерено: ровно 2 врезки в каждой L2, 0 в L3 |
| | **Достоверность** | | | | | | | | | |
| 26 | Каждое утверждение сведено к лично открытому источнику (11.2) | ± | + | ± | ± | ± | ± | ± | − | Ф-22…Ф-30 |
| 27 | Примеры запущены лично и проходят в CI (11.2) | н/д | н/д | н/д | н/д | н/д | н/д | н/д | н/д | исполняемых примеров нет: все листинги помечены «не исполняется» |
| 28 | Маркеры уверенности проставлены (11.2) | + | + | + | ± | + | + | ± | + | Ф-30 (07), Ф-27 (10) |
| 29 | Числа, версии, флаги, CVE проверены вручную (11.2) | + | + | + | + | − | + | + | + | Ф-22 (08). CWE без версии — Ф-32, форма, не факт |
| | **Публикация** | | | | | | | | | |
| 30 | Vale, markdownlint, линк-чекер — чисто (9.4) | обвязка | обвязка | обвязка | обвязка | обвязка | обвязка | обвязка | обвязка | Vale 0 ошибок, markdownlint 0, модельный линтер 5 ошибок (Ф-34); линк-чекера нет — 24/25 руками, § 6.4 |
| 31 | Прочитано вслух или перечитано через сутки (11.2) | − | − | − | − | − | − | − | − | восемь тем написаны и объявлены готовыми в один день |
| 32 | Замер времени внесён в таблицу (11.3) | + | + | + | + | + | + | + | + | `WRITE-0B.md` § 5.1, отрезок на каждую из восьми |
| 33 | Поставлено в очередь на ревизию через 4–8 недель (11.2) | − | − | − | − | − | − | − | − | очереди нет; `review_interval: 24` — это 24 месяца из 9.3, другой механизм |

**Что стоит за нетривиальными статусами.**

Пункт 1. Состав блоков у семи L2-тем — ровно `[0 1 3 5 6 9 10 11 12 13 14]`, у
L3-темы — `[0 3 13 14]`. Оба состава сходятся со сводом, прочитанным целиком, но
не с одним его местом: список 3.1 для L2 — это «блоки 0, 1, 3, 5, 6, 9, 10, 12»,
без 11, 13 и 14. Блок 11 стоит в темах потому, что та же строка 3.1 требует
«задача с критерием», а блоки 13 и 14 — потому, что часть 4 помечает их
`[L1 L2 L3]`. У L3 то же: блок 3 часть 4 помечает `[L1 L2]`, а 3.1 требует от L3
«своё короткое объяснение механизма». Решения автора считаю верными,
противоречия — сводовыми (С-02, С-03). Минус в другую сторону: проза семи тем
выше нормы L2 в среднем на 11,2 %, а L3 — на 4,2 % (§ 6.1). Норму не подгоняю,
поэтому ± у всех восьми, а не выборочно.

Пункт 3. У `security-headers` нет блока 12 «Проверь себя» — и это не небрежность
автора, а состав L3 из 3.1, где блока 12 нет. Пункт чеклиста при этом требует
«Проверь себя» без оговорки об уровне. Минус ставлю теме, находку — своду (С-05).

Пункт 4. Тот же конфликт, что нашла первая приёмка: страницы объявлены концептом,
а блок 10 «Чеклист ревью» написан в залоге руководства, потому что этого требует
сам скелет L2. Внутри темы противоречия нет, противоречие внутри свода (С-08).
У L3-темы блока 10 нет — отсюда единственный плюс в строке.

Пункт 27. Ставлю «н/д», а не «н/п» первой приёмки: исполняемых примеров в
восьмёрке нет по факту. Из шестнадцати листингов пятнадцать несут маркер
«Псевдокод, не
исполняется», «Фрагмент, не исполняется» или «Фрагмент схемы» — запускать нечего,
и это законно по 7.1 п. 11, а не пропуск. Шестнадцатый — транскрипт в 08, и у
него маркера нет вовсе (Ф-38).

Пункт 30. Единственная строка, где по указанию досье стоит не статус, а
«обвязка». Vale и markdownlint дают ноль ошибок на всех восьми — это измерено, а
не заявлено (§ 6.3). Но 269 предупреждений по репозиторию, из них 156 в этих
восьми темах, обвязка не снимает; линк-чекера в наборе `tools/` нет вовсе; а
появившийся во время приёмки модельный линтер даёт по восьмёрке пять
**ошибок** — ровно те длинные заголовки, что нашлись глазами (Ф-34). Прогон
`tools/check.py` на репозитории сейчас не проходит: 13 ошибок, 269
предупреждений.

---

## 4. Definition of done (11.2), 25 пунктов × 8 тем

Последняя колонка — расхождение с самооценкой автора (`WRITE-0B.md` § 4.1).
Пусто — статусы совпали. Автор поставил 200 статусов (25 × 8); мои отличаются
в 52 клетках: 24 — потому что появились линтеры и глоссарий, которых при письме
не было; 16 — переклассификация «н/п» в «н/д» и «+» в «±» по пунктам 11 и 21;
12 — по существу.

| № | Пункт | 04 | 05 | 06 | 07 | 08 | 09 | 10 | 11 | Расхождение с `WRITE-0B` |
|--:|---|:--:|:--:|:--:|:--:|:--:|:--:|:--:|:--:|---|
| | **Планирование** | | | | | | | | | |
| 1 | Уровень соответствует критерию отнесения | ± | ± | ± | ± | ± | ± | ± | ± | — |
| 2 | 2–5 целей активными глаголами | + | + | + | + | + | + | + | + | — (замерено: `teaches` 5·7 и 3 у 08) |
| 3 | Раздел есть в утверждённом оглавлении этапа | + | + | + | + | + | + | + | + | — (`gen_topics.py --check` чисто) |
| | **Содержание** | | | | | | | | | |
| 4 | Каждая цель достигается текстом | + | + | + | + | + | + | + | + | — |
| 5 | Режим не смешан | ± | ± | ± | ± | **+** | ± | ± | ± | 08: у L3 нет блока 10, смешивать нечем |
| 6 | Есть блок «зачем это в работе AppSec-инженера» | + | + | + | + | + | + | + | + | — (замерено: ровно один в каждой) |
| 7 | L1: пример, чеклист, заблуждения, самопроверка, лаба | н/д | н/д | н/д | н/д | н/д | н/д | н/д | н/д | — |
| 8 | L2: чеклист 5–7 пунктов и 2–3 ссылки на первоисточник | + | + | + | + | н/д | − | − | − | — (Ф-41; замерено, § 6.5) |
| 9 | L3: своё объяснение и свой пример | н/д | н/д | н/д | н/д | + | н/д | н/д | н/д | — |
| | **Достоверность** | | | | | | | | | |
| 10 | Каждое утверждение сведено к лично открытому источнику | **±** | + | **±** | **±** | **±** | **±** | **±** | **−** | автор поставил плюс всем восьми; 7 расхождений, Ф-22…Ф-30 |
| 11 | `[авто]` Примеры запущены лично и проходят в CI | **н/д** | **н/д** | **н/д** | **н/д** | **н/д** | **н/д** | **н/д** | **н/д** | у автора «н/п»: исполняемых примеров нет по факту, а не «нечем проверить» |
| 12 | Проставлены маркеры уверенности | + | + | + | **±** | + | + | **±** | + | Ф-30 (07), Ф-27 (10) |
| 13 | Числа, версии, флаги, CVE проверены вручную | + | + | + | + | **−** | + | + | + | Ф-22 (08): «три требования MUST» вместо четырёх |
| 14 | Нет советов, не обоснованных первоисточником | + | + | + | + | + | **±** | + | + | Ф-28 (09): совет усилен против источника |
| | **Форма** | | | | | | | | | |
| 15 | `[авто]` Vale — без ошибок | обвязка | обвязка | обвязка | обвязка | обвязка | обвязка | обвязка | обвязка | у автора «н/п»: теперь прогнано, 0 ошибок, 85 предупреждений на восьмёрку |
| 16 | `[авто]` markdownlint — без ошибок | обвязка | обвязка | обвязка | обвязка | обвязка | обвязка | обвязка | обвязка | у автора «н/п»: теперь прогнано, 0/0 |
| 17 | `[авто]` Битых ссылок нет | + | + | + | + | + | + | + | + | — (проверено руками: 24 адреса из 25 отдали 200, § 6.4) |
| 18 | Терминология соответствует глоссарию | **±** | **±** | **+** | **+** | **±** | **+** | **+** | **+** | у автора «н/п»: глоссарий есть, G-CANON 0, G-FIRST 4 (Ф-46) |
| 19 | Перечитан вслух или через сутки | − | − | − | − | − | − | − | − | — |
| 20 | Не повторяет вводные абзацы соседних | + | + | + | + | + | + | + | + | — (замерено: 0 общих четырёхграмм прозы) |
| | **Интеграция** | | | | | | | | | |
| 21 | Пререквизиты и «дальше»; встроен в навигацию | **±** | **±** | **±** | **±** | **±** | **±** | **±** | **±** | пререквизиты и блок 13 есть у всех восьми; встраивания в навигацию нет — движка нет |
| 22 | Работает виджет обратной связи | н/п | н/п | н/п | н/п | н/п | н/п | н/п | н/п | — |
| 23 | Зафиксирован замер: дата, часы, уровень | + | + | + | + | + | + | + | + | — |
| | **Отложенно** | | | | | | | | | |
| 24 | В очереди на перечитывание через 4–8 недель | − | − | − | − | − | − | − | − | — |
| 25 | L1 — в очередь на внешнее ревью | н/д | н/д | н/д | н/д | н/д | н/д | н/д | н/д | — |

**Где я не согласен с автором и почему.**

**Пункт 10 — главное расхождение захода.** Автор поставил плюс всем восьми
темам; у меня плюс остался один. Это не спор о строгости: в семи темах нашлось
конкретное место, где утверждение расходится со своим же источником, или
опирается на источник, которого нет ни в блоке 14, ни в `sources.yaml`, ни в
маркерах (Ф-22…Ф-30). Три из этих мест — фактические ошибки, исправленные на
месте (§ 9). Отдельно отмечу, что общий уровень работы с источниками остался
таким же, каким его застала первая приёмка: из примерно восьмидесяти проверяемых
утверждений неверными оказались три. Плюс всем восьми — это не завышенная
оценка качества, это неверная оценка *проверки*: автор проверил не всё, что
пометил проверенным.

**Пункт 13 против пункта 10.** Ошибка в 08 — единственная числовая: «три
требования уровня MUST» там, где RFC 6797 даёт четыре. Остальные две ошибки не
числовые, поэтому пункт 13 у 04 и 07 остаётся плюсом, а пункт 10 — нет. Разница
между строками существенная: числа автор действительно вычитывал, а вот
пересказ нормативных формулировок — не всегда.

**Пункты 15, 16, 18.** У автора «н/п» — на момент письма линтеров и глоссария
не было. Теперь они есть, и статусы заменены измерением, а не мнением. Vale и
markdownlint дают ноль ошибок; глоссарный линтер даёт четыре предупреждения
G-FIRST на четыре аббревиатуры, раскрытые не при первом употреблении.

**Пункт 21.** Мелкое, но систематическое: «встроен в навигацию» проверить нечем
и нечем выполнить — движка сайта нет. Плюс здесь означал бы, что пункт закрыт;
он закрыт наполовину.

---

## 5. Сверка с первоисточниками

Проверялись не пересказы, а сами документы. В `.smgr/appsec-review-8/src/`
лежат 56 файлов на 14 МБ: 27 текстов первоисточников после снятия разметки,
23 исходных страницы, пять глав ASVS (V3, V4, V7, V9, V12) в markdown и
`asvs-v5.json`. Идентификаторы CWE открывались по одному на `cwe.mitre.org`.
Ниже — что именно сверено, а не «всё проверено».

**Даты и статусы документов — сверены дословно.** HTML Standard «Last Updated
21 August 2026» (тема говорит «правка 21 августа 2026»); Fetch Standard «Last
Updated 20 August 2026»; CSP Level 3 «W3C Working Draft, 13 August 2026»;
RFC 9846 «July 2026», обсолечивает 5077, 5246, 6961, 7627, 8422, 8446 — ровно
шесть, как в теме; RFC 9111 действительно STD 98; `draft-ietf-httpbis-rfc6265bis-22`
датирован 1 декабря 2025; OpenAPI 3.2.0 — 19 сентября 2025. Ни одной ошибки в
датах, статусах и номерах версий на восемь тем.

**04 `sessions-vs-tokens`.** Свойства идентификатора сессии сверены с Session
Management Cheat Sheet, требования — с ASVS 7.2.3, 7.2.4, 9.1.2. Врезка о
расхождении «не менее 64 бит» (Cheat Sheet) против «не менее 128 бит»
(ASVS 7.2.3) проверена по обоим источникам и верна: расхождение существует,
показано, а не сглажено — это сильное место темы. Модель JWT сверена с RFC 7519
§ 3 и § 6; здесь нашлась ошибка в ответе на первый вопрос самопроверки —
**Ф-23, исправлена на месте**. Три CWE (384, 613, 347) открыты лично, заголовки
совпадают, версия списка 4.20. Тело печатает два требования, которых нет в
шапке: v5.0-7.4.2 (строка 230) и v5.0-9.1.3; маркеры уверенности называют
сверенными оба (Ф-31).

**05 `same-origin-policy`.** Определение origin, сравнение по схеме, хосту и
порту, статус сеттера `document.domain` сверены с HTML Standard §§ 7.1.1,
7.1.1.1, 7.1.1.2; спецификация действительно помечает сеттер удаляемым, и тема
это передаёт точно. Три категории кросс-доступа и модель cookie — с MDN. Пример
`store.company.com` взят из MDN дословно (см. С-07 про плейсхолдеры). Тело
печатает ASVS v5.0-3.5.1, которого нет в шапке (Ф-31). CWE 346 верен. **Ни одного
фактического расхождения.** Замечание об уровне темы — § 12, статусами не
наказана.

**06 `cors`.** Состав полей `Access-Control-*`, определение предварительного
запроса, набор безопасных полей запроса и **значение по умолчанию
`Access-Control-Max-Age` = 5 секунд** сверены с Fetch Standard § 3.3;
единственная таблица набора (2 столбца, 6 строк) проверена построчно и верна
целиком. Формулировка «Multiple CORS header 'Access-Control-Allow-Origin' not
allowed» найдена в MDN CORS Errors дословно. Расхождение одно, зато
методическое: тема пишет, что ответ **обязан** нести `Vary: Origin`, и относит
это к MDN, где стоит «should», — при том что нормативная формулировка есть в
Fetch, и Fetch у темы в списке (**Ф-29**). CWE 942 и 346 верны.

**07 `csp`.** Грамматика `nonce-source` и `hash-source` (§ 2.3.1) и назначение
`Content-Security-Policy-Report-Only` (§ 3.2) сверены с текстом W3C. Там же
нашлась инверсия субъекта и объекта в описании сопоставления источников —
**Ф-24, исправлена на месте** по § 6.7.2.7. Отдельно: маркеры уверенности темы
утверждают, что текст спецификации за пределами § 4.5 из среды миссии
недоступен, и потому нормативные формулировки про `'strict-dynamic'` и
`'unsafe-inline'` взяты у MDN. Это неверно: документ выкачан целиком
(1 010 768 байт, 6 552 строки после снятия разметки), и нужные нормативные шаги
стоят в § 6.7.3.2 и § 8.2 (**Ф-30**). CWE 693 и 1021 верны.

**08 `security-headers`.** Директивы HSTS, обязательность `max-age`,
необязательность `includeSubDomains`, поведение при `max-age=0` сверены с
RFC 6797. Здесь нашлась единственная числовая ошибка восьмёрки: «три требования
уровня MUST» при четырёх перечисленных в том же абзаце — **Ф-22, исправлена на
месте**. Статус `X-Frame-Options` проверен по обоим источникам: ASVS 3.4.6
дословно говорит «is obsolete and may not be relied upon», Cheat Sheet называет
поле вытесненным — тема передаёт это верно. Примечание ASVS 3.4.5 действительно
называет «path and query data». Шапка объявляет 3.4.4 «сверенным», но требование
в тексте не печатается ни разу, а печатается 3.4.6, которого в шапке нет
(Ф-31). CWE 1021 и 319 верны.

**09 `tls-and-proxy`.** Фазы рукопожатия, состав сообщения `Certificate`, выбор
и приём сертификата сверены с RFC 9846 §§ 2, 4.5.1, 4.5.1.2, 4.5.1.3 — все
разделы существуют под этими номерами и говорят то, что сказано в теме.
Алгоритм проверки пути — с RFC 5280 §§ 6.1, 6.1.3. Три страницы документации
Burp открыты лично; расхождение заголовка «Proxy intercept» с оглавлением
раздела тема замечает сама, и это верно. Дефект один, и он в языке, а не в
факте: два хеджа источника сняты при переводе — «any untrusted party» стало
«никому», «may be able to intercept» стало «сможет перехватывать» (**Ф-28**).
CWE 295 и 319 верны.

**10 `app-architecture`.** Поле `Forwarded`, недостоверность пересылаемых
сведений и утечка через них сверены с RFC 7239 §§ 4, 8.1, 8.2. Разбор
`X-Forwarded-For` с крайним левым элементом проверен отдельно и **верен** —
это то место, где ошибиться проще всего. Правила кеша сверены с RFC 9111: § 3.5
действительно называет ровно `must-revalidate`, `public` и `s-maxage`, и ответ
на вопрос самопроверки про кеш верен. Словарь способов рендеринга сверен с
web.dev. Два дефекта сводятся к сноскам, не к фактам: ответ на шестой вопрос
опирается на MDN CORS Errors, которого нет ни в блоке 14 темы, ни в её маркерах
(**Ф-27**), а седьмой пункт чеклиста вводит `X-Content-Type-Options` и
ASVS 3.4.4, о которых в теле нет ничего (**Ф-39**). Идентификатор
ASVS v5.0-3.4.4 при этом **верен** — я проверил его отдельно, потому что
подозревал ошибку. CWE 348 и 524 верны.

**11 `rest-and-graphql`.** Пять мест параметра (§ 4.12.1) и объект схемы
безопасности (§ 4.27) сверены с OpenAPI 3.2.0. Утверждение о том, что место
`querystring` появилось в 3.2.0, проверено сравнением с текстом 3.1.1: в 3.1.1
слово не встречается ни разу, в 3.2.0 стоит в перечне мест — **утверждение
верно**. Оговорки про интроспекцию и контроль спроса сверены со страницами
graphql.org/learn. Но два утверждения темы опираются на документы, которых у
неё нет: «спецификация обязывает» — про поведение `__typename` и `__schema`, а
это спецификация GraphQL, и её нет ни в блоке 14, ни в `sources.yaml`, ни в
маркерах (**Ф-25**); сравнение с версией 3.1 сделано без источника на 3.1
(**Ф-26**). Сами утверждения я проверил и они **верны** — спецификация GraphQL
(October 2021 Edition) §§ 4.1 и 4.2 говорят именно это, — но проверил их я, а не
автор: в теме нет следа этой проверки. CWE 200 и 770 верны.

**Итог сверки.** Из примерно восьмидесяти проверяемых утверждений неверными
оказались три (Ф-22, Ф-23, Ф-24) — все три исправлены на месте, § 9. Ещё в пяти
местах утверждение верно, но не сведено к источнику, который у темы объявлен
(Ф-25…Ф-27, Ф-29, Ф-30). Ни одной выдуманной ссылки, ни одного выдуманного
номера раздела, ни одного суррогатного источника вместо стандарта — на этом
уровне работа держится.

---

## 6. Измерения

Каждое число ниже получено командой, запущенной 2026-08-22 в этом каталоге.
Команды названы, чтобы их можно было повторить.

### 6.1 Объём

`.venv-tools/bin/python tools/wordcount.py content/stage-0/*.md`

| Тема | Проза | Норма | Превышение | Текст без служебного аппарата | Превышение |
|---|--:|--:|--:|--:|--:|
| 04 | 1327 | ≤1200 | +10,6 % | 1245 | +3,8 % |
| 05 | 1365 | ≤1200 | +13,8 % | 1319 | +9,9 % |
| 06 | 1297 | ≤1200 | +8,1 % | 1258 | +4,8 % |
| 07 | 1349 | ≤1200 | +12,4 % | 1312 | +9,3 % |
| 09 | 1261 | ≤1200 | +5,1 % | 1210 | +0,8 % |
| 10 | 1374 | ≤1200 | +14,5 % | 1336 | +11,3 % |
| 11 | 1370 | ≤1200 | +14,2 % | 1334 | +11,2 % |
| **Семь L2, среднее** | **1334,7** | ≤1200 | **+11,2 %** | **1287,7** | **+7,3 %** |
| 08 (L3) | 521 | ≤500 | +4,2 % | 462 | **в норме** |

Что здесь важно кроме самой цифры. Норму превышают все восемь тем, но по второй
метрике L3-тема в норму **попадает**, а среднее превышение L2 падает с 11,2 % до
7,3 %. Разница между колонками — это ровно те двести слов служебного аппарата
(состав блоков, скоропортящийся слой, маркеры уверенности, каркас этапа), которые
свод предписывает частью 4, 9.6 п. 21 и 11.2 п. 12, но в норму 800–1200 слов не
заложил. То есть вопрос «резать текст или править норму» стоит не в вакууме:
пока метод подсчёта не назван, обе цифры законны, и обе отличаются в полтора
раза по величине превышения. Это находка Ф-09 первой приёмки, не снятая до сих
пор; сама по себе восьмёрка объём держит ровно и без выбросов — разброс
превышения 5–14 %, ни одной темы вдвое длиннее нормы.

### 6.2 Повторы и запрещённые конструкции

`.venv-tools/bin/python tools/stoplist.py content/stage-0/*.md`

- **Общих четырёхграмм прозы во всех восьми темах — 0.** Во всех двенадцати
  темах гайда — тоже 0. Шаблонности по этой мере нет: DoD п. 20 выполнен
  измерением, а не на слово.
- **Совпадений по 6.5 — шесть, из них законных пять.** Три — знак `!` внутри
  листингов (`!==` в 05, `[Invoice!]!` в 11): язык кода под редполитику не
  попадает. Два — термин «простой запрос» из Fetch Standard в 06, и он **формально
  снят** записью в `tools/exceptions.yaml` с письменным обоснованием. Шестое —
  «современного» в заголовке 10, и это настоящее нарушение (Ф-40).
- **Накопительный стоп-лист связок**: пять совпадений в 04, одно в 05, ноль в
  остальных шести. Связки, найденные первой приёмкой, в новых темах почти не
  воспроизводятся.

### 6.3 Обвязка параллельной миссии

`.venv-tools/bin/python tools/check.py --full` — 12 тем, 10 проверок. Полный
вывод сохранён в `.smgr/appsec-review-8/check-full2.txt`.

| Линтер | Ошибок | Предупр. | Снято |
|---|--:|--:|--:|
| vale | 0 | 160 | 2 |
| markdownlint | 0 | 0 | 0 |
| glossary | 8 | 5 | 0 |
| style | 0 | 100 | 0 |
| model | 5 | 4 | 0 |
| **Итог по репозиторию** | **13** | **269** | **2** |

Прогон **не проходит**. Но ни одна из восьми принимаемых тем не отвечает за
восемь ошибок глоссаря: все восемь — `G-CANON` «argon2id» в
`content/stage-1/password-storage.md`, вне набора. **Пять ошибок из тринадцати —
в восьмёрке**, и все пять одного рода: `C-FM-TITLE-LEN`, заголовок длиннее 60
символов (10 — 95, 08 — 89, 09 — 78, 06 — 80, 11 — 63). Это Ф-34, найденная
глазами до того, как линтер появился, и подтверждённая им независимо.

Предупреждений в восьмёрке — 156. По правилам: `L-NBSP` 61, `S-SENT-LONG` 66,
`L-CLERICAL2` 22, `G-FIRST` 4, `L-TIMELESS` 2, `C-BODY-CHECKLIST` 1.

| Тема | 04 | 05 | 06 | 07 | 08 | 09 | 10 | 11 |
|---|--:|--:|--:|--:|--:|--:|--:|--:|
| Предупреждений | 20 | 16 | 19 | 22 | 14 | 20 | 29 | 16 |

Что за этими числами стоит — проверено по одному, а не оценено на глаз:

- **`L-CLERICAL2`, 22 совпадения — все ложные.** Правило само просит проверить
  глазами; я вывел все двадцать два в контексте: везде существительное
  «учётные данные», «входные данные», «данные изменит». Ни одного местоимения
  «данный». В статусах эти предупреждения не учитываются.
- **`S-SENT-LONG`, 66 совпадений — все настоящие.** 6.1 говорит: предложение
  свыше 25 слов переписывается. Самое длинное в восьмёрке — 61 слово
  (`app-architecture.md:75`). Распределение по темам: 5, 8, 7, 8, 4, 9, 14, 11.
  В чеклисте части 13 строки для длины предложения нет вовсе — Ф-45.
- **`L-NBSP`, 61 совпадение — все настоящие.** 6.4 требует неразрывного пробела
  в «§ 4», «10 МБ», «30 мин». Символа U+00A0 в этих восьми темах **ноль** — я
  посчитал отдельно, — и во всём каталоге `content/` тоже ноль при 130
  предупреждениях по репозиторию. То есть правило 6.4 не выполнено нигде и
  никогда: это дефект не восьмёрки, а всего гайда (Ф-47).
- **`G-FIRST`, 4 совпадения — настоящие**: аббревиатура раскрыта не при первом
  употреблении (Ф-46). `G-CANON` в восьмёрке — 0.
- **`C-BODY-CHECKLIST`, 1 совпадение**: восемь пунктов чеклиста в 11 при норме
  5–7 (Ф-41). Тот же линтер нашёл восьмипунктные чеклисты ещё в двух темах —
  `cookies.md` и `http-basics.md`, — а это темы, **уже прошедшие приёмку**. Их
  первая приёмка пропустила; замечание для оператора, не для этого набора.

`.venv-tools/bin/python tools/validate.py` — **ЗЕЛЁНЫЙ, 0 ошибок, 12
предупреждений**; реестр 307 записей. Из предупреждений к восьмёрке относятся
три `ГЛУБИНА` — у 09 пять собственных источников, у 10 и 11 по четыре при норме
до трёх (Ф-41), — и ни одно не относится к её фактам.
`.venv-tools/bin/python tools/gen_topics.py --check` — «topics.yaml
соответствует плану».

### 6.4 Ссылки

Линк-чекера в `tools/` нет, поэтому проверялось руками:
`curl -sS -o /dev/null -w '%{http_code}' -A "<UA браузера>" -L --max-time 45`
по каждому адресу.

Из восьми тем извлечено 33 совпадения `https?://`, из них 8 — плейсхолдеры
шести видов (`app.example.com`, `admin.example.com`, `widgets.example.com`,
`widgets.example.com.attacker.test`, `rabbit.invalid`, `store.company.com`), они
никуда не ведут по замыслу. Остаётся **25 живых адресов: 24 отдали 200**.

Единственное исключение — `https://www.w3.org/TR/CSP3/`: **403**. Адрес при этом
**верен**: с него в этом же заходе выкачан полный документ на 1 010 768 байт, и
403 отдаётся Cloudflare по повторным обращениям из командной строки, а не
сервером спецификации. Формулирую по правилу досье: **не подтверждено
автоматически, источник закрыт из этой среды; адрес проверен и правилен**.

Ловушки среды, названные в досье, в этот раз сработали не все: `graphql.org`
отдал 200 оба раза (обе страницы `/learn/`), `cisa.gov` в восьмёрке не
встречается вовсе — запись `cisa-bod-22-01` висит в реестре сиротой,
`*.gov.ru` и `ptsecurity.com` в этих темах не используются.

### 6.5 Механика формы

Состав и порядок блоков, `grep` по заголовкам `^## N.`:

| Тема | Блоки | Чеклист | Вопросов 12 | Целей | Пунктов 13 | Источников | Листингов | `УЯЗВИМО` | Врезок | Схем |
|---|---|--:|--:|--:|--:|--:|--:|--:|--:|--:|
| 04 | 0 1 3 5 6 9 10 11 12 13 14 | 6 | 5 | 5 | 4 | 3 | 3 | 1 | 2 | 1 |
| 05 | 0 1 3 5 6 9 10 11 12 13 14 | 6 | 5 | 5 | 4 | 2 | 2 | 1 | 2 | 0 |
| 06 | 0 1 3 5 6 9 10 11 12 13 14 | 6 | 6 | 5 | 4 | 3 | 2 | 1 | 2 | 0 |
| 07 | 0 1 3 5 6 9 10 11 12 13 14 | 7 | 6 | 5 | 4 | 2 | 2 | 1 | 2 | 0 |
| 08 | 0 3 13 14 | — | — | 3 | 3 | 2 | 1 | 0 | 0 | 0 |
| 09 | 0 1 3 5 6 9 10 11 12 13 14 | 6 | 6 | 5 | 3 | 5 | 2 | 1 | 2 | 0 |
| 10 | 0 1 3 5 6 9 10 11 12 13 14 | 7 | 6 | 5 | 4 | 4 | 2 | 1 | 2 | 0 |
| 11 | 0 1 3 5 6 9 10 11 12 13 14 | 8 | 6 | 5 | 4 | 4 | 3 | 2 | 2 | 0 |

Порядок блоков возрастающий во всех восьми, ни одной перестановки. Состав
одинаков у всех семи L2-тем и отличается от списка 3.1 на три блока — 11, 13, 14,
— каждый из которых требует другое место свода (разбор в § 3, пункт 1; находки
С-02 и С-03). Число источников в шапке совпадает с числом записей в блоке 14 у
всех восьми.

Листинги: 17 огороженных блоков, из них 16 — код и один — схема `mermaid`. Язык
объявлен у каждого. Самая широкая строка — 73 символа при норме 76, самый
длинный листинг — 10 строк при норме 25, подсветки строк нет нигде. Все восемь
блоков `УЯЗВИМО` несут маркер в первой строке (7.2). Пятнадцать листингов из
шестнадцати несут маркер вида примера (7.1 п. 11); шестнадцатый — транскрипт в
08 (Ф-38).

Визуальное: врезок ровно две в каждой L2-теме и ни одной в L3 — при норме 7.6
п. 42 «до пяти на страницу». Схема на восемь тем **одна** (Ф-33), и у неё есть
«Описание схемы» и текстовое дублирование смысла — 7.4 п. 32 и п. 34 выполнены.
Таблица на восемь тем **одна** (в 06, два столбца, шесть строк, проверена
построчно по Fetch Standard). Скриншотов — ноль, аналогий — ноль.

Отдельно проверено, чего в восьмёрке нет и что в уже принятых темах есть: **ни
одного нарушения 7.6 пп. 42 и 45** (врезка сразу после заголовка, врезка длиннее
пяти строк). В `cookies.md` и `url-and-encoding.md` таких мест четыре — первая
приёмка их не поймала; замечание для оператора.

---

## 7. Реестр находок

Нумерация продолжает первую приёмку: та закрылась на Ф-21. Серьёзность — по её
же шкале: **высокая** — искажает картину для читателя или решение оператора;
**средняя** — портит конкретную тему; **низкая** — правится в рабочем порядке.

Двадцать шесть находок: три исправлены на месте, двадцать три оставлены списком.
Правки на месте — только фактические ошибки, как велит досье; всё остальное не
моя работа.

### 7.1 Достоверность

**Ф-22. Высокая. «Три требования уровня MUST» при четырёх перечисленных.**
`content/stage-0/security-headers.md:82`, блок 3.
RFC 6797 задаёт эти четыре требования в §§ 7.2, 8.1, 8.5 и 8.4: узел не должен
присылать поле по незащищённому транспорту (§ 7.2); клиент обязан игнорировать
поле, полученное по незащищённому транспорту (§ 8.1); клиент обязан игнорировать
`http-equiv` с этим именем (§ 8.5); клиент обязан разорвать соединение при любой
ошибке транспорта (§ 8.4). В самом абзаце перечислены все четыре — числительное
противоречило собственному перечислению строкой ниже, а маркеры уверенности той
же темы говорят «четыре».
Воспроизведение: `grep -n 'MUST NOT include the STS header'
.smgr/appsec-review-8/src/rfc6797.txt` и счёт перечисленного в абзаце.
**Было:** «Три требования уровня MUST». **Стало:** «Четыре требования уровня
MUST», абзац переложен по ширине. Подробности — § 9.
Почему высокая: тема — про заголовки, а число требований стандарта читатель
запоминает как факт.

**Ф-23. Средняя. «Каждая часть JWT декодируется в осмысленный JSON».**
`content/stage-0/sessions-vs-tokens.md:279`, ответ на первый вопрос самопроверки.
RFC 7519 § 3: «A JWT is represented as a sequence of URL-safe parts separated by
period ('.') characters. Each part contains a base64url-encoded value. The number
of parts in the JWT is dependent upon the representation of the resulting JWS …
or JWE …». То есть части несут base64url-значения, а не непременно JSON: подпись
JWS — не JSON, у Unsecured JWT (§ 6) она и вовсе пустая строка, а число частей
зависит от представления.
Воспроизведение: `sed -n '318,330p' .smgr/appsec-review-8/src/rfc7519.txt`.
**Стало:** ответ говорит про base64url-значения, про зависимость числа частей от
представления и отдельно — какие части JSON не содержат. Подробности — § 9.
Почему средняя: ошибка в ответе, то есть в том месте, которое читатель принимает
как эталон, но сам приём распознавания токена описан верно.

**Ф-24. Средняя. Инверсия субъекта и объекта в описании сопоставления
источников.** `content/stage-0/csp.md:88`, блок 3.
Было: «Если **ресурс разрешает** хотя бы одно из перечисленных выражений, ресурс
разрешён». Сопоставляет не ресурс, а алгоритм: CSP Level 3 § 6.7.2.7 «Does url
match source list in origin with redirect count?» возвращает `Matches`, если
**URL соответствует** одному или более выражению из списка.
Воспроизведение: `grep -n 'match source list in origin'
.smgr/appsec-review-8/src/csp3.txt`.
**Стало:** «Если ресурс **соответствует** хотя бы одному из перечисленных
выражений, ресурс разрешён». Подробности — § 9.
Почему средняя: формулировка ставит смысл с ног на голову ровно в том месте, где
читатель учится читать директиву, но остальной разбор политики верен.

**Ф-25. Высокая. «Спецификация обязывает» — спецификации у темы нет.**
`content/stage-0/rest-and-graphql.md:109`.
Утверждение о том, что мета-поле `__typename` спецификация обязывает
поддерживать, — про спецификацию GraphQL. В блоке 14 темы её нет; в
`sources.yaml` записи о ней нет; в маркерах уверенности она не названа. Четыре
источника темы — OpenAPI, две страницы `graphql.org/learn` и OWASP REST Cheat
Sheet; `graphql.org/learn` — учебные страницы, не нормативный документ.
Проверил сам: **утверждение верно** — GraphQL specification, October 2021
Edition, § 4.1 и § 4.2 говорят именно это.
Воспроизведение: `grep -n '__typename' .smgr/appsec-review-8/src/gql-spec.txt`.
Почему высокая: слово «обязывает» — ссылка на норму, и читатель, пришедший
проверить, не найдёт по теме ни адреса, ни номера раздела. Это ровно тот дефект,
против которого написано 11.2 п. 10.

**Ф-26. Средняя. Сравнение с версией 3.1 без источника на 3.1.**
`content/stage-0/rest-and-graphql.md:84–86`, врезка.
Тема утверждает, что место параметра `querystring` появилось в 3.2.0 и что
инструменты, знающие версию 3.1, его не понимают. В блоке 14 названа только
3.2.0.
Проверил сам: в тексте 3.1.1 слово `querystring` не встречается ни разу, в 3.2.0
стоит в перечне мест, — **утверждение верно**.
Воспроизведение: `grep -c querystring .smgr/appsec-review-8/src/openapi311.txt`
→ `0`; `grep -c querystring .smgr/appsec-review-8/src/openapi.txt` → больше нуля.
Почему средняя: сравнительное утверждение всегда о двух документах, и второй
должен быть назван.

**Ф-27. Средняя. Ответ опирается на источник, которого у темы нет.**
`content/stage-0/app-architecture.md:295`, ответ на шестой вопрос.
Ответ говорит: «MDN называет эту причину отдельной строкой в списке отказов».
Это страница MDN «CORS errors»; в блоке 14 темы её нет, в маркерах — тоже.
Страница есть в реестре (`mdn-cors-errors`) и в теме `cors`, то есть добавить её
дёшево.
Проверил сам: строка «Reason: Multiple CORS header 'Access-Control-Allow-Origin'
not allowed» в списке есть — **утверждение верно**.
Воспроизведение: `grep -n "Multiple CORS header"
.smgr/appsec-review-8/src/mdn-cors-errors.txt`.

**Ф-28. Высокая. Три хеджа источника сняты в одном предложении.**
`content/stage-0/tls-and-proxy.md:120–122`.
Источник — документация Burp, страница про удостоверяющий центр: «You should not
disclose the private key for your certificate to **any untrusted party**. **A
malicious attacker** in possession of your certificate and key **may be able to**
intercept your browser's HTTPS traffic even when you are not using Burp».
Тема: «закрытый ключ не следует раскрывать **никому**, потому что **владеющий**
вашим сертификатом и ключом **сможет** перехватывать ваш трафик HTTPS».
Потеряно: условие «недоверенной стороне» (стало «никому» — то есть и своему
коллеге, и себе), фигура злоумышленника (стал любой владелец) и модальность
«может оказаться способен» (стала «сможет»).
Воспроизведение: `grep -n 'untrusted party'
.smgr/appsec-review-8/src/burp-tools-proxy-manage-certificates.txt`.
Почему высокая: 6.2 прямо запрещает снимать хеджирование, несущее условие
применимости, и здесь снято именно условие. Три потери в одном предложении — это
уже не случайность формулировки, а способ перевода.

**Ф-29. Средняя. «Обязан» вместо «should», при том что нормативный источник у
темы есть.** `content/stage-0/cors.md:125–128`, врезка.
Тема: «MDN добавляет к этому правило кеширования: … ответ **обязан** нести
`Vary: Origin`». MDN говорит «the server **should** also include Origin in the
Vary response header». Нормативная формулировка есть в Fetch Standard — «…origin,
`Vary` **is to be used**», — и Fetch стоит первым источником этой самой темы.
Воспроизведение: `grep -n 'Vary' .smgr/appsec-review-8/src/mdn-cors.txt` (строка
1191) и `grep -n 'is to be used' .smgr/appsec-review-8/src/fetch.txt` (строка
6414).
Почему средняя: сам совет правильный и важный, но модальность приписана
источнику, который её не даёт, — а тот, который даёт, не назван.

**Ф-30. Высокая. Маркеры уверенности объявляют недоступным то, что доступно.**
`content/stage-0/csp.md:342–346`.
Маркеры утверждают: «текст спецификации за пределами § 4.5 из среды миссии
недоступен … Поэтому нормативные формулировки о `'strict-dynamic'` и
`'unsafe-inline'` взяты у MDN, а не у W3C». Это неверно: документ доступен
целиком и в этой самой среде — 1 010 768 байт, 6 552 строки после снятия
разметки, — а нужные нормативные шаги стоят в § 6.7.3.2 («Does element match
source list for type and source?», где разобрано взаимодействие
`'strict-dynamic'` с `'unsafe-inline'`) и в § 8.2.
Воспроизведение: `wc -c .smgr/appsec-review-8/src/csp3.html` → `1010768`;
`grep -n "strict-dynamic" .smgr/appsec-review-8/src/csp3.txt | head`.
Почему высокая: это маркер уверенности, то есть обещание читателю о том, откуда
взято утверждение. Ложное ограничение хуже отсутствующего: оно снимает вопрос,
который стоило задать, и делает вид, что первоисточник проверить нельзя. Отмечу
рядом честную часть: сама трудность реальна — адрес отдаёт 403 при повторных
обращениях (§ 6.4), и первый заход автора мог упереться в неё. Записывать
предположение о причине в маркеры как факт всё равно нельзя.

**Ф-31. Средняя. Шапка и тело расходятся по требованиям ASVS в трёх темах из
восьми.**
`sessions-vs-tokens` — тело печатает `v5.0-7.4.2` (строка 230) и `v5.0-9.1.3`,
ни того ни другого в шапке нет, а маркеры уверенности называют сверенными оба;
`same-origin-policy` — тело печатает `v5.0-3.5.1`, в шапке его нет;
`security-headers` — шапка объявляет `v5.0-3.4.4` и называет его сверенным, но в
тексте требование не печатается ни разу, зато печатается `v5.0-3.4.6`, которого
в шапке нет.
Воспроизведение: сравнить множество `asvs` из frontmatter с
`grep -o 'ASVS v5\.0-[0-9.]*'` по телу; расхождение ровно в трёх файлах.
Почему средняя: шапка — это то, из чего потом собирается индекс покрытия. Пока
индекса нет, дефект дешёвый; после его появления он станет ложными данными.

**Ф-32. Средняя. Шестнадцать упоминаний CWE без версии, без записей в реестре и
без маппинг-индекса.**
Строки «маппинг» в шапках всех восьми тем: `CWE-384`, `CWE-613`, `CWE-347`,
`CWE-346`, `CWE-942`, `CWE-693`, `CWE-1021`, `CWE-319`, `CWE-295`, `CWE-348`,
`CWE-524`, `CWE-200`, `CWE-770` — тринадцать разных идентификаторов, шестнадцать
упоминаний.
9.6 п. 25 требует печатать внешние идентификаторы с версией; ни одного номера
версии нет. 9.6 п. 24 требует держать соответствие с внешними каталогами
отдельным маппинг-индексом; индекса в проекте нет. Записей о CWE в `sources.yaml`
тоже нет ни одной.
Проверил сам: **все тринадцать идентификаторов верны** — открыл каждый на
`cwe.mitre.org`, заголовки совпадают с тем, что подразумевает тема; текущая
версия списка — **4.20**, и именно её следует печатать.
Почему средняя: факты верны, форма нет; правится одной строкой в каждой шапке
плюс индекс.

### 7.2 Форма и схема

**Ф-33. Средняя. Одна схема на восемь тем; по эвристике 7.4 их должно быть
три-четыре.**
Единственная схема набора — `mermaid` в `sessions-vs-tokens.md:180`, с врезкой
«Описание схемы», как требует 7.4 п. 32. В остальных семи темах схем нет.
7.4 даёт эвристику: схема нужна там, где объясняется последовательность
взаимодействий или ветвление решения. Под неё прямо попадают: предполётный запрос
в `cors` (последовательность из двух обменов с ветвлением по результату),
подстановка удостоверяющего центра в `tls-and-proxy` (три стороны, две сессии
TLS), путь запроса через обратный прокси и шлюз в `app-architecture` (та самая
цепочка, на которой держится вся тема). У 7.4 есть и «Правило отсечения» — если
текст объясняет то же короче, схема не нужна, — так что находка не автоматическая:
это место, где автор обязан был решение зафиксировать, а не пропустить.
Воспроизведение: `grep -c '```mermaid' content/stage-0/*.md` по восьми файлам.
Почему средняя, а не высокая: тексты понятны и без схем; дефект в том, что
решение не принято, а просто не сделано.

**Ф-34. Высокая. Пять заголовков длиннее 60 знаков — блокирующая ошибка сборки.**
9.3 задаёт `title` не длиннее 60 знаков. Фактически:
`app-architecture` 95, `security-headers` 89, `cors` 80, `tls-and-proxy` 78,
`rest-and-graphql` 63. В норме три темы: `same-origin-policy` 53, `csp` 47,
`sessions-vs-tokens` 44.
Воспроизведение: `.venv-tools/bin/python tools/check.py --full` → пять ошибок
`C-FM-TITLE-LEN` с теми же числами; или разбор строки `title:` каждого файла.
Почему высокая: это единственные пять ошибок обвязки во всём наборе, и они
блокирующие — пока они есть, сборка красная. Заодно они делают h1 длиннее строки
в оглавлении.

**Ф-35. Высокая. Три ключа шапки вне схемы 9.3 во всех восьми темах.**
Каждая тема набора несёт `asvs`, `wstg` и `plan_id`; в перечне ключей 9.3 их нет.
9.4 объявляет несоответствие схеме блокирующей проверкой сборки — значит,
буквально прочитанный свод делает красными все восемь тем (и три принятых тоже).
Ключи полезны: `plan_id` связывает тему с планом обучения, `asvs` и `wstg` дают
внешнее покрытие. Дефект не в темах, а в том, что схема не догнала практику.
Воспроизведение: `grep -c '^plan_id:' content/stage-0/*.md` → 1 в каждом из
одиннадцати файлов; сверить с перечнем ключей в PLAYBOOK.md § 9.3.
Почему высокая: если проверку схемы включить буквально, не собирается ничего.
Правится одной правкой свода (§ 10, С-09), а не одиннадцатью правками тем.

**Ф-36. Средняя. Врезки «Откуда это взялось» нет ни в одной из восьми тем.**
6.6 требует: «Каждая тема, объясняющая механизм защиты или ограничение, содержит
врезку "Откуда это взялось"» (PLAYBOOK.md:360). Все восемь тем объясняют ровно
механизмы защиты и ограничения — правило единого источника, CORS, CSP, заголовки
безопасности, TLS. Врезки нет ни одной.
В принятых темах она есть — `cookies.md:130` и `url-and-encoding.md:154`, — то
есть приём в проекте живой и в первой приёмке претензий не вызвал.
Воспроизведение: `grep -rn 'Откуда это взялось' content/` → две строки, обе в
принятых темах.
Почему средняя: не ошибка факта, но пропуск требования свода, единый для всего
набора. Историческая справка в этих темах особенно к месту: правило единого
источника и CORS без истории выглядят произвольными.

**Ф-37. Средняя. Предполётный запрос описан прозой, транскрипта нет.**
`content/stage-0/cors.md`, блок 3.
7.3 п. 21 вводит транскрипт HTTP как отдельный тип блока и требует держать запрос
и ответ в разных блоках с подписями; п. 22 требует start-line, релевантные
заголовки, пустую строку и тело. В `cors` два листинга, и оба не транскрипты:
предполётный обмен — единственное место набора, где обмен из двух пар
«запрос-ответ» разобран целиком, — изложен прозой с врезкой имён заголовков.
Воспроизведение: `grep -n '```' content/stage-0/cors.md` и сверить с текстом
блока 3.
Почему средняя: содержание верное, форма не та, которую свод предписывает именно
для этого материала.

**Ф-38. Средняя. Единственный транскрипт L3-темы нарушает четыре требования
7.3.** `content/stage-0/security-headers.md:91–97`.
Один блок ```http содержит только ответ. Нет: блока запроса, подписей к обоим
блокам, пустой строки и тела (после заголовков блок кончается), маркировки
примера по 7.1 п. 11 — единственный немаркированный листинг набора из
шестнадцати.
Воспроизведение: `sed -n '88,99p' content/stage-0/security-headers.md`.
Почему средняя: тема короткая и с одним листингом, поэтому цена дефекта — этот
листинг целиком.

### 7.3 Обучение

**Ф-39. Средняя. Пункт чеклиста требует того, чего в теме нет.**
`content/stage-0/app-architecture.md:243`, пункт 7 чеклиста.
Пункт велит проверить, что шлюз не переписывает `X-Content-Type-Options`. В теле
темы этот заголовок не встречается ни разу — только в самом пункте. Читатель
получает требование, к которому тема не давала ни объяснения, ни критерия.
Воспроизведение: `grep -n 'X-Content-Type-Options'
content/stage-0/app-architecture.md` → одна строка, 243.
Почему средняя: 5.6 и 11.2 п. 6 требуют, чтобы чеклист вырастал из тела темы;
здесь пункт приехал из соседней темы.

**Ф-40. Низкая. Запрещённое сводом слово в заголовке первого уровня.**
`content/stage-0/app-architecture.md`, `title` и h1 (строки 4 и 33): «Модель
**современного** приложения: SPA + REST/GraphQL API, серверный рендеринг, reverse
proxy, CDN». 6.5 запрещает `современн\w+`, а `L-TIMELESS` требует назвать версию
или дату. Формулировка взята из плана обучения дословно (строка 18 плана), а план
— только для чтения; но `title` пишет автор темы, и переписать его можно, не
касаясь плана.
Воспроизведение: `.venv-tools/bin/python tools/check.py --full` → два `L-TIMELESS`
на `app-architecture.md:4` и `:33`, единственные во всём репозитории.
Почему низкая: одно слово, правится вместе с Ф-34 — заголовок этой темы и так
самый длинный в наборе (95 знаков).

**Ф-41. Средняя. Числа состава вне вилок свода в трёх темах.**
Источников: `tls-and-proxy` 5, `app-architecture` 4, `rest-and-graphql` 4 — при
вилке 11.2 п. 8 «2–3 ссылки на первоисточник». Пунктов чеклиста:
`rest-and-graphql` 8 — при вилке «5–7» того же пункта.
Оговорка по чеклисту: часть 4 разрешает «5–8 пунктов», то есть свод сам себе
противоречит (§ 8, С-01), и восьмой пункт нарушает только более узкую из двух
формулировок.
Оговорка по источникам: превышение вилки — единственный случай, когда я не считаю
находку дефектом содержания. Пять первоисточников в теме про TLS честнее двух.
Воспроизведение: `.venv-tools/bin/python tools/validate.py` → предупреждения
ГЛУБИНА для `t-0-0-09`, `t-0-0-10`, `t-0-0-11`;
`.venv-tools/bin/python tools/check.py --full` →
`C-BODY-CHECKLIST` для `rest-and-graphql`.
Почему средняя: расхождение с числом свода, к которому обвязка уже привязана, а
значит оно будет всплывать в каждой сборке.

**Ф-43. Низкая. Вопрос самопроверки не согласован грамматически.**
`content/stage-0/rest-and-graphql.md`, четвёртый вопрос блока 12: «Чем
ограничение глубины запроса в GraphQL отличается от ограничения размера тела
запроса в REST **и что каждое из них не закрывает**». Второе сочинённое
предложение теряет вопросительную рамку: получается «чем … отличается … и что не
закрывает», где второй вопрос требует другого падежа связки.
Воспроизведение: `grep -n 'не закрывает'
content/stage-0/rest-and-graphql.md`.
Почему низкая: смысл понятен, правится одним словом.

**Ф-44. Средняя. Правило возврата к темам N−2 и N−5 буквально соблюдено в одной
теме из семи.**
5.2 п. 10 требует, чтобы блок 12 возвращал читателя к темам с номерами N−2 и N−5.
Фактически: `same-origin-policy` (05) → `cookies` (03) — это N−2, а N−5 не
существует, то есть правило выполнено полностью; `cors` (06) → `http-basics` (01,
это N−5) плюс `same-origin-policy` (N−1) — половина; `csp` (07) →
`same-origin-policy` (05, это N−2) плюс `http-basics` — половина;
`sessions-vs-tokens` (04) → `cookies` (N−1), `tls-and-proxy` (09) → `cookies` и
`security-headers` (N−1), `app-architecture` (10) → `tls-and-proxy` (N−1) и `cors`
(N−4), `rest-and-graphql` (11) → `app-architecture` (N−1) и
`sessions-vs-tokens` (N−7) — ни одного попадания. У `security-headers` блока 12
нет по составу L3.
При этом более слабое требование части 13 («1–2 вопроса на возврат к прошлым
темам») выполнено во всех семи, и выбранные возвраты осмысленнее механических:
`cors` возвращает к правилу единого источника, без которого CORS не читается.
Воспроизведение: `grep -n 'Возврат к теме' content/stage-0/*.md`.
Почему средняя: расхождение со сводом реально и системно, но виновата здесь скорее
формулировка правила — см. С-10.

### 7.4 Мера и обвязка

**Ф-42. Высокая. Норма объёма превышена во всех семи темах L2, в среднем на
11,2 %.**
Проза (текст без служебного аппарата, шапки и блока 14) при норме 3.1 «800–1200
слов»: `sessions-vs-tokens` 1327, `same-origin-policy` 1365, `cors` 1297, `csp`
1349, `tls-and-proxy` 1261, `app-architecture` 1374, `rest-and-graphql` 1370.
Превышение от +5,1 % до +14,5 %, среднее по семи темам 1334,7 слова = **+11,2 %**.
Ни одна тема не укладывается. L3 `security-headers` — 521 слово прозы при норме
«300–500», превышение +4,2 %; по метрике «текст» (462) — в норме.
Досье прямо запрещает мне подгонять норму и требует измерить и записать. Записываю:
это не восемь независимых промахов, а систематическое расхождение нормы с тем, как
автор пишет тему, разобранную до критериев проверки. Решение — сокращать тексты
или менять вилку 3.1 — открытый вопрос оператора.
Воспроизведение: `.venv-tools/bin/python tools/wordcount.py content/stage-0/*.md`.
Почему высокая: решение оператора зависит прямо от этого числа, а тексты уже
написаны.

**Ф-45. Средняя. Шестьдесят шесть предложений длиннее 25 слов при правиле 6.1.**
6.1 велит переписывать предложение свыше 25 слов. По восьми темам обвязка даёт 66
предупреждений `S-SENT-LONG`; самое длинное — 61 слово,
`content/stage-0/app-architecture.md:75`. Распределение: `app-architecture` 14,
`rest-and-graphql` 11, `tls-and-proxy` 9, `csp` 8, `same-origin-policy` 8,
`cors` 7, `sessions-vs-tokens` 5, `security-headers` 4.
Отдельно замечу: в чеклисте части 13 строки про длину предложения нет вообще
(§ 8, С-12), поэтому по букве приёмки это не пункт — только предупреждение
обвязки. Обвязка здесь строже свода-в-части-13 и мягче свода-в-части-6.
Воспроизведение: `.venv-tools/bin/python tools/check.py --full`, фильтр по
`S-SENT-LONG`.
Почему средняя: 66 срабатываний — это не шум, а способ письма; при этом ни одно
из них не портит факт.

**Ф-46. Низкая. Четыре термина глоссария употреблены без раскрытия при первом
упоминании.** `G-FIRST`: два в `same-origin-policy` (строка 53: «CORS» и «CSP»),
по одному в `security-headers` (строка 105, «MITM») и `sessions-vs-tokens`
(строка 236, «URL»). Пятое срабатывание правила — в принятой `http-basics`
(строка 184, «URL»), то есть дефект не новый.
Воспроизведение: `.venv-tools/bin/python tools/check.py --full`, фильтр по
`G-FIRST`.
Почему низкая: правится вставкой раскрытия на месте.

**Ф-47. Низкая по каждому случаю, но сквозная. Неразрывных пробелов в проекте нет
ни одного.** 6.4 требует неразрывный пробел в конструкциях вида «§ 3», «10 МБ».
Фактически U+00A0 не встречается ни в одном файле `content/` — ноль вхождений во
всех одиннадцати темах. Обвязка находит 61 место в восьми темах, где он нужен, и
130 по всему каталогу.
Воспроизведение: `grep -c $' ' content/stage-0/*.md` → нули;
`.venv-tools/bin/python tools/check.py --full` и фильтр по `L-NBSP`.
Почему выношу отдельно: это не дефект восьми тем, а незакрытое требование
типографики во всём справочнике, включая три принятые темы. Правится один раз
скриптом по всему каталогу — и правку стоит делать после того, как параллельная
миссия закончит с обвязкой, чтобы не спорить за те же файлы.

### 7.5 Счёт

| Серьёзность | Сколько | Номера |
|---|--:|---|
| высокая | 7 | Ф-22, Ф-25, Ф-28, Ф-30, Ф-34, Ф-35, Ф-42 |
| средняя | 15 | Ф-23, Ф-24, Ф-26, Ф-27, Ф-29, Ф-31, Ф-32, Ф-33, Ф-36, Ф-37, Ф-38, Ф-39, Ф-41, Ф-44, Ф-45 |
| низкая | 4 | Ф-40, Ф-43, Ф-46, Ф-47 |
| **всего** | **26** | из них исправлено на месте 3 (Ф-22, Ф-23, Ф-24) |

Три находки говорят не только о восьми темах: Ф-35 (ключи шапки вне схемы) и
Ф-47 (неразрывные пробелы) касаются всех одиннадцати тем, Ф-41 задевает две
принятые (см. § 13).

---

## 8. Системные находки: это про свод, а не про темы

Двенадцать расхождений внутри PLAYBOOK.md. Они вынесены отдельно, потому что
исправлять их в темах бессмысленно: пока свод спорит сам с собой, любая тема
нарушает одну из двух его формулировок. В §§ 3 и 4 статусы «±» ссылаются сюда.
Формулировок для правки не предлагаю — это § 10.

**С-01. Длина чеклиста ревью задана двумя разными вилками.**
Часть 4, блок 10: «5–8 пунктов в залоге "Verify that…"». DoD 11.2 п. 8: «L2:
чеклист из 5–7 пунктов». Восьмой пункт в `rest-and-graphql` нарушает вторую и
удовлетворяет первую; новый линтер обвязки (`C-BODY-CHECKLIST`) реализует вторую
и потому ругается ещё и на две принятые темы (§ 13).

**С-02. Список блоков L2 в 3.1 расходится с тегами уровней в части 4.**
3.1: «Блоки 0, 1, 3, 5, 6, 9, 10, 12». Часть 4 при этом помечает блок 4
(«Эксплуатация») тегом `[L1 L2]`, а блоки 13 и 14 — тегом `[L1 L2 L3]`. То есть
по части 4 у L2 должно быть одиннадцать блоков, а по 3.1 — восемь, и три из
одиннадцати попадают в разные множества. Все восемь тем следуют части 4 в части
13 и 14 (иначе тема осталась бы без источников) и 3.1 в части блока 4 (иначе L2
пришлось бы писать разбор эксплуатации).

**С-03. 3.1 требует прозой то, чего не даёт списком.**
Та же строка L2 после перечня блоков говорит «задача с критерием» — это блок 11
«Лаба», помеченный в части 4 как `[L1]` и в перечне 3.1 отсутствующий. Строка L3
говорит «своё короткое объяснение механизма» и «один минимальный собственный
пример» — это блоки 3 и 5, помеченные `[L1 L2]`. Автор в обоих случаях выбрал
прозу против перечня: у семи L2-тем блок 11 есть, у L3-темы есть блок 3.
Считаю выбор верным, но он сделан вопреки букве.

**С-04. Часть 4 предписывает схему безусловно, часть 7.4 — по правилу отсечения.**
Часть 4, блок 3: «Модель: граница доверия, источник, sink. Схема.» — без
оговорок. 7.4: «**Правило отсечения:** если вопрос читателя не формулируется одной
фразой — схема не нужна, нужен текст. Если формулируется, но ответ помещается в
два предложения — тоже текст.» Первая формулировка делает отсутствие схемы
дефектом в семи темах из восьми, вторая — законным решением. Ср. Ф-33: я поставил
находку не за отсутствие схемы, а за то, что решение не зафиксировано.

**С-05. Состав L3 лишает тему двух блоков, которые часть 5 требует от каждого
раздела.** 5.2 п. 7: «Каждый раздел заканчивается блоком самопроверки. Ни одного
раздела без него.» 5.3 п. 14: «Блок "Ловушка" … Минимум один в каждом крупном
разделе.» Состав L3 не содержит ни блока 12, ни блока 9. Часть 13 повторяет
требование о «Проверь себя» тоже без оговорки об уровне. Отсюда минусы в строке 3
§ 3 у `security-headers` — минус, которого тема не могла избежать, не нарушив 3.1.
Оговорка в пользу свода: 5.3 говорит «в крупном разделе», и L3 крупным разделом
назвать трудно; про самопроверку такой оговорки нет.

**С-06. Верхняя граница числа источников наказывает добросовестность.**
11.2 п. 8: «L2: … 2–3 ссылки на первоисточник». `tls-and-proxy` даёт пять,
`app-architecture` и `rest-and-graphql` — по четыре, и обвязка (`validate.py`,
ГЛУБИНА) считает это отклонением. Тема про TLS, честно опирающаяся на RFC 8446,
RFC 9846, документацию Burp и MDN, лучше темы, ужатой до трёх ссылок. Нижняя
граница осмысленна, верхняя — нет; при этом верхняя единственная из двух
проверяется автоматически.

**С-07. Белый список плейсхолдеров запрещает цитировать первоисточник дословно.**
7.1 п. 6: «Никаких реальных секретов, доменов и IP: только `example.com`,
`192.0.2.x` (RFC 5737), `<PLACEHOLDER>` в угловых скобках». В темах встречаются
`store.company.com` (дословно из таблицы сравнения источников на странице MDN
про правило единого источника, строка 874 снятого текста), `attacker.test` и
`rabbit.invalid` — домены из зарезервированных RFC 2606 зон `.test` и `.invalid`,
то есть ровно того же назначения, что `example.com`. Буква свода делает их
нарушением; смысл — нет. Отсюда «±» в строке 22 § 3 у двух тем.

**С-08. «Один режим на страницу» против собственного скелета L2.**
9.2: «Смешивать режимы на одной странице запрещено: объяснение "почему" на
странице-рецепте — дефект». Все семь L2-тем объявлены `mode: концепт`, и все семь
обязаны по 3.1 нести блок 10 «Чеклист ревью» в залоге «Verify that…» и блок 11
«Задача» — то есть рецепт и практику внутри концепта. Первая приёмка нашла это же;
за два захода формулировка не изменилась, поэтому «±» стоит у семи тем снова.

**С-09. Схема frontmatter не знает трёх ключей, без которых не живёт ни одна
тема.** 9.3 перечисляет ключи шапки; `asvs`, `wstg` и `plan_id` в перечень не
входят. 9.4 объявляет несоответствие схеме блокирующей проверкой сборки. Ключи
стоят во всех одиннадцати темах этапа 0, включая принятые. См. Ф-35: буквальное
включение проверки красит весь этап.

**С-10. Правило возврата задано расстоянием, а работает смысл.**
5.2 п. 10: «Каждый раздел начиная с третьего содержит 1–2 вопроса из разделов,
пройденных 2 и 5 разделов назад». Расстояние — не отношение: `cors` (06) по
правилу обязан возвращать к `sessions-vs-tokens` (04) и `http-basics` (01), а
предпосылка, без которой CORS не читается, — `same-origin-policy` (05), то есть
N−1. Фактические возвраты (§ 7, Ф-44) выбраны по предпосылкам и осмысленнее
предписанных. Часть 13 при этом требует лишь «1–2 вопроса на возврат к прошлым
темам» — без расстояния, и это требование выполнено всюду. Свод в двух местах
задаёт разную строгость одного правила.

**С-11. Пункт «оглавление читается как конспект» невыполним, пока заголовки
блоков заданы каркасом.** Часть 13, строка 6 отсылает к 6.1. Заголовки блоков
предписаны частью 4 («Коротко», «Механика», «Как выглядит в коде», «Ловушка»,
«Чеклист ревью»), одинаковы во всех темах и потому конспектом темы быть не могут
в принципе. Первая приёмка записала это как Ф-20; автор в `WRITE-0B` (правка 14)
согласился и отнёс к своду. За восемь тем ничего не изменилось — минус стоит у
всех восьми, и он не про темы.

**С-12. Чеклист приёмки не проверяет шести требований свода.**
В части 13 нет строки: про длину предложения (6.1 «свыше 25 слов
переписывается» — 66 срабатываний, Ф-45); про врезку «Откуда это взялось» (6.6 —
нет ни в одной из восьми тем, Ф-36); про наличие схемы там, где эвристика 7.4 её
требует (есть только строка про описание уже существующей схемы, Ф-33); про
правило двух стеков (часть 4). Плюс нет строки про объём, хотя это единственное
числовое требование уровня (Ф-42), и про типографику 6.4 (Ф-47). Часть 13
слабее свода, и приёмка по ней одной пропустила бы шесть находок этого отчёта.

---

## 9. Что исправлено на месте

Досье разрешает правку на месте только фактических ошибок и требует записать
каждую. Их три. Всё остальное — списком в § 7, без касания текстов: приёмка не
переписывание.

Правки не закоммичены: рабочее дерево делится с параллельной миссией
`appsec-infra`, и коммит захватил бы её файлы. `git diff` показывает ровно четыре
изменённых файла — три темы и `HANDOFF.md`.

**Правка 1 — `content/stage-0/security-headers.md:82` (Ф-22).**
Источник: RFC 6797, §§ 7.2, 8.1, 8.4, 8.5.
**Было:** «Три требования уровня MUST определяют границы механизма: …» — и далее
в том же абзаце перечислены четыре.
**Стало:** «Четыре требования уровня MUST определяют границы механизма: …».
Переложены переносы строк в абзаце, ни одно слово кроме числительного не
изменено.
Проверка: `grep -n 'MUST NOT include the STS header field'` и
`grep -n 'MUST ignore' .smgr/appsec-review-8/src/rfc6797.txt` — четыре требования,
адресованные узлу и клиенту, в §§ 7.2, 8.1, 8.5, 8.4 соответственно.
Рядом, но не правкой: § 7.2 — источник первого из четырёх требований — не назван
в перечне разделов RFC 6797 в блоке 14 темы (там §§ 6.1, 8.1, 8.4, 8.5, 14.6).
Дописывать перечень источников я не стал: это правка сноски, а не факта. Условие
вердикта по теме — в § 12.

**Правка 2 — `content/stage-0/sessions-vs-tokens.md:279` (Ф-23).**
Источник: RFC 7519 § 3, дословно: «A JWT is represented as a sequence of URL-safe
parts separated by period ('.') characters. Each part contains a
base64url-encoded value. The number of parts in the JWT is dependent upon the
representation of the resulting JWS using the JWS Compact Serialization or JWE
using the JWE Compact Serialization.»
**Было:** ответ утверждал, что каждая часть декодируется в осмысленный JSON.
**Стало:** «несколько частей, разделённых точкой, каждая — своё значение в
base64url, а число частей зависит от того, подписан токен или зашифрован
(RFC 7519 § 3). Осмысленный JSON декодируется не из каждой части: заголовок и
утверждения — да, подпись — нет, а у Unsecured JWT она и вовсе пустая строка.»
Ссылка на § 6 (Unsecured JWT) в ответе не ставилась: сноски темы этого раздела не
называют, а править сноски — не моя работа.

**Правка 3 — `content/stage-0/csp.md:88` (Ф-24).**
Источник: CSP Level 3, § 6.7.2.7 «Does url match source list in origin with
redirect count?»: алгоритм возвращает `Matches`, если URL соответствует одному
или более выражению из списка источников.
**Было:** «Если ресурс **разрешает** хотя бы одно из перечисленных выражений,
ресурс разрешён».
**Стало:** «Если ресурс **соответствует** хотя бы одному из перечисленных
выражений, ресурс разрешён».
Одно слово. Ложное утверждение маркеров уверенности той же темы о недоступности
спецификации (Ф-30) я не правил: это не факт о предмете темы, а утверждение
автора о своей работе — переписывать его вправе только автор.

---

## 10. Что править в своде (список, без формулировок)

Порядок — по цене ошибки, не по номеру. Формулировки не предлагаю: свод —
документ автора.

1. **Схема frontmatter (9.3) против практики (С-09, Ф-35).** Внести `asvs`,
   `wstg`, `plan_id` в перечень ключей — иначе блокирующая проверка 9.4 красит
   все одиннадцать тем этапа 0. Это единственная правка свода, без которой
   сборка не может быть зелёной по определению.
2. **Норма объёма L2 (3.1) против семи написанных тем (Ф-42).** Решение
   оператора: либо вилка 800–1200 остаётся и семь тем сокращаются на 5–15 %, либо
   вилка меняется под фактический объём. Третьего варианта — «оставить как есть»
   — нет: пока расходятся норма и текст, каждая приёмка будет ставить ± в первой
   строке чеклиста.
3. **Длина чеклиста ревью (С-01).** Согласовать «5–8» части 4 и «5–7» DoD п. 8.
   Линтер уже реализует второе.
4. **Состав уровней (С-02, С-03).** Свести список блоков 3.1 и теги части 4 в
   одно место. Сегодня уровень темы приходится собирать из двух списков, которые
   расходятся на три блока у L2 и на два у L3.
5. **Состав L3 против части 5 (С-05).** Либо L3 получает блок «Проверь себя»,
   либо 5.2 п. 7 и строка 3 части 13 получают оговорку об уровне. Сейчас
   `security-headers` не может выполнить чеклист, не нарушив 3.1.
6. **Один режим на страницу (С-08).** 9.2 против блоков 10 и 11 в скелете L2.
   Найдено дважды, двумя приёмками; за это время не изменилось.
7. **Верхняя граница числа источников (С-06).** «2–3 ссылки» наказывает тему,
   опирающуюся на пять первоисточников, и именно эта граница проверяется
   автоматически.
8. **Схема: обязанность или решение (С-04).** Часть 4 предписывает схему в блоке
   3 безусловно, 7.4 даёт правило отсечения. Выбрать одно.
9. **Правило возврата (С-10).** Расстояние N−2/N−5 против предпосылок. Если
   остаётся расстояние — семь тем из семи нарушают; если предпосылки — правило
   надо переписать через `prerequisites`.
10. **Белый список плейсхолдеров (С-07).** Добавить зоны `.test` и `.invalid`
    (RFC 2606) и оговорку про дословные цитаты из первоисточника.
11. **Чеклист приёмки (С-11, С-12).** Снять невыполнимую строку про оглавление;
    добавить строки, которых нет: объём, длина предложения, врезка «Откуда это
    взялось», наличие схемы по эвристике 7.4, типографика 6.4, правило двух
    стеков. Сегодня приёмка по одной части 13 пропустила бы шесть находок из
    двадцати шести.
12. **Печать внешних идентификаторов (9.6 п. 25) и маппинг-индекс (9.6 п. 24).**
    Требования есть, исполнения нет ни в одной теме (Ф-32). Либо требование
    исполняется, либо оно снимается — сейчас оно висит.

---

## 11. Что просить у обвязки (списком, без правок)

`tools/` и конфиги проверок я не касался: их прямо сейчас делает параллельная
миссия `appsec-infra`. Ниже — только заявки, каждая с находкой, которую она бы
закрыла.

1. **Линк-чекера нет вообще** — по состоянию на момент замеров, см. оговорку о
   сроке в § 14. Часть 13, строка 30 требует «Vale, markdownlint, линк-чекер —
   чисто»; в `check.py` пять проверок, линк-чекера среди них нет, и
   я проверял 25 адресов руками. Нужна оговорка про среду: `www.w3.org` отдаёт 403
   при повторных обращениях, `*.gov.ru` и `ptsecurity.com` не проходят по TLS
   отсюда — такие адреса должны помечаться «недостижим из среды», а не «битый».
   Закрывает: § 6.4 руками, Ф-30 (ложный вывод автора о недоступности источника).
2. **`stoplist.py` считает совпадения внутри огороженных блоков.** Три из шести
   срабатываний 6.5 — это `!==` в JavaScript и `[Invoice!]!` в схеме GraphQL, то
   есть синтаксис, а не восклицание. Проверка прозы обязана снимать код: сам
   скрипт это уже умеет для четырёхграмм (`prose_ngrams`), но не для запрещённых
   конструкций.
3. **Сверка `asvs` в шапке с идентификаторами в теле.** Закрывает Ф-31: три темы
   из восьми печатают требования, которых нет в шапке, а `security-headers`
   объявляет сверенным требование, которого нет в тексте.
4. **Проверка печати внешних идентификаторов с версией.** Закрывает Ф-32:
   шестнадцать упоминаний CWE без версии при требовании 9.6 п. 25. Дешевле всего
   как правило Vale по шаблону `CWE-\d+`, не сопровождаемому номером версии.
5. **Проверка неразрывных пробелов уже есть (`L-NBSP`), не хватает правки.**
   130 срабатываний по каталогу, 0 неразрывных пробелов в проекте (Ф-47). Правку
   удобно делать скриптом один раз — после того, как параллельная миссия отпустит
   файлы.
6. **Что уже закрыто параллельной миссией и подтверждено моими замерами:**
   `C-FM-TITLE-LEN` называет те же пять заголовков и те же длины — 95, 89, 80,
   78, 63 (Ф-34); `C-BODY-CHECKLIST` находит восьмипунктный чеклист в
   `rest-and-graphql` (Ф-41) — и дополнительно два в принятых темах (§ 13).

---

## 12. Вердикты

Шкала — та же, что в первой приёмке: **годна** (публикуется как есть),
**годна при условиях** (условия перечислены, каждое снимается правкой без
переписывания), **не годна** (требует переписывания разбора).

**Ни одной «не годна».** Разборы верны по существу: из примерно восьмидесяти
проверяемых утверждений неверными оказались три (§ 5), и все три — локальные
ошибки формулировки, а не понимания. Работа с первоисточниками, за которую первая
приёмка хвалила образец, в восьми темах удержана: даты, версии, статусы
документов, тринадцать идентификаторов CWE, состав RFC 9846 — всё сверено и всё
верно.

**Ни одной «годна» без условий.** Два условия общие для всех восьми: врезки
«Откуда это взялось» нет ни в одной (Ф-36) и объём вне нормы (Ф-42, решение
оператора). Ниже — то, что добавляется к этим двум по каждой теме.

| Тема | Вердикт | Условия сверх общих |
|---|---|---|
| 04 `sessions-vs-tokens` | годна при условиях | шапка и тело расходятся по ASVS (Ф-31) |
| 05 `same-origin-policy` | годна при условиях | шапка и тело расходятся по ASVS (Ф-31); один термин без раскрытия (Ф-46) |
| 06 `cors` | годна при условиях | модальность MDN против Fetch (Ф-29); предполётный обмен без транскрипта (Ф-37); заголовок 80 знаков (Ф-34); решение о схеме не зафиксировано (Ф-33) |
| 07 `csp` | годна при условиях | **маркеры уверенности объявляют недоступным доступное (Ф-30)**; термин без раскрытия (Ф-46) |
| 08 `security-headers` | годна при условиях | транскрипт нарушает четыре требования 7.3 (Ф-38); ASVS 3.4.4 объявлен сверенным, но в тексте отсутствует (Ф-31); заголовок 89 знаков (Ф-34); § 7.2 RFC 6797 не назван в сносках (§ 9) |
| 09 `tls-and-proxy` | годна при условиях | **три хеджа источника сняты в одном предложении (Ф-28)**; заголовок 78 знаков (Ф-34); решение о схеме (Ф-33) |
| 10 `app-architecture` | годна при условиях | пункт чеклиста без опоры в теле (Ф-39); ответ опирается на источник вне блока 14 (Ф-27); «современного» в заголовке (Ф-40); заголовок 95 знаков (Ф-34); решение о схеме (Ф-33); 14 длинных предложений, включая одно на 61 слово (Ф-45) |
| 11 `rest-and-graphql` | годна при условиях | **«спецификация обязывает» без спецификации в источниках (Ф-25)**; сравнение с OpenAPI 3.1 без ссылки на 3.1 (Ф-26); восемь пунктов чеклиста и четыре источника (Ф-41); заголовок 63 знака (Ф-34); негладкий вопрос самопроверки (Ф-43); термин без раскрытия (Ф-46) |

Полужирным выделены условия высокой серьёзности, снимаемые правкой разбора: их
три, по одному в `csp`, `tls-and-proxy` и `rest-and-graphql`. Остальные четыре
высокие находки правятся не разбором: Ф-22 уже снята на месте (§ 9), Ф-34 —
переписыванием пяти заголовков, Ф-35 — правкой схемы 9.3, Ф-42 — решением
оператора о норме объёма.

**Общий вердикт: восемь тем принимаются при условиях; блокирующих для сборки
условий два, и оба не в текстах.** Первое — пять заголовков длиннее 60 знаков
(Ф-34): пока они есть, `check.py` красный. Второе — три ключа шапки вне схемы 9.3
(Ф-35): правится сводом, не темами.

**Что стало лучше по сравнению с первой приёмкой.** Три её системных условия
автор снял, и это видно в числах, а не на слово:

- **Объём.** Было 26–43 % превышения по щадящей метрике, стало 5,1–14,5 %
  (среднее 11,2 %). Норма всё ещё не выполнена, но расхождение уменьшилось
  втрое.
- **Шаблон.** Было десять авторских связок, повторяющихся ровно по одному разу в
  каждой теме; стало **шесть** срабатываний накопительного стоп-листа на восемь
  тем (пять в `sessions-vs-tokens`, одно в `same-origin-policy`, ноль в
  остальных шести) и **ноль** общих четырёхграмм прозы во всех восьми — и ноль
  во всех одиннадцати. Восемь тем не читаются как копии одного абзаца.
- **Ложные плюсы в DoD.** Самооценка автора (`WRITE-0B` § 4.1) в целом честна:
  из 200 клеток по существу расходятся 12, и семь из двенадцати — это пункт 10
  «каждое утверждение сведено к лично открытому источнику», по которому автор
  поставил плюс всем восьми. Расхождение не в самооценке как жанре, а в одном
  пункте, и находки Ф-25…Ф-30 показывают, где именно.

**Замечание об уровнях, которое я не имею права превращать в правку.**
`same-origin-policy` (L2) и `security-headers` (L3) — темы, чей уровень досье
оставляет оператору: он назначается по 3.2 с участием личного опыта, которого у
меня нет. Наблюдения на его усмотрение, все замерены.
`same-origin-policy`: после `http-basics` (10 упоминаний) это самая несущая тема
этапа — она стоит предпосылкой у `cors` и у `csp`, и обе возвращаются к ней
вопросами блока 12; её проза — 1365 слов, второй результат из семи L2. Критерий
3.2 п. 4 («несущая способность») читается в пользу уровня выше, критерий 3.2 п. 3
(«личный опыт») — только оператором.
`security-headers`: при 521 слове прозы вышел за норму L3 меньше всех (+4,2 %), а
по метрике без служебного аппарата (462) в норму укладывается — по объёму L3 ему
подходит. Два наблюдения против: состав L3 оставил тему без «Проверь себя» и без
«Ловушки» (С-05), и это единственная причина её минусов в § 3; и она сама стоит
предпосылкой у L2-темы `tls-and-proxy`, то есть единственная L3 этапа несёт на
себе тему уровнем выше.

---

## 13. Что остаётся оператору

**Два решения, которые я не имею права принять.**

1. **Норма объёма (Ф-42).** Сокращать семь текстов на 5–15 % или менять вилку 3.1
   под фактический объём. Досье запрещает мне подгонять норму; замер сделан,
   решение открыто. Замечу одно: сокращать придётся не воду, а разборы, —
   служебный аппарат уже вычтен из подсчёта, а средняя тема превышает норму на
   135 слов.
2. **Уровни `same-origin-policy` и `security-headers`.** Наблюдения в § 12, ни
   одной правки в шапках я не сделал.

**Двенадцать правок свода** — § 10. Первая (схема frontmatter) блокирует зелёную
сборку и не может быть закрыта в темах.

**Три дефекта в уже принятых темах.** Найдены попутно, в границы этого захода не
входят, но молчать о них нельзя:

- **Восьмипунктные чеклисты в `cookies.md` и `http-basics.md`.** Новый линтер
  `C-BODY-CHECKLIST` ругается на них так же, как на `rest-and-graphql`. Первая
  приёмка их пропустила: линтера тогда не было, а часть 4 разрешает «5–8»
  (С-01). Воспроизведение: `.venv-tools/bin/python tools/check.py --full`, фильтр по
  `C-BODY-CHECKLIST`.
- **Четыре нарушения 7.6 в двух принятых темах.** Врезка на 6 строк при пределе 5
  (п. 45) — `url-and-encoding.md:101` и `cookies.md:130`; раздел начинается с
  врезки (п. 42) — `url-and-encoding.md:110` и `cookies.md:130`. В восьми
  принимаемых темах нарушений 7.6 нет ни одного: врезок по две на тему при
  пределе пять, ни одной длиннее пяти строк, ни одной пары подряд, ни одного
  раздела, начатого врезкой. Воспроизведение: счёт блоков `^>` и их длины по
  одиннадцати файлам.
- **Ф-35 и Ф-47 касаются всех одиннадцати тем**, а не восьми: ключи шапки вне
  схемы 9.3 стоят и в принятых, неразрывных пробелов нет нигде.

**Четыре записи, которых не хватает в `sources.yaml`.** Реестр — источник истины,
правится руками автора, и добавлять записи мне разрешено. Я этого не сделал
сознательно, и вот почему: каждая из четырёх записей полезна только вместе с
правкой ссылки в тексте темы, а правка текста здесь — работа автора, не приёмки.
Запись без ссылающейся темы дала бы тринадцатое предупреждение `validate.py` в
файле, который прямо сейчас делит со мной параллельная миссия. Поэтому — списком:

1. **GraphQL specification, October 2021 Edition** — для `rest-and-graphql`
   (Ф-25). Утверждение о `__typename` опирается на §§ 4.1 и 4.2 этого документа;
   в реестре его нет вовсе.
2. **OpenAPI Specification 3.1.1** — для `rest-and-graphql` (Ф-26). Сравнение
   «появилось в 3.2.0» требует второго документа.
3. **MDN «CORS errors»** — для `app-architecture` (Ф-27). Запись
   `mdn-cors-errors` в реестре уже есть, нужна только ссылка в блоке 14 темы.
4. **RFC 6797 § 7.2** — для `security-headers` (§ 9). Запись есть, перечень
   разделов в блоке 14 темы неполон.

**Что этот заход проверить не мог** — § 2: исполняемость примеров (исполняемых
нет), чтение вслух и через сутки (темы написаны и приняты в один день), вёрстка и
навигация (движка нет), виджет обратной связи. Ни один из этих пунктов не закрыт
плюсом.

---

## 14. Гигиена захода

- **Первоисточники открывались лично.** 56 файлов, 14 МБ в
  `.smgr/appsec-review-8/src/`: 27 текстов со снятой разметкой, 23 сырые страницы,
  5 глав ASVS и `asvs-v5.json`. Каждое число в отчёте — из команды, которую я
  запустил; там, где источник не открылся, стоит «не подтверждено, источник
  недоступен», а не «нет».
- **Ссылки.** 25 живых адресов из восьми тем проверены запросами: 24 отдали 200,
  `www.w3.org/TR/CSP3/` — 403 при повторном обращении, и адрес при этом верен
  (документ снят целиком, 1 010 768 байт). 8 адресов-плейсхолдеров не проверялись
  по 7.1 п. 6. Известные ловушки среды (`*.gov.ru`, `ptsecurity.com`, `cisa.gov`,
  `graphql.org`) в этих восьми темах не встречаются; `graphql.org` из среды
  отдаёт 200.
- **Обвязка прогнана после моих правок**, а не до.
  `.venv-tools/bin/python tools/check.py --full` → 13 ошибок, 269 предупреждений,
  10 проверок, не пройдено (в восьми темах — 5 ошибок и 156 предупреждений);
  `.venv-tools/bin/python tools/validate.py` → зелёный, 0 ошибок, 12
  предупреждений; `.venv-tools/bin/python tools/gen_topics.py --check` → чисто.
- **Срок замеров.** Все числа сняты 22 августа и в ночь на 23 августа 2026,
  последний прогон обвязки — до 00:28 23 августа. В 00:28 параллельная миссия
  `appsec-infra` добавила `tools/linkcheck.py` (23 800 байт, четыре правила
  ссылок и флаг `--external`), которого на момент замеров не существовало.
  Поэтому утверждение «линк-чекера нет» в § 3 (строка 30), § 6.4 и § 11 заявке 1
  верно на момент замера и уже неверно на момент чтения: заявка 1 закрыта
  обвязкой, и проверять её результат — работа `appsec-infra`, не приёмки.
  Оговорка про среду (`www.w3.org` отдаёт 403 при повторных обращениях, адрес при
  этом верен) остаётся в силе и относится теперь к новому инструменту. Остальные
  заявки § 11 и все статусы по темам замером не затронуты: `tools/` я не запускал
  заново, чтобы не мерить инструмент в середине его написания.
- **Границы соблюдены.** `tools/`, конфиги проверок, `INFRA.md`, `SCOPE.md`,
  `STYLE.md`, глоссарий и всё, что связано со сборкой сайта, не тронуты —
  заявки к ним списком в § 11. `sources.yaml` не изменён ни в одной записи.
  `topics.yaml` не редактировался руками. Учебный план
  (`/home/tarchok/Obsidian Vault/AppSec-engineer/plan.md.md`) открывался только на
  чтение. Правки на месте — три, все фактические, все в § 9. Суррогатных
  источников не использовано: ни одного блога или пересказа вместо стандарта.
- **Наружу ничего не публиковалось, ничего не коммитилось, денег не потрачено.**

---

**Итог.** Восемь непринятых тем этапа 0 прогнаны по 33 пунктам части 13 и 25
пунктам DoD 11.2 — 464 статуса, поставленных по замеру, а не по памяти. Факты
сверены с первоисточниками: из примерно восьмидесяти проверяемых утверждений
неверны три, все три исправлены на месте. Найдено 26 находок (7 высоких, 15
средних, 4 низких) и 12 системных расхождений внутри свода. Вердикт по каждой из
восьми тем — годна при условиях, условия перечислены в § 12; «не годна» нет ни
одной. Два блокирующих условия лежат вне текстов: пять длинных заголовков и три
ключа шапки вне схемы 9.3.

ПРИЁМКА ВОСЬМИ ТЕМ ЗАКРЫТА
