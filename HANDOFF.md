# Передача работы

## Миссия `appsec-stage1-injection` — ЗАВЕРШЕНА

Досье — `~/.supermanager/missions/appsec-stage1-injection/dossier.md`. Отчёт —
`STAGE1-INJECTION.md`; последней строкой стоит фраза-маркер из досье. Статус —
`.smgr/appsec-stage1-injection/status.json`, вывод прогонов —
`.smgr/appsec-stage1-injection/final/`. Вопросов оператору не задавалось: всё
решалось в рамках досье.

**Что сдано.** Подраздел 1.3 «Injection» написан целиком: одиннадцать тем в
`content/stage-1/`. Три L1 (`sqli-basics` 2520, `parameterized-queries` 2502,
`os-command-injection` 2508), шесть L2 (`sqli-blind`, `filter-and-waf-bypass`,
`nosql-injection`, `ssti`, `xxe`, `crlf-injection` — 840–1027), две L3
(`sqli-oob` 313, `ldap-xpath-injection` 309). Три лабы (`lab-sqli-basics`,
`lab-parameterized-queries`, `lab-os-command-injection`) и два правила SAST
(`sql-query-string-built`, `os-command-shell-built`, оба taint) прогнаны и
сошлись. Уровни, слаги, предпосылки и порядок — из замороженного оглавления без
правок.

**Состояние проверок на 2026-08-24**

```text
make check          0 ошибок, 397 предупреждений, 5 снято — пройдено
make lint-selftest  166 из 166 — пройдено
make links          0 ошибок, 0 предупреждений, 3 снято — пройдено
make site           45 страниц, 3155 внутренних ссылок — пройдено
make phone          пройдено
semgrep check.py    семь правил из семи — ПРАВИЛО СВЕРЕНО
```

Прирост предупреждений (145 → 397) — почти целиком `L-CLERICAL2` на законном
существительном «данные»: подраздел про инъекции. Новых видов предупреждений на
одиннадцати темах нет; `S-SENT-LONG`, `L-NBSP`, `S-CODE-*` — ноль.

**Что изменилось в обвязке — знать обязательно**

1. **Реестр источников: +2** — `owasp-top10-2025-a05`, `owasp-top10-2025-a02`
   (последний под сноску XXE, находка Н-I1).
2. **`taxonomy.yaml`:** мёртвая категория `INJ` (Н-I3) и теги `sql`, `nosql`,
   `oscmd`, `template`, `ldap`, `xpath`, `xml`, `injection`.
3. **`labs.yaml`:** три записи лаб этой миссии.
4. **`pilot/semgrep/check.py`:** наборы `sql-query`, `os-command`.
5. **ASVS сверены с документом v5.0.0 §V1.2:** 1.2.4 SQL/NoSQL, 1.2.5 OS,
   1.2.6 LDAP, 1.2.7 XPath, 1.2.1 (заголовки/CRLF).

**Ключевые решения и находки**

* **XXE (CWE-611) в издании 2025 — A02 Security Misconfiguration, не Injection**
  (Н-I1). Тема стоит в подразделе 1.3 по учебному плану, но поле `owasp` у неё
  `A02:2025` по факту. Аналог Н-S1 (SSRF/A03). Оглавление не тронуто.
* **`parameterized-queries` — L1 без своего правила SAST осознанно** (раздел 5
  отчёта). На границе применимости параметра форма безопасной сборки
  заполнителей и опасной склейки данных совпадает; `sql-query-string-built`
  срабатывает на обоих. Лаба намеренно НЕ в `SUITES` `check.py`; блок 8
  разбирает это как слепое пятно SAST.
* **Сироты подраздела закрыты** перекрёстными ссылками; осталась
  `password-storage` — долг подраздела A01, не этой миссии.

**Грабли (в дополнение к граблям миссии SSRF ниже)**

* `wordcount.py` не считает абзацы «Зачем это в работе AppSec-инженера»,
  «Откуда это взялось», «Маркеры уверенности», листинги и ответы `<details>`.
  Добор объёма делать в обычных блоках.
* Неразрывный пробел «NN мин» в шапке — отдельным проходом
  (`scratchpad/nbsp.py`, regex `время \d+ мин`), при heredoc теряется.
* В `tests.py` лаб — только функциональность, зелёная и на `code.py`. Свойство,
  верное лишь после починки (апостроф в SQL, `;` как текст в OS-cmd), ловит
  `hack.py`, не `tests.py`.
* Условная ошибка `1/0` в слепом SQLi на SQLite не воспроизводится (даёт NULL) —
  привязана к SQL Server, помечать по документации.

**Что писать следующей миссии.** Подраздел 1.4 «XSS и клиентские уязвимости»,
восемь тем (`STAGE1-TOC.md` строки 111+). Тема `xxe` теперь существует —
ссылки вперёд на неё из тем SSRF можно перевести с номера плана на слаг (долг 3
отчёта).

---

## Миссия `appsec-stage1-ssrf` — ЗАВЕРШЕНА

Досье — `~/.supermanager/missions/appsec-stage1-ssrf/dossier.md`. Отчёт —
`STAGE1-SSRF.md`; последней строкой стоит фраза-маркер из досье. Маркер в этом
файле не повторяется намеренно. Статус — `.smgr/appsec-stage1-ssrf/status.json`,
вывод прогонов — `.smgr/appsec-stage1-ssrf/final/`. Вопросов оператору не
задавалось: всё решалось в рамках досье.

**Что сдано.** Подраздел 1.2 «SSRF» написан целиком: пять тем, одна L1
(`ssrf-basics`, 2629 слов) и четыре L2 (1161–1419). Лаба
`pilot/lab/ssrf-basics` и правило SAST `outbound-url-unvalidated` прогнаны и
сошлись. Уровни, слаги, предпосылки и порядок взяты из замороженного оглавления
без правок.

**Состояние проверок на 2026-08-24**

```text
make check          0 ошибок, 145 предупреждений, 5 снято — пройдено
make lint-selftest  166 из 166 — пройдено
make links          0 ошибок, 0 предупреждений, 3 снято — пройдено
make site           34 страницы, 1994 внутренние ссылки — пройдено
make phone          11 тем, 574 КиБ — пройдено
semgrep check.py    пять правил из пяти — ПРАВИЛО СВЕРЕНО
```

Предупреждений стало 145 против 120: все 25 новых — `L-CLERICAL2` на законном
существительном «данные». Новых видов предупреждений нет.

**Что изменилось в обвязке и словарях — знать обязательно**

1. **Реестр источников: 318 записей**, добавлены три — `gcp-metadata-server`
   (адрес редиректит на `docs.cloud.google.com`, в реестре стоит канонический
   `cloud.google.com`), `azure-imds`, `iana-ipv4-special-registry`.
2. **`taxonomy.yaml`:** категория кода `SSRF`, теги `ssrf` и `cloud`. Категория
   мертва — см. находку Н-S2 отчёта: `code_categories` после чистки шапок никем
   не читается.
3. **`labs.yaml`:** запись `lab-ssrf-basics`.
4. **`pilot/semgrep/check.py`:** пятый набор `outbound-url`.
5. **Ссылка вперёд закрыта:** блок «Дальше» темы `url-and-encoding` переведён с
   «Тема 1.2.03» на слаг `ssrf-filter-bypass`.

**Грабли этой миссии**

* **Тест-кейсы правила в режиме `taint` — не заглушки.** Semgrep распространяет
  пометку через тело функции в том же файле, поэтому «санитайзер», возвращающий
  свой аргумент, санитайзером не работает, даже будучи объявленным в
  `pattern-sanitizers`. Заглушка обязана возвращать пересобранное значение.
* **Санитайзер `int(...)` нужен почти всякому правилу про адреса.** Без него
  ловится адрес, собранный приложением из постоянной части и номера, — законный
  код и типовое ложное срабатывание.
* **Неразрывный пробел в шапке темы вставляется не heredoc'ом.** Строка
  «время NN мин» требует `\xa0`; при письме через `cat <<'EOF'` он теряется, и
  `L-NBSP` находит это последним. Вставляйте его отдельным проходом по файлу.
* **`L-NBSP` ловит цифру в имени продукта:** «IMDSv2 с отключением» читается
  правилом как «2 с». Обходится переформулировкой, чинится просмотром назад в
  выражении — заявка Н-S4.
* **Заголовок подраздела 1.2 в `STAGE1-TOC.md` называл `A03:2025`.** Это
  ошибка: SSRF входит в `A01:2025`. Темы написаны с `A01:2025`. Заявка Н-S1
  закрыта в рабочем дереве до сдачи правкой не от автора тем.
* **Слово «свод» в теле темы — след процесса**, которого чистка шапок не
  коснулась. Оставалось в `privilege-escalation-vertical:110` и `idor:379`;
  заявка Н-S3 закрыта до сдачи, тоже не автором тем. В `content/**` слова
  больше нет ни разу — если оно вернётся, это регресс, и правила на него нет.
* **Облачное не проверяется лично** и не может: аккаунта нет, заводить его
  границы миссии запрещают. Всё, что взято из документации трёх провайдеров,
  помечено в «Маркерах уверенности» как таковое.

**Что писать следующей миссии.** Подраздел 1.3 «Injection», одиннадцать тем, по
тому же оглавлению. Категория кода — `INJ`, теги из заявки З-02 оглавления
заводятся вместе с темами, которые их требуют. Тема `xxe` (1.3.10) ссылается на
`ssrf-basics` как на предпосылку — теперь она существует.

---

## Миссия `appsec-clean-headers` — ЗАВЕРШЕНА

Досье — `~/.supermanager/missions/appsec-clean-headers/dossier.md`. Отчёт —
`CLEAN-HEADERS.md`; последней строкой стоит фраза-маркер из досье. Статус —
`.smgr/appsec-clean-headers/status.json`. Вопросов оператору не задавалось:
всё решалось в рамках досье.

**Что сдано.** Со страниц гайдбука убраны следы процесса разработки.
Все четыре пункта досье закрыты, сплошной проход по собранному сайту дал ещё
двенадцать находок, и они убраны тоже. Правки внесены в шаблон, генератор,
валидатор, самопроверку линтеров и свод, а не только в готовые файлы.

Коротко, что изменилось на странице темы: шапка была шестистрочным блоком
данных (`**AG-AC-11**`, `reviewed:`, «проверено на:», «пререквизиты:»,
«маппинг:») плюс абзац «Состав блоков — …» со ссылкой на `PLAYBOOK.md`. Стала
одной строкой «Уровень **L2** · время 30 мин (теория 17 / задача 8 /
самопроверка 5, оценка)» и отдельным абзацем «Что прочитать сначала: …»,
которого нет вовсе, когда предпосылок нет. На главной снят столбец «Написано»,
с каждой страницы — подвал «Исходник темы: `content/…`».

**Свод правился под решение оператора** (шесть мест): часть 4 — блок «ШАПКА» в
скелете, правило «удаление с явной пометкой», правило схемы, **новый раздел
4.1 «Что со страницы темы убрано»**; 3.1 — признаки исключений из подсчёта
объёма; 9.6 пп. 19 и 25. В `SCHEMA.md` переписаны § 3.2, § 4.2 («Декларация
состава снята»), § 5 и таблица правил.

**Обвязка.** Сняты шесть правил: `C-HEAD-CODE`, `C-HEAD-REVIEWED`,
`C-HEAD-VERSIONS`, `C-HEAD-MAP`, `C-DECL`, `C-DECL-SET`. Добавлено
`C-HEAD-CLEAN` — ошибка, ловит возврат любого из шести следов во всю вводную
часть темы (от `h1` до первого `## `). Ослаблена одна сторона `C-BODY-IDENT`
(«объявлено, но не напечатано»), причина в отчёте § 4.2. Метрика объёма
(`tools/wordcount.py`) переведена на новые признаки шапки.

**Состояние проверок на 2026-08-24**

```text
make check          0 ошибок, 120 предупреждений, 5 снято — пройдено
make lint-selftest  166 из 166 — пройдено
make site           29 страниц, 9 схем, 1543 внутренние ссылки — пройдено
make phone          11 тем, 574 КиБ — пройдено
```

Предупреждений ровно столько же, сколько было до миссии (120): ни одно не
появилось и ни одно не спрятано. Число утверждений самопроверки упало со 168 до
166 — сняты фикстуры восьми снятых проверок, добавлены шесть новых.

**Что осталось намеренно** — четыре пункта, разобраны в отчёте § 5. Главный:
служебный слой блока 14 («Каркас этапа», «Число источников», «Скоропортящийся
слой», «Маркеры уверенности» с датами личного открытия страниц). По признаку
досье это следы процесса, но чистка требует переписывать текст темы, что
границы миссии запрещают. Из этого слоя убраны только ссылки на `PLAYBOOK.md`,
«свод» и слово «миссия». **Если оператор захочет продолжить — это отдельная
миссия на 23 темы плюс правила `C-BODY-TRUST` и `C-BODY-SOURCES`.**
Остальные три: страница «Обслуживание» целиком (свод 9.6 п. 23 заводит её как
служебную; исполнено требование держать её вне поискового индекса), плашка
статуса темы и столбец «Статус» на карте, HTML-комментарий «Собрано
tools/build_site.py».

---

## Миссия `appsec-stage1-a01` — ЗАВЕРШЕНА

Досье — `~/.supermanager/missions/appsec-stage1-a01/dossier.md`. Отчёт —
`STAGE1-A01.md`; последней строкой стоит фраза-маркер из досье, поставленная
2026-08-24 после прогонов. Маркер в этом файле не повторяется намеренно: досье
требует его ровно в одном месте. Ответ оператора по норме L2 —
`.smgr/appsec-stage1-a01/answer-1.md`, вывод прогонов —
`.smgr/appsec-stage1-a01/final/`.

Коммиты миссии: `d4cf467` (три правки свода), `18f5e06` (словари, реестр,
`order`), `9ca24fb` и `f63243a` и `c6c6426` (девять тем L2), `169fd9f` и
`b2d0c45` (две темы L1 с лабами и правилами SAST), `5b3fd5c` (отчёт и заявка),
плюс коммит с решением по норме L2 и маркером сдачи.

**Что сдано.** Подраздел 1.1 «Broken Access Control» написан целиком:
одиннадцать тем, все в норме объёма, ноль ошибок и ноль предупреждений
стилевых правил. Две лабы (`pilot/lab/privilege-escalation-vertical`,
`pilot/lab/idor`) и два правила SAST (`admin-route-no-authz`,
`object-lookup-unscoped`) прогнаны и сошлись. Три правки свода из досье
внесены. Вилка нормы объёма L2 закрыта решением оператора.

**Состояние проверок на 2026-08-24 — всё зелёное впервые**

```text
make check          0 ошибок, 120 предупреждений, 5 снято — пройдено
make lint-selftest  168 из 168 — пройдено
make links          0 ошибок, 0 предупреждений, 3 снято — пройдено
make site           29 страниц, 1546 внутренних ссылок — пройдено
make phone          11 тем, схем 6 — пройдено
semgrep check.py    четыре правила из четырёх — ПРАВИЛО СВЕРЕНО
```

`C-VOL-OVER` даёт ноль впервые за историю проекта. Путь был двухшаговый:
правка метрики объёма (решение 2 досье) вывела `security-headers`,
`sessions-vs-tokens` и `tls-and-proxy` в прежнюю норму без правки текста —
десять ошибок стало семью, — а решение оператора от 2026-08-23 подняло верхнюю
границу L2 до 1700 и закрыло остальные семь.

**Что изменилось в обвязке и своде — знать обязательно**

0. **Норма объёма L2 — 800–1700 слов**, а не 800–1200 (решение оператора от
   2026-08-23). Число лежит в четырёх местах, и все четыре сведены: таблица 3.1
   свода, пункт 8 DoD в 11.2, словарь `depths` в `taxonomy.yaml` и словарь
   `NORM` в `tools/wordcount.py`. Нижняя граница не менялась: девять тем этой
   миссии уложились в 871–995 слов, и 1700 — потолок, а не цель.
1. **Состав L3 изменился.** Блоки 5 «Как выглядит в коде» и 12 «Проверь себя»
   убраны из L3 в таблице 3.1 и в тегах части 4. L3 — это блоки 0, 3, 13, 14.
2. **Метрика объёма изменилась.** Абзацы «Зачем это в работе AppSec-инженера»
   и «Откуда это взялось» из подсчёта выпадают. Следствие для автора: фраза
   «Откуда это взялось», вписанная в середину длинного абзаца, унесёт из
   подсчёта **весь абзац**. Ставьте её отдельным абзацем.
3. **Правило `L-YO` сузилось на слове «счёт».** Формы множественного числа
   (счета, счетов, счетам) больше не ловятся: в них «ё» нет. Форма «счета»
   выпала намеренно — она омограф.
4. **`code_categories` пополнился категорией `AC`**, словарь тегов — тремя
   тегами: `access-control`, `jwt`, `logic`. Остальные категории и теги заявки
   З-02 заводятся вместе с темами, которые их требуют, а не пачкой.
5. **`order` у `password-storage` теперь 480**, не 490. Фикстуры
   `lint_selftest.py` переведены следом — если будете двигать `order` ещё раз,
   проверьте селф-тест: он ловит это режимом «мутация не применилась», а не
   падением.

**Грабли этой миссии**

* Реестр источников вырос до 315 записей, и добавить дубль стало легко. Перед
  новой записью — `grep` по `id` и по `url`. Валидатор ловит, но позже.
* Ссылка в прозе на ненаписанную тему пишется номером плана, а по слагу —
  ошибка `C-REF-TOPIC`. Это касается и поля `prerequisites`: предпосылку из
  замороженного оглавления, которой ещё нет, завести нельзя (случай
  `role-parameter-tampering` → `jwt-attacks`).
* Правило `C-BODY-IDENT` двустороннее. Требование ASVS, названное в тексте и
  не объявленное во frontmatter, — ошибка; объявленное и нигде не напечатанное
  — предупреждение. Проверять обе стороны до коммита.
* Сноска на главу документа расходится с записью реестра, если запись ведёт на
  оглавление. Заводите отдельную запись на главу — так уже сделано у API
  Security Top 10.
* Прежняя норма L2 800–1200 была достижима: девять тем этой миссии уложились в
  871–995 слов и не потеряли ни разбора, ни чеклиста, ни задачи. Верхнюю
  границу подняли не потому, что 1200 мало, а потому, что десять тем этапа 0
  написаны до появления метрики. Писать под 1700 не нужно.

**Долги, вышедшие из этой миссии** — раздел 9 файла `STAGE1-A01.md`. Коротко:
таблица трудоёмкости 3.4 не пересчитана под норму 1700; режим «рецепт» против
`C-BODY-ORIGIN` (выстрелит на этапе 2); предпосылка `jwt-attacks`; двенадцать
ссылок вперёд из пятнадцати; сирота `password-storage`; заявки З-03 и З-04
оглавления.

**Что писать следующей миссии.** Подраздел 1.2 «SSRF», пять тем, по тому же
оглавлению. Категория кода — `SSRF`, тег `ssrf`: и то и другое заводится
вместе с первой темой подраздела, как велит свод 9.2.

## Миссия `appsec-stage0-close` — ЗАВЕРШЕНА

Досье — `~/.supermanager/missions/appsec-stage0-close/dossier.md`. Отчёт —
`STAGE0-CLOSE.md`, 437 строк; последней строкой стоит фраза-маркер из досье,
поставленная 2026-08-23 после прогонов. Маркер в этом файле не повторяется
намеренно: досье требует его ровно в одном месте. Статус для демона —
`.smgr/appsec-stage0-close/status.json`, вывод прогонов —
`.smgr/appsec-stage0-close/final/`. Коммиты миссии: `37f3316` (решение 2),
`eec2c57` (сайт и файл для телефона), `2a3fbe4` (Ф-36), `5b5d2fa` (отчёт,
Ф-29/Ф-33/Ф-37/Ф-39, остаток предупреждений).

**Что сдано.** Четыре решения оператора от 2026-08-23 проведены; условия
приёмки этапа 0 закрыты, кроме двух, которые закрываются только оператором.
Одиннадцать тем этапа 0 годны. Подробный разбор — в `STAGE0-CLOSE.md`; ниже
только то, что нужно, чтобы продолжить работу.

**Состояние проверок на 2026-08-23**

```text
make check          10 ошибок, 82 предупреждения, 3 снято — не пройдено
                    все 10 ошибок — C-VOL-OVER (норма объёма L2, вопрос к оператору)
                    vale 0/42/2  markdownlint 0/0/0  glossary 0/0/0
                    style 0/39/0  model 0/1/0  links 0/0/1  volume 10/0/0
make lint-selftest  166 из 166 — пройдено
make links          0/0/1, 41 внешний адрес — пройдено (единственная проверка с сетью)
make site           18 страниц, 8 картинок, 724 внутренние ссылки
make check-site     пройдено
make phone          dist/appsec-stage-0.html, экран 390×844
make check-phone    11 тем, 6 схем (битых 0), 556 внутрь / 23 наружу — пройдено
make diagrams       7 схем;  make glossary  86 терминов;  make topics  196 тем
semgrep check.py    два правила из двух — сверено
```

Предупреждения: `L-CLERICAL2` × 42 (все просмотрены — законное существительное
«данные»), `S-SENT-LONG` × 39, `C-REF-ORPHAN` × 1 (пилот этапа 1, ссылаться
пока нечему).

**Осталось — только за оператором**

1. **Вилка нормы объёма L2.** Вопрос в `.smgr/appsec-stage0-close/ask.json`
   от 2026-08-23, ответа нет. Варианты: `widen` (800–1700, рекомендован),
   `keep`, `warning`, `split`. Пока ответа нет, `make check` красный ровно на
   десяти строках `C-VOL-OVER` — это измеренная находка Ф-42, а не поломка
   текста, и по правилу 1 досье резать готовые тексты нельзя.
   **Если придёт `.smgr/appsec-stage0-close/answer-N.md`** — применить ответ к
   таблице 3.1 `PLAYBOOK.md` и словарю `NORM` в `tools/wordcount.py`, затем
   прогнать `make check` и дописать раздел 12 `STAGE0-CLOSE.md`.
2. **Внешнее техническое ревью для L1** (`same-origin-policy`). Решением 2
   оператора не добывается; условие записано открытым в отчёте.

**Что закрыто, чтобы не искать заново**

- 13 ошибок `make check`: `C-FM-TITLE-LEN` × 5 (все 12 `title` теперь 21–55
  знаков), `G-CANON` × 8 (`argon2id` → `Argon2id` в `password-storage`).
- Все 26 находок приёмки, кроме Ф-42. Ф-33 закрыта двумя новыми схемами
  (`tls-and-proxy`, `app-architecture`) и зафиксированным решением «транскрипт
  вместо схемы» в `cors`; Ф-37 — парой транскриптов предполётного обмена;
  Ф-39 — правкой пункта 7 чеклиста `app-architecture` и пустым `asvs` в его
  шапке; Ф-29 — врезкой про `Vary`, где нормативность отдана Fetch Standard.
- Все 12 системных расхождений свода (С-01…С-12): каждая правка датирована
  2026-08-23 в `PLAYBOOK.md` и называет находку.
- Все 6 заявок к обвязке § 11 приёмки. Появились `tools/linkcheck.py`
  (6 правил, сетевой режим `make links`, отказ 403/429/503 помечается как
  недостижимость из среды), `mask_code()` в `tools/stoplist.py`, правила
  `C-BODY-IDENT`, `C-HEAD-VERSIONS`, `C-BODY-ORIGIN`; неразрывные пробелы
  расставлены (174).
- Три записи реестра, которых не хватало: `openapi-311`, `graphql-spec`,
  `mdn-cors-errors` — заведены руками по одной, источники открыты лично.
- Чеклист `rest-and-graphql` сведён с 8 пунктов к 7. Дефекты 7.6 в принятых
  темах проверены глазами: нарушений нет.
- `tools/exceptions.yaml`: две записи, три снятых срабатывания, мёртвых нет.
  Запись `csp` `S-LINK-EXT` на `www.w3.org/TR/CSP3` больше не нужна — сетевой
  линк-чекер сам отличает антибот-щит от битого адреса.

**Правила, которые здесь легко нарушить**

- Ни одного «плюса» без запущенной команды; нечем проверить — пишется «не
  подтверждено».
- Факты правятся только по первоисточникам, открытым лично. Кэш скачанного —
  `.smgr/appsec-review-8/src/` и `.smgr/appsec-stage0-close/src/`.
- Готовые тексты не режутся ради нормы; проверки не подгоняются под содержание.
- `sources.yaml` правится руками по одной записи; сносить и перегенерировать
  нельзя.
- Наружу ничего: ни `git push`, ни хостинга. Локальные коммиты — можно.
- Новых тем не начинать (решение 4): этап 1 открывается оглавлением, и это
  работа другой миссии.
- Ловушки ссылок: `www.w3.org` отдаёт 403 при повторных обращениях (адрес при
  этом верен), `cisa.gov` и `graphql.org` — 403, `*.gov.ru` и `ptsecurity.com`
  — проблемы TLS.
- Системный `python3` не видит `yaml`: все скрипты запускаются через
  `.venv-tools/bin/python`.
- Знак `§` в корпусе стоит перед неразрывным пробелом (U+00A0). Любое
  сопоставление строк по тексту тем должно писать `\xa0` явно, иначе не
  находит.

## Миссия `appsec-infra` — ЗАВЕРШЕНА

Обвязка фазы 1: сборка сайта, проверки, шаблоны, глоссарий. Досье —
`~/.supermanager/missions/appsec-infra/dossier.md`, отчёт — `INFRA.md`
(маркер сдачи стоит последней строкой отчёта). Всё сдаётся двумя командами:
`make check` — вердикт по проверкам, `make site` — собрать и проверить сайт.

**Что уже сделано и проверено**

- Каталог заведён под git. Первый коммит `3a53c37` — снимок состояния до фазы 1.
  Remote нет и не будет. `.gitignore` исключает окружения, `site/`, `.smgr/`.
- `SCOPE.md` — персоны, анти-персона, non-scope. Читатель один, это сокращает
  документ; вторая персона свода в проекте отсутствует.
- Оснастка поставлена и запущена: Vale 3.18.0 (`tools/bin/vale`, скачан с
  GitHub), markdownlint-cli2 0.23.2 и mermaid-cli 11.16.0 (`tools/node/`, pnpm),
  mkdocs-material 9.7.7 (`.venv-site/`, отдельный venv, чтобы не тронуть
  semgrep в `.venv-tools`), chrome-headless-shell 152 в `~/.cache/puppeteer`.
- Стайлгайд Vale написан (`tools/vale/styles/AppSec/`, 16 правил), прогнан по
  всем 12 темам, ложные срабатывания вычищены.
- `STYLE.md` (685 строк) — соответствие «пункт свода → правило → уровень».
  Три уровня: error роняет `make check`, warning печатается, «глазами» —
  закрытый список § 9 из 19 строк, который машина не проверяет. § 4 требует,
  чтобы хеджирование не помечало ни одно правило, и это проверяется машинно.
- Глоссарий: `glossary.yaml` (86 терминов, 10 групп) — рукописный источник,
  `GLOSSARY.md` (664 строки) генерируется `tools/gen_glossary.py`, сверка —
  `--check`. Правки только в yaml.
- `tools/glossary_lint.py` — G-CANON (написание с точностью до регистра, дефис
  значим), G-FIRST (аббревиатура без расшифровки, с учётом транзитивных
  `prerequisites`), G-SYNSET, G-UNUSED, G-GLOSS, метрика G-NEW-PAGE.
- `tools/lint_style.py` — S-CODE-WIDTH, S-CODE-LEN, S-CODE-PUNCT, S-LIST-ORDER,
  S-LIST-MIX, S-SENT-LONG, S-PARA-LONG, S-HEAD-DEPTH.
- `tools/mdtext.py` — общий разбор: маскирование с сохранением смещений
  (`prose`, `code`, `prose_spans` одной длины с исходником), границы
  предложений и абзацев, начала предложений за разметкой, основы слов.
- `tools/exceptions.yaml` + `tools/check.py` — одна команда, один вердикт:
  реестр (3 проверки), линтеры (4), отчёты (2, без вердикта). Исключение одно;
  восемь `argon2id` в исключения не попали и не попадут — это замечание к
  содержанию, и `make check` из-за них законно красный.
- `tools/lint_selftest.py` — 62 утверждения «правило ловит своё и молчит на
  законном», все зелёные. Он нашёл два дефекта: `S-HEAD-DEPTH` не срабатывал
  никогда (`\A` вместо `^`), `L-NAV` не видел текста ссылки (Vale снимает
  разметку до правил — половина правила вынесена в `NavLink.yml`, `scope: raw`).
- Контентная модель: `SCHEMA.md` (391 строка) — что где лежит, девять типов
  записей, 24 поля с нормами, состав блоков по глубине, заголовок как вторая
  копия frontmatter, таблица всех 47 правил `C-*`, закрытый список «глазами».
  `taxonomy.yaml` — машинные словари (стадии с папками, режимы, теги, состав
  блоков по уровню с различением «обязателен» и «разрешён»).
  `tools/validate_content.py` — 47 правил `C-*`; подключён в `check.py`
  линтером `model` целиком по корпусу, потому что цикл в графе предпосылок и
  занятый `order` — свойства набора тем, а не страницы. Расхождение 3.1 и
  канона части 4 по блокам 3, 4 и 11 разобрано в `SCHEMA.md` § 4.1.
- `tools/lint_selftest.py` дорос до 153 утверждений: фикстура есть у каждого из
  47 правил `C-*`, у шести правил ссылок и у `S-PLACEHOLDER`. Ради этого из
  `load_pages()` выделен `read_page(path)` — страничные правила гоняются на
  фикстуре во временном каталоге, в `content/**` не пишется ничего.
- `tools/linkcheck.py` — шесть правил: `S-EXT-IN-BODY` (внешний адрес вне блока
  14), `S-LINK-BARE` (адрес в прозе без `<…>`), `S-LINK-TOPIC` (ссылка на
  несуществующую тему), `S-LINK-MD` (файл/анкорь), `S-LINK-SOURCE-URL` (сноска
  расходится с реестром, warning), `S-LINK-EXT` (адрес не открылся, warning,
  только под `tools/check.py --external`). Сеть за флагом: красный вердикт
  `make check` должен означать «текст сломан», а не «сети нет». Из 38 внешних
  адресов открылись 37, `www.w3.org` снят исключением (Cloudflare).
- `templates/L1.md`, `L2.md`, `L3.md` — по канону части 4 и составу блоков
  `SCHEMA.md` § 4. Шаблон проходит все страничные правила модели: скопированная
  тема зелёная до первой правки. Всё удаляемое размечено `⟨…⟩` (включая врезки
  `⟨Автору⟩` с нормами блоков), это ловит новое правило `S-PLACEHOLDER` (error)
  в `lint_style.py` по `raw`; вводная врезка стоит перед `h1`, поэтому забытую
  ловит вторым способом markdownlint (MD041). Селф-тест перебирает `ctx.depths`
  и проверяет каждый шаблон дважды: «все правила модели молчат» и «заполнители
  на месте»; уровень без шаблона виден как незакрытое утверждение.
- Полный прогон `tools/check.py` на корпусе: **13 ошибок** (8 `G-CANON`
  `argon2id` в `password-storage`, 5 `C-FM-TITLE-LEN` — `title` до 95 символов
  при норме 60), **270 предупреждений** (130 `L-NBSP`, 100 `S-SENT-LONG`,
  28 `L-CLERICAL2`, 5 `G-FIRST`, 3 `C-BODY-CHECKLIST`, 2 `L-TIMELESS`,
  1 `C-REF-ORPHAN`, 1 `S-LINK-SOURCE-URL`), 2 снято исключением. Снимок вывода целиком —
  `.smgr/appsec-infra/findings-snapshot.txt`, из него собирается список находок
  для `INFRA.md`.

**Сборка сайта — сделана и проверена**

- `mkdocs.yml` в корне пишется руками; `build/mkdocs.yml` собирается кодом и
  наследует корневой через `INHERIT` (`nav`, `docs_dir`, `site_dir`). Сборщик
  корневой конфиг не читает — и не смог бы: тег
  `!!python/object/apply:pymdownx.slugs.slugify` не разбирается `yaml.safe_load`,
  а именно этим слагом считаются анкоря, которые проверяет `S-LINK-MD`.
- `tools/build_site.py` — staging-дерево `build/site-src`, `content/` только
  читается. За прогон: 12 тем, 5 генерируемых страниц, 71 ссылка из
  `` `topic-id` ``, 40 вклеенных расшифровок аббревиатур, 5 схем. Правки
  считаются смещениями по `doc.raw`/`prose_spans`, поэтому `cookies` внутри
  листинга ссылкой не станет; перекрытие правок — исключение, а не порча файла.
- `strict: true` плюс `validation.links.{not_found,anchors}: warn` — вторая
  рубрика под DoD 17: битая ссылка или анкорь роняют `make site`. Проверено
  подложенной поломкой: «Aborted with 2 warnings in strict mode!», код 1.
- `use_directory_urls: false` + плагин `offline` — сайт открывается из `file://`.
  Схемы mermaid нарисованы заранее в SVG (`tools/render_diagrams.py`, кэш по
  хешу содержимого), клиентского JS для них нет.
- `tools/check_site.mjs` и `make check-site` (входит в `make site`) — браузерная
  проверка: chrome-headless-shell, сеть обрублена, требуется ноль внешних
  запросов, работающий поиск и чистая консоль. Обратный опыт проведён.
- `Makefile` — 16 целей, `make help` печатает список из комментариев `## `.
  `tools/setup.sh` восстанавливает окружение из gitignore, идемпотентен,
  `.venv-tools` не пересоздаёт никогда (в нём semgrep другой миссии); режим
  `--check` только проверяет и печатает версии — все семь блоков зелёные.
- `INFRA.md` (306 строк) — движок и решения, доступность инструментов, как
  стали машинно проверяемы пункты DoD 15/16/17/18/21/22, находки с разбивкой по
  темам, объём против нормы, хрупкие места.

**Самая дорогая находка захода: «офлайновый» сайт ходил в сеть**

Плагин `offline` у Material вставляет в каждую страницу
`<script src="https://unpkg.com/iframe-worker/shim">`: браузер не создаёт Web
Worker из `file://`, а поиск Material живёт в воркере. Сборка была зелёной, а
поиск без сети висел на «Инициализация поиска» — 0 совпадений против 13 с
сетью. Шим (1391 байт, MIT) лежит теперь в `tools/vendor/` с провенансом и
sha256, ссылка после сборки переписывается на относительный путь (18 страниц).
Свойство закреплено проверкой `make check-site`, иначе оно сломается снова и
снова молча. Мораль на будущее: «плагин называется offline» — не проверка.
При проверке браузером не ставить `--allow-file-access-from-files`: с ним
воркер создаётся и без шима, проверка становится ложно зелёной.

**Что миссия намеренно не сделала**

- Ни одной правки в `content/**`. Находки — списком в `INFRA.md` (13 ошибок,
  270 предупреждений на 2026-08-23), правит их приёмка или автор.
- Виджет обратной связи (исключён досье). DoD 22 закрыт наполовину: вместо
  «предложить правку» каждая страница кончается путём к исходнику.
- Деплой и хостинг — только оператор лично.
- Норма объёма L2 не тронута, хотя все десять L2-тем выше вилки 800–1200 слов;
  наблюдение оставлено вопросом в `INFRA.md` § 6.3.

Поля `next:` в схеме нет и не будет: DoD 21 закрывают `prerequisites`, блок 13
по 9.5 п. 8 и навигация, выводимая из `stage`/`order` (9.1 п. 7). Прежняя
запись «первое известное замечание — нет поля `next:`» отменена, разбор — в
досье, раздел «Контентная модель и схема».

**Три грабли этого захода, чтобы не наступать снова**

1. **`\b` в Vale не работает с кириллицей.** Vale на Go/RE2, `\b` и `\w` там
   ASCII-only: `\bпростой\b` матчится через раз. Все правила написаны с
   `nonword: true` и явными границами `(?:^|[^\p{L}\p{N}_-])...(?:[^\p{L}]|$)`.
   Без правой границы «скоро» ловится внутри «скорость» — так и было, 10 ложных.
2. **npm на машине нет, есть pnpm.** `/usr/bin/pnpm`, node v26.7.0.
   Postinstall-скрипты pnpm блокирует: Chromium для mermaid-cli ставится руками
   через `./node_modules/.bin/puppeteer browsers install chrome-headless-shell`.
3. **Внутренние ссылки в темах — не markdown-ссылки.** Перекрёстные ссылки
   стоят как `` `topic-id` `` в обратных кавычках, внешние адреса — автоссылками
   `<https://...>` и только в блоке 14. Markdown-ссылок в `content/` нет ни
   одной. Поэтому линк-чекер свой, а движок превращает id в ссылки на сборке,
   не правя исходники.

**Границы, которые нельзя нарушить:** `content/**/*.md` не править ни строкой —
восемь из этих тем прямо сейчас правит миссия приёмки `appsec-review-8`.
Замечания линтеров идут списком в `INFRA.md`, а не правками в темы.

## Миссия `appsec-review-8` — ЗАВЕРШЕНА

Приёмка восьми непринятых тем этапа 0, 22–23 августа 2026. Отчёт —
`WRITE-REVIEW-2.md` (1329 строк), последняя строка — маркер сдачи. Досье:
`~/.supermanager/missions/appsec-review-8/dossier.md`, принятые по ходу решения
дописаны в его раздел «РЕШЕНИЯ ПО ХОДУ».

**Что сделано.** Восемь тем (`t-0-0-04`…`t-0-0-11`) прогнаны по 33 пунктам части
13 и 25 пунктам DoD 11.2 — 464 статуса, каждый по замеру. Факты сверены с
первоисточниками, скачанными в `.smgr/appsec-review-8/src/` (56 файлов, 14 МБ):
из ~80 проверяемых утверждений неверны три. Найдено 26 находок (Ф-22…Ф-47: 7
высоких, 15 средних, 4 низких) и 12 системных расхождений внутри свода
(С-01…С-12). Вердикт по каждой теме — годна при условиях; «не годна» нет.

**Три правки на месте** (только фактические ошибки, не закоммичены):
`content/stage-0/security-headers.md:82` — «три требования MUST» → «четыре»
(RFC 6797 §§ 7.2, 8.1, 8.4, 8.5); `content/stage-0/sessions-vs-tokens.md:279` —
ответ про части JWT переписан по RFC 7519 § 3; `content/stage-0/csp.md:88` —
«ресурс разрешает выражение» → «ресурс соответствует выражению» (CSP3 § 6.7.2.7).

**Что ждёт оператора** (§ 13 отчёта):

1. Норма объёма L2: семь тем превышают вилку 800–1200 на 5,1–14,5 % (среднее
   +11,2 %). Резать текст или менять 3.1 — решение оператора, я только замерил.
2. Уровни `same-origin-policy` (L2) и `security-headers` (L3) — оставлены
   оператору, наблюдения в § 12 отчёта.
3. Двенадцать правок свода (§ 10). Первая блокирующая: `asvs`, `wstg`, `plan_id`
   стоят во всех одиннадцати темах, а в схеме 9.3 их нет, при этом 9.4 объявляет
   несоответствие схеме блокирующей проверкой.
4. Дефекты в **уже принятых** темах: восьмипунктные чеклисты в `cookies.md` и
   `http-basics.md` (`C-BODY-CHECKLIST`); четыре нарушения 7.6 —
   `url-and-encoding.md:101` и `cookies.md:130` (врезка 6 строк при пределе 5),
   `url-and-encoding.md:110` и `cookies.md:130` (раздел начинается с врезки).
   Неразрывных пробелов (6.4) нет ни в одном файле `content/` при 130
   предупреждениях `L-NBSP` по репозиторию.
5. Четыре записи, которых не хватает в `sources.yaml`: спецификация GraphQL
   (October 2021), OpenAPI 3.1.1, MDN «CORS errors» в блок 14
   `app-architecture`, § 7.2 RFC 6797 в блок 14 `security-headers`. Сам не
   добавлял: запись без ссылки из темы даёт лишнее предупреждение `validate.py`,
   а править тело темы — работа автора.

**Заявки к обвязке** (§ 11 отчёта, `tools/` не тронут): линк-чекер на момент
замеров отсутствовал, а в 00:28 23 августа `appsec-infra` добавила
`tools/linkcheck.py` — заявка закрыта, оговорка про `www.w3.org` (403 при
повторных обращениях, адрес верен) относится теперь к нему;
`stoplist.py` считает совпадения 6.5 внутри огороженных блоков
(`!==`, `[Invoice!]!`); нет сверки `asvs` в шапке с идентификаторами в теле; нет
проверки печати CWE с версией списка (9.6 п. 25).

**Числа обвязки на конец миссии** (`.venv-tools/bin/python tools/check.py
--full`): 13 ошибок, 269 предупреждений, 10 проверок, не пройдено. В восьми
темах — 5 ошибок (все `C-FM-TITLE-LEN`: `app-architecture` 95,
`security-headers` 89, `cors` 80, `tls-and-proxy` 78, `rest-and-graphql` 63) и
156 предупреждений. Остальные 8 ошибок — `G-CANON` «argon2id» в
`content/stage-1/password-storage.md`, вне набора. `validate.py` — зелёный, 0
ошибок, 12 предупреждений. `gen_topics.py --check` — чисто.

## Миссия `appsec-pilot-auth` — ЗАВЕРШЕНА

Пилот L1 закончен 2026-08-22. Отчёт — `PILOT-AUTH.md`. Досье:
`~/.supermanager/missions/appsec-pilot-auth/dossier.md`. Это увязка 2
`WRITING-PLAN.md`, и она была обязательна до любого планирования сроков.

Написана одна тема уровня L1 — `content/stage-1/password-storage.md`
(`t-1-6-07`, хранение паролей). К ней: лаба `pilot/lab/password-storage/` из
семи файлов в формате Secure Code Game, правило Semgrep в `pilot/semgrep/`
со своей сверкой, три новые записи реестра (307 записей). Валидатор зелёный:
0 ошибок, 12 предупреждений.

### Четыре вещи, которые надо знать, прежде чем продолжать

1. **Норматив свода по весу L1 не подтвердился, и это главный результат.**
   Измеренное отношение L1 к L2 — 6,3 против 3,2 по нормативу; отношение
   «примеры к прозе» — 2,4 против 0,83. Оба расхождения в одну сторону: L1
   дороже, чем считает `PLAYBOOK.md` 3.4, и дорога в нём не проза, а аппарат
   примеров. Точка одна; разбор и пересчёт остатка — `PILOT-AUTH.md` раздел 3.

2. **Стенд для L1 обязателен и при этом почти бесплатен.** Блоки 4, 7 и 8 без
   него не пишутся: процедуры ретеста нет ни в одном из шести источников
   темы. Docker не понадобился — формат Secure Code Game покрыл всё на
   стандартной библиотеке Python, за 4 минуты и ноль рублей. Правило 8.1
   («стенд в Docker Compose») шире, чем нужно: предложение 19 отчёта.

3. **Правило двух стеков выполнено полностью впервые за двенадцать тем**, и
   только потому, что миссия завела в реестр `python-hashlib` и `node-crypto`.
   Дефицит, который называли обе предыдущие миссии, закрыт **точечно** — для
   двух стеков и одной темы, не вообще. Для следующей темы его придётся
   закрывать снова.

4. **Тема к публикации не готова, и это сказано в DoD прямо.** Из 25 пунктов
   выполнено 12. Пункты 15, 16, 18 закрыть нечем (Vale, markdownlint,
   глоссарий), 22 — нет движка, 19 и 24–25 — за оператором. Отдельно пункт 3
   (раздел есть в утверждённом оглавлении этапа) помечен **невыполненным**:
   оглавление этапа 1 не зафиксировано, а пилот по устройству пишет тему вне
   этапа. Это противоречие свода, предложение 18 отчёта.

### Если работу продолжает другой агент

Доделывать по этой миссии нечего. Список решений оператора — `PILOT-AUTH.md`
раздел 9, восемь пунктов; там же судьба Semgrep в `.venv-tools`.

**Перед письмом следующих тем читать то же, что и раньше** (`WRITE-0.md` § 6,
`WRITE-REVIEW.md` § 6, `WRITE-0B.md` §§ 1 и 8), плюс `PILOT-AUTH.md` § 8 —
восемь новых предложений к своду, нумерация сквозная, 17–24.

### Оснастка и грабли этого захода

**Semgrep 1.174.0 стоит в `.venv-tools`** вместе с 66 зависимостями. PyYAML не
тронут (6.0.3), все четыре инструмента проекта работают.

- **`pip install semgrep` не укладывается в 300 секунд.** Второй заход — около
  9 минут. Запускать фоном.
- **`semgrep --test` этой версии падает** с `IndexError` в `relatively_eq`,
  когда правило и тест-кейсы лежат в подкаталоге проекта. Обход —
  `pilot/semgrep/check.py`, разбор JSON обычного прогона.
- **`# nosemgrep` действует на строку прямо под ним.** Многострочное
  объяснение перед подавлением не работает: строка с `nosemgrep` должна быть
  последней перед находкой.
- **Рекомендованные OWASP параметры scrypt не работают без явного `maxmem`** —
  ни в Python, ни в Node. Умолчание OpenSSL 32 МиБ, нужно 128 МиБ. Точный
  порог: `128·N·r` плюс `128·r·p + 256·r` байт, проверено на пяти наборах.
  Ни в одном источнике этого нет.
- **`.replace(",", " ")` для разделителя групп в числе** заменяет и запятые в
  тексте сообщения. Форматируйте число отдельно от строки.

---

## Миссия `appsec-write-0b` — ЗАВЕРШЕНА

Третья миссия письма закончена 2026-08-22. Отчёт — `WRITE-0B.md`. Досье:
`~/.supermanager/missions/appsec-write-0b/dossier.md`. Это увязка 3
`WRITING-PLAN.md`.

**Этап 0 написан целиком: одиннадцать тем из одиннадцати.** Эта миссия
написала восемь (`t-0-0-04` … `t-0-0-11`), разобрала поимённо все двадцать одну
находку приёмки `WRITE-REVIEW.md`, расписала DoD по 25 пунктам на каждую тему
и пересчитала DoD трёх старых. Валидатор зелёный: 0 ошибок, 11 предупреждений
(было 9; оба новых — `[ГЛУБИНА]`, обоснованы в темах).

### Четыре вещи, которые надо знать, прежде чем продолжать

1. **Ни одна тема этапа 0 не готова к публикации, и это не осторожность.**
   У всех одиннадцати `status: draft`. Закрыть нечем пять пунктов DoD (Vale,
   markdownlint, глоссарий, виджет, запуск примеров) и ещё два выполняет
   лично оператор (перечитать через сутки, поставить в очередь на ревизию).

2. **Норма объёма L2 не выдерживается второй раз подряд, и теперь это
   измерено дважды.** Восемь тем писались с прямой установкой уложиться в
   800–1200: перерасход упал с 36,7 % до 11,2 %, но нулём не стал. Резать
   дальше — резать содержание, что запрещает 3.3. Решение за оператором:
   `WRITE-0B.md` правка 12. Считать объём теперь есть чем —
   `tools/wordcount.py`, три метода сразу.

3. **Дефицит реестра тот же и никуда не делся: нет документации стеков.**
   Из-за него правило двух стеков выполнено один раз из одиннадцати
   (в `t-0-0-11`, на паре REST + GraphQL), а блок «как выглядит в коде» в
   пяти темах написан псевдокодом. Это по-прежнему следующая по очевидности
   работа по источникам, и без неё пункт 1 части 14 свода не закрывается.

4. **Текст CSP Level 3 из этой среды читается только до § 4.5.** `www.w3.org`
   закрыт проверкой Cloudflare для командной строки, а штатный фетчер отдаёт
   длинный документ усечённым. Тема `csp` написана честно: то, что видно, —
   по спецификации, остальное — по MDN, и это помечено в самой теме. Прежняя
   запись в этом файле («`w3.org` открывается штатным фетчером») верна только
   для коротких страниц.

### Если работу продолжает другой агент

Доделывать по этой миссии нечего. Что дальше — решает оператор; его список
собран в `WRITE-0B.md` раздел 7, восемь пунктов, и он включает нерешённые
пункты первой миссии.

**Перед письмом следующих тем прочитать три места:** `WRITE-0.md` раздел 6
(одиннадцать противоречий свода и как их решили), `WRITE-REVIEW.md` § 6
(реестр находок) и `WRITE-0B.md` разделы 1 и 8 (что из находок применено и
пять новых правок к своду). Решать это заново — потерянное время.

**Стоп-лист связок ведётся и работает.** Он в `tools/stoplist.py` вместе с
проверкой по списку 6.5 и мерой шаблонности. Дополнять список `CONNECTIVES`,
а не переписывать. Что мера показывает сейчас: общих четырёхграмм прозы во
**всех** темах набора после вычета служебного аппарата — 10 у трёх старых тем
и **0** у восьми новых. Объём считает `tools/wordcount.py` тремя методами.

### Оснастка и грабли этого захода

Скачивание — прежним приёмом (`curl` с браузерным User-Agent плюс свой
HTML→текст на `html.parser` со сбросом счётчика на `<body>`; PDF —
`pdftotext -layout`). Новое:

- **Оглавление курса выглядит как содержательная страница.**
  `graphql.org/learn/` отдаёт 200, у неё есть заголовки и абзацы — но абзацы
  аннотируют шестнадцать уроков, а не объясняют предмет. Отличать надо по
  наличию утверждений, которые можно процитировать, а не по объёму текста.
  Запись отвязана от темы и помечена в реестре.
- **Двоеточие в поле `title` реестра ломает YAML.** Без кавычек скаляр
  становится отображением; сообщение валидатора указывает на строку ниже.
- **Синтаксис инструмента по памяти — тот же дефект, что число по памяти.**
  Директива обратного прокси, выписанная по памяти, заменена псевдокодом:
  документации прокси в реестре нет.
- **Адрес страницы MDN может переехать при 200.**
  `/Web/Security/Same-origin_policy` → 301 →
  `/Web/Security/Defenses/Same-origin_policy`. Смотрите `url_effective`.

---

## Миссия `appsec-write-0` — ЗАВЕРШЕНА

Первая миссия **письма** закончена 2026-08-22. Отчёт — `WRITE-0.md`. Досье:
`~/.supermanager/missions/appsec-write-0/dossier.md`. Это увязка 1
`WRITING-PLAN.md`.

Сделано: зафиксировано оглавление этапа 0 со всеми одиннадцатью уровнями
(0 × L1, 10 × L2, 1 × L3), написаны три темы в `content/stage-0/`, расписан
Definition of done по 25 пунктам на каждую, снят замер времени, переоценена
достаточность источников, предложены одиннадцать правок к `PLAYBOOK.md`.
Валидатор зелёный: 0 ошибок, 9 предупреждений — те же, что были.

### Три вещи, которые надо знать, прежде чем продолжать

1. **В этапе 0 нет тем уровня L1, и это не экономия.** Блоки, которые делают
   тему L1 (эксплуатация, проверка фикса, правило SAST, лаба), розданы этапу 1
   самой картой тем — `audit.yaml` фиксирует это дословно по пяти темам.
   Следствие: стоимость самого дорогого случая после этой миссии по-прежнему
   неизвестна, и увязка 2 `WRITING-PLAN.md` обязательна до планирования сроков.

2. **Норма объёма L2 из свода не выдерживается.** Три темы, написанные честно
   по открытым источникам, дали 1486, 1668 и 1630 слов прозы при норме
   800–1200. Правка на сокращение убрала 4 %, дальше пришлось бы резать
   содержание. Это первое собственное число проекта, и оно противоречит
   `PLAYBOOK.md` 3.1. Разбор — `WRITE-0.md`, раздел 6, правка 2.

3. **Найден один системный дефицит реестра: нет документации фреймворков.**
   Блок «как выглядит в коде» по своду должен нести парные листинги на двух
   стеках, а в реестре нет ни одной такой записи. Все три темы написаны
   псевдокодом с явной пометкой. Из-за этого пункт 1 части 14 свода («сколько
   языков в блоке 5») закрыть нельзя: вопрос упирается не в методику, а в
   реестр. Это следующая по очевидности работа по источникам.

### Если работу продолжает другой агент

Доделывать нечего: миссия закончена. Что дальше — решает оператор, и список
его решений собран в `WRITE-0.md`, раздел 7.1. Самое короткое из них: два
уровня в оглавлении этапа 0 (`same-origin-policy` и `security-headers`)
зависят от того, есть ли у него по ним личный опыт.

**Перед письмом следующих тем прочитать `WRITE-0.md` раздел 6 целиком.** Там
одиннадцать мест, где свод сам себе противоречит или невыполним, и по каждому
записано, как решили при письме. Решать их заново — потерянное время.

### Оснастка письма

Источники скачиваются приёмом из раздела про `appsec-gapfix` ниже. Новая
грабля этого захода:

- **`<head>` без закрывающего тега съедает весь документ.** Конвертер
  HTML→текст, который выбрасывает `head` по счётчику пропуска, на странице без
  `</head>` больше никогда не вернёт счётчик в ноль. `url.spec.whatwg.org` —
  ровно такой случай: 729 КБ HTML дали 1,3 КБ текста, страница выглядела
  пустой. Лечится сбросом счётчика на `<body>`. Класс тот же, что у
  `re.DOTALL` в прошлой миссии: молчаливая потеря содержимого.
- **Таблица сравнения парсеров у Claroty — картинка.** Пять категорий
  расхождений и восемь CVE есть в тексте, построчное поведение шестнадцати
  библиотек — только на изображении.

Проверено 2026-08-22: `rfc6265bis` всё ещё черновик -22 от 1 декабря 2025 в
очереди RFC Editor; запись реестра верна, обновлять пока не нужно.

---

## Миссия `appsec-gapfix` — ЗАВЕРШЕНА

Находки финальной проверки закрыты 2026-08-22. Отчёт — `GAP-FIX.md`, сводка —
в верхней части `STATUS.md`. Досье:
`~/.supermanager/missions/appsec-gapfix/dossier.md`.

Реестр вырос с 254 до 301 записи, привязки изменены у 40 тем и у этапа 1,
в `audit.yaml` исправлено 82 блока в 42 темах — каждый с сохранённым прежним
значением в поле `corrections`. Тем с блоком `partial` или `none` было 43,
осталось 20; из них девять — настоящий остаток, остальные partial — свойство
темы (дефект не в коде приложения либо тема заявлена планом как обзорная).

### Если работу продолжает другой агент

Ничего доделывать не нужно: миссия закончена, валидатор зелёный, отчёт написан.
Что может понадобиться дальше — либо написание гайдбука (отдельная задача,
оператор её закажет), либо повторная проверка реестра, инструкция ниже.

### Оснастка, которую стоит переиспользовать

Скачивание и превращение страницы в текст делалось так (файлы жили в
scratchpad сессии и не сохранялись в репозиторий):

```
curl -sS -L --max-time 40 \
  -A "Mozilla/5.0 (X11; Linux x86_64) AppleWebKit/537.36 (KHTML, like Gecko) Chrome/126.0 Safari/537.36" \
  -H "Accept-Language: en-US,en;q=0.9" -o page.html "$URL"
```

затем свой HTML→текст на `html.parser` (выбрасывать `script`, `style`, `nav`,
`svg`, `noscript`, `iframe`, `form`, `head`; блочные теги переводить в перевод
строки). Для PDF — `pdftotext -layout`. `pandoc`, `lynx`, `w3m` и `html2text` в
среде отсутствуют, в `.venv-tools` только `PyYAML`.

Грабли, на которые наступили в этот заход:

- **`re.DOTALL` при правке YAML.** Шаблон вида `(?:      .*\n|\n)*?` с флагом
  `re.S` съедает файл до конца, потому что `.` начинает совпадать с переводом
  строки. Четырнадцать абзацев уехали в последнюю запись реестра. Правьте
  построчно, а не регулярками по всему файлу.
- **Тире и двоеточие в скалярах YAML.** `version_or_date: Version 2025, 18 ноября
  2024 (история версий: 1.0 — ...)` ломает разбор: двоеточие с пробелом внутри
  незакавыченного скаляра — это отображение. Кавычить.
- **Код на страницах бывает в HTML-комментариях.** На `dev.java` примеры лежат
  внутри `<readonly-code-editor><snippet><!-- ... --></snippet>`; текстовый дамп
  их теряет, и страница выглядит пустой. Прежде чем объявить страницу
  бессодержательной, посмотрите сырой HTML.
- **Редиректы меняют канонический адрес.** `semgrep.dev/docs/*` уводит на
  `docs.semgrep.dev/*`, `trivy.dev/latest/docs/*` — на `trivy.dev/docs/latest/*`,
  `codeql-cli/getting-started` — на `docs.github.com/.../prepare-code-for-analysis`.
  Записывайте тот адрес, который отдал `%{url_effective}`.

### Как проверить, что дыры действительно закрыты, а не «закрыты»

Проверка та же, что была планкой миссии: открыть источник, привязанный к теме, и
убедиться, что нужный блок скелета по нему пишется с конкретикой уровня «что
именно искать в коде» и «какой именно ретест». Формальный признак, который можно
прогнать скриптом: раскрыть наследование для каждой темы и посмотреть, остаётся
ли хоть один источник за вычетом семнадцати навигационных записей (ищутся грепом
по фразе `УКАЗАТЕЛЬ, НЕ ПОКРЫТИЕ` в `sources.yaml`). Сейчас таких тем ноль.

---

## Миссия `appsec-final` — ЗАВЕРШЕНА


Финальная проверка реестра употреблением закончена 2026-08-22. Отчёт —
`FINAL-CHECK.md`; краткая сводка и список того, что осталось оператору, —
в верхней части `STATUS.md`. Досье:
`~/.supermanager/missions/appsec-final/dossier.md`.

Этот файл остаётся как инструкция на случай, если проверку придётся повторить —
например, после правки учебного плана, после закрытия дыр или через несколько
месяцев, когда документация снова разъедется.

### Как воспроизвести проверку

1. Прочитать досье миссии и часть 4 `PLAYBOOK.md` (канонический скелет — мерило).
2. Собрать выборку по четырём категориям досье: темы с блоком `partial` в
   `audit.yaml`; темы без собственных источников; темы с единственным
   источником; контрольная группа из тем, где все блоки `ok`. Прошлый состав —
   67 тем, машинный список в отчёте и в разделе «Выборка» `FINAL-CHECK.md`.
3. Скачать все источники выборки заново. Что сработало: `curl` с браузерным
   User-Agent, свой грубый HTML→текст на регулярках, `pdftotext -layout` для
   NIST. Из 109 страниц не отдались две: `fastapi.tiangolo.com` (нет
   соединения) и `www.w3.org/TR/CSP3/` (403 на первый UA, отдалась на
   Safari-UA).
4. По каждой теме писать четыре блока **только по открытым страницам**. То, что
   знает модель, но чего нет на странице, помечать `[ПАМЯТЬ МОДЕЛИ]` и считать
   дырой. Эта ловушка сработала минимум один раз (тема 2.2.08): черновик уже
   начал писаться по памяти про Fulcio и Rekor, хотя обе привязанные страницы —
   голые оглавления.
5. Вердикт по теме писать **до** починки и после починки не менять.

### Что найдено и что исправлено

Четыре системные находки — С-1 (навигационные страницы как источники, 83 темы),
С-2 (сдвиг наследования в этапах 7–8, исправлено), С-3 (восемь записей NIST —
карточки публикаций, исправлено), С-4 (аудит ссылался на непривязанные
источники). Подробности — `STATUS.md` и `FINAL-CHECK.md`.

Результат: 28 тем из 67 написались целиком, 24 частично, 14 не написались,
1 не проверена. Тем с дырой — 57 % выборки; в контрольной группе — 25 %.

### Проверки, которые должны оставаться зелёными

```
.venv-tools/bin/python tools/validate.py            # 254 записи, 188/188 тем, 0 ошибок
.venv-tools/bin/python tools/gen_topics.py --check  # topics.yaml соответствует плану
.venv-tools/bin/python tools/gen_sources_md.py      # после любой правки sources.yaml
```

Три предупреждения валидатора намеренные: сирота `cisa-bod-22-01` (отозванная
директива, оставлена как факт) и `[ГЛУБИНА]` на темах `t-0-0-10` и `t-8-0-01`.

**Валидатор не видит целого класса ошибок** — он проверяет, что id существует и
что у темы есть источник, но не то, о том ли этот источник и есть ли на
странице содержание. Обе крупные находки (С-1 и С-2) прошли мимо него.

### Границы, о которых легко забыть

- Лицензии не собирать (решение оператора 2026-08-21, проект приватный).
- Учебный план оператора — только чтение.
- `sources.yaml` правится руками; в `topics.yaml` руками только списки
  `sources`; `sources/*.md` генерируются.
- `audit.yaml` целиком не переписывать: расхождение — это находка, её место в
  отчёте.
- Наружу ничего не публиковать, денег не тратить, за пределами
  `/home/tarchok/bootcamp/AppSec-guidebook` ничего не менять.
- Этап 6 исключён из гайдбука.

---

## Прошлая миссия: `appsec-audit` (завершена)

Для агента, который продолжает миссию `appsec-audit` с нуля контекста.

Идёт **аудит достаточности источников**: по каждой теме проверяется, можно ли
по её источникам написать четыре блока канонического скелета — механика, как
выглядит в коде, как чинится, как проверить фикс. Реестр уже собран прошлой
миссией; здесь его перепроверяют содержательно и добирают недостающее.

**АУДИТ ЗАВЕРШЁН.** Все 188 тем восьми этапов (0, 1, 2, 3, 4, 5, 7, 8)
имеют в `audit.yaml` вердикт `closed`. Этот файл остаётся как инструкция на
случай, если реестр придётся аудировать снова — например, после правки плана
обучения или через несколько месяцев, когда документация снова разъедется.

## Прочитать в этом порядке

1. `~/.supermanager/missions/appsec-audit/dossier.md` — цель, границы и
   заранее принятые решения оператора. Главное оттуда: планка одна для всех
   тем и не поднимается; уровни L1/L2/L3 темам не назначаются; каждый уже
   заведённый источник открывается заново лично, судить по полю `notes`
   нельзя; потолок 1–3 источника на тему, четвёртый — только под незакрытый
   блок и с объяснением в `notes`; пробел, который нечем закрыть, помечается
   и не блокирует миссию.
2. `STATUS.md`, верхняя часть — что уже сделано аудитом и какие решения
   приняты по ходу. Нижняя часть, от «Итог миссии appsec-sources», — история
   сбора реестра; её выводы про среду в силе.
3. `PLAYBOOK.md`, часть 4 — скелет темы, по которому и меряется
   достаточность. Часть 9.3 — схема frontmatter.
4. `audit.yaml` — состояние аудита: id темы, вердикт по каждому из четырёх
   блоков, что добавлено, что отвязано. Схема — в шапке файла.

## Команды

```bash
cd /home/tarchok/bootcamp/AppSec-guidebook
.venv-tools/bin/python tools/validate.py              # схема + ссылки + покрытие
.venv-tools/bin/python tools/validate.py --stage 1    # покрытие одного этапа
.venv-tools/bin/python tools/gen_topics.py --check    # topics.yaml не разошёлся с планом
.venv-tools/bin/python tools/gen_sources_md.py        # перегенерация sources/*.md
```

Если `.venv-tools/` нет: `python3 -m venv .venv-tools && .venv-tools/bin/pip install pyyaml`.

## Цикл аудита одного этапа

1. Выписать темы этапа и их источники из `topics.yaml` (наследуемые от этапа
   и подраздела учитывать — они покрывают все темы внутри).
2. **Открыть каждый источник лично.** Порядок обхода: `WebFetch` → `curl` с
   браузерным User-Agent → зеркало или архивная копия. Спрашивать у страницы
   надо не «о чём она», а конкретно: есть ли примеры кода, есть ли раздел
   про исправление, есть ли процедура проверки, какая дата и статус.
3. Оценить четыре блока скелета: `ok`, `partial` или `none`, и записать, чем
   именно блок закрыт — с номерами разделов, если они есть.
4. Пробел закрывать добором источника, а не переформулировкой. Кандидата
   тоже открывать лично и заводить в `sources.yaml` по схеме из шапки файла:
   обязательны все 11 полей, включая `license` (явное «не указана» — это
   факт) и `checked`.
5. Источник, который к теме привязан, но её не покрывает, — отвязать от темы;
   запись в реестре оставить, она может подойти другой. Предупреждение
   [СИРОТА] в этом случае допустимо.
6. Записать тему в `audit.yaml`. Прогнать валидатор, перегенерировать
   markdown, дописать `STATUS.md`.

Этап закрывается целиком, и только потом начинается следующий: результат по
закрытому этапу сразу годен в работу, даже если миссия остановится на полпути.

Когда пройдены все этапы (0, 1, 2, 3, 4, 5, 7, 8) — в `STATUS.md` ставится
строка `АУДИТ ЗАВЕРШЁН`. Это машинная проверка завершения из `mission.toml`,
другого маркера нет. Раньше времени эту строку не писать.

## Что известно про среду

- `w3.org` и `cve.org` отдают curl'у 403 (Cloudflare, SPA) и открываются
  штатным фетчером `WebFetch`.
- `graphql.org` наоборот: 403 штатному фетчеру, открывается curl'ом с
  браузерным User-Agent. `cisa.gov` — так же.
- `rfc-editor.org` изредка рвёт TLS — помогает `--retry 3
  --retry-all-errors`. Заголовки RFC (дата, статус, STD, Obsoletes) удобнее
  брать из `.txt`-версии: `https://www.rfc-editor.org/rfc/rfcNNNN.txt`.
  Список `Updated by` виден только на `https://www.rfc-editor.org/info/rfcNNNN`.
- `developer.mozilla.org/en-US/observatory/` — JS-приложение: таблицы не
  отдаются ни одним каналом, в `mdn/content` на GitHub этих страниц нет.
- `ptsecurity.com`, `ntia.gov` и домены `*.gov.ru` не проходят проверку
  сертификата. Это отказ среды, а не источника: `needs_manual_check` и дальше.
- Долгий `curl` без `--max-time` может съесть весь таймаут вызова. Ставьте
  `--max-time 20` в циклах проверки доступности.

## Грабли, на которые уже наступили

- **Проверяйте, не отозван и не стабилизирован ли документ — и не наоборот.**
  UTR #36 «Unicode Security Considerations» не поддерживается с 2014 и сам
  отсылает к UTS #39; страница отдаёт 200 и выглядит обычно. Обратная ошибка
  тоже случается: прошлая миссия записала NIST SP 800-162 (Guide to ABAC) как
  «отозванный 2 августа 2019», а на самом деле CSRC пишет «Date Published:
  January 2014 (includes updates as of 08-02-2019)» — это дата обновления.
  Пометки withdrawn нет ни на странице, ни в PDF. Документ живой и заведён
  как `nist-sp-800-162-abac`. Отзыв у NIST выглядит как явная плашка
  WITHDRAWN, а не как дата в скобках.
- **Истёкший черновик IETF выглядит как живой.** `ietf-idempotency-key` —
  Expired Internet-Draft: ревизия -07 от 15 октября 2025, RFC не стал.
  Статус виден на странице datatracker, в самом тексте черновика — нет.
- **Проверяйте не только код ответа, но и адрес после редиректа.** Отозванный
  документ отдаёт 200 и выглядит нормально: CISA BOD 22-01 редиректит на URL
  с суффиксом `-revoked`, а заголовок страницы содержит «(Revoked)». Смотрите
  `curl -sL -o /dev/null -w '%{url_effective}'` и `<title>`. Так же ловится
  переезд страницы SonarQube: старый адрес отдал 200, но привёл на другую
  страницу.
- **Версия спецификации может уйти в Retired.** SLSA v1.1 отдаёт 200 и сама
  пишет «Status: Retired. Version 1.2 is the current version». Проверяйте
  плашку статуса, а не только доступность.
- **Домен документации переезжает целиком.** Все `semgrep.dev/docs/*` теперь
  301 на `docs.semgrep.dev/*`. Если запись ведёт на документацию продукта,
  проверьте `%{url_effective}` — редирект молчалив.
- **Корни документации — это карты, а не содержание.** Bandit, CodeQL, ZAP,
  Sigstore, Trivy: на корневой странице только перечень разделов. Смотрите
  подстраницы, прежде чем писать в `notes`, что источник что-то покрывает.
- **Спецификация редко закрывает блоки 5–7.** RFC 9110 даёт семантику методов,
  но не даёт ни ревью, ни фикса, ни проверки; RFC 9846 в §4.5.1.3 прямо
  выводит проверку сертификата за свои рамки. Проверяйте это на самой
  странице, а не по названию документа.
- **Примечание в реестре — не доказательство.** Пять примечаний из
  семнадцати по этапу 0 расходились со страницей: обещанного содержания там
  не было. Именно это аудит и ищет.
- **Не додумывайте имена cheat sheet'ов OWASP.** Индекс живых страниц:
  `curl -s https://cheatsheetseries.owasp.org/ | grep -o 'cheatsheets/[A-Za-z0-9_.-]*\.html' | sort -u`
  Структуру конкретного cheat sheet'а быстрее всего смотреть по заголовкам:
  `grep -o '<h[23][^>]*>[^<]*'` — так, например, нашёлся раздел «Testing
  Proper Implementation of Security Headers», закрывший блок 7 темы 0.08.
- **Даты обновления есть не везде.** PortSwigger Academy и OWASP Cheat Sheets
  их не показывают. Писать «даты на странице нет» честнее, чем подставлять
  дату проверки.
- **Для PDF проверяйте содержание, а не аннотацию** (`pdftotext` в системе
  есть).
- **Наследование против дубля.** Якорь подраздела ставится в `sources`
  подраздела; повтор того же id в теме валидатор считает ошибкой. А вот один
  и тот же источник у двух разных тем — норма, так стоит `mdn-cors` у 0.06 и
  1.1.09.
- **Тип `standard` валидатор ждёт уровня 2, `spec` — уровня 1.** Спецификация
  от издающего органа (W3C, WHATWG, Unicode, IETF) — это `spec`.

## Чему научил этот аудит

Главный вывод, который стоит перенести в следующую миссию: **расхождение
записи со страницей — это норма, а не исключение.** Из 236 записей реестра,
собранных за один день, за один же день аудита больше двадцати оказались
неточными: отозванные документы, переехавшие страницы, витрины проектов
вместо содержания и примечания, обещающие то, чего на странице нет.

Порядок проверки, который себя оправдал:

1. `curl -sL -o /dev/null -w '%{url_effective}'` — куда на самом деле ведёт
   адрес. Ловит переезды и отзывы (суффикс `-revoked`).
2. `<title>` страницы — ловит пометки вроде «(Revoked)».
3. Плашка статуса в теле — `Retired`, `Expired Internet-Draft`, «написано
   для JDK 8».
4. И только потом — содержание: есть ли то, что обещает примечание реестра.

## Состояние (на конец миссии `appsec-audit`, 2026-08-21 — историческое)

> Актуальные цифры — в верхней части файла и в `STATUS.md`: 301 запись,
> 9 предупреждений валидатора. Ниже — состояние на момент завершения аудита.

`tools/validate.py` зелёный: 254 записи, 188 тем из 188, 0 ошибок. Три
предупреждения, все намеренные: [ГЛУБИНА] по темам 0.10 и 8.1 и [СИРОТА] по
`cisa-bod-22-01` — отозванной директиве, отвязанной от темы 2.5.04, но
оставленной в реестре как исторический документ.

`audit.yaml`: 188 тем (все восемь этапов), все `closed`. Полный разбор
находок — в `STATUS.md`. По этапу 0 добавлено 11
привязок (10 новых записей плюс переиспользованный `rfc7519-jwt`), по этапу 1
— 5 привязок (3 новые записи: `nist-sp-800-162-abac`, `wstg-v42-inpv-09-xpath`,
`cwe-113-crlf`; плюс переиспользованные `rfc7239-forwarded` и
`rfc9110-http-semantics`).

Этап 1 оказался собран заметно плотнее нулевого: из 83 тем правки
потребовались пяти. Причина видна по структуре — у восьми подразделов из
двенадцати есть якоря (страница академии плюс cheat sheet), и они закрывают
темы без собственных источников целиком.

По этапу 2 — 3 привязки (2 новые записи: `cisa-bod-26-04`,
`zap-desktop-authentication`, `spa-textbook` — и одна отвязка). Отдельно
исправлены адреса и статусы девяти записей: пять страниц Semgrep, страница
SonarQube, две версии SLSA и отозванная директива CISA. Этап 2 — самый
подвижный: почти все его источники это документация живых продуктов, и
расхождение записи со страницей здесь норма, а не исключение.

Для ручного закрытия оператором: `pt-application-inspector` — открыть
`ptsecurity.com` с обычной сети, дозаполнить `version_or_date` и `license`,
переключить статус на `ok`. Перепроверено 2026-08-20, среда по-прежнему не
пускает. Покрытие темы 2.1.09 от этого не зависит.

---

## Приёмка первых трёх тем (миссия `appsec-write-review`, 2026-08-22)

Отчёт — `WRITE-REVIEW.md`. Проверялись `t-0-0-01`, `t-0-0-02`, `t-0-0-03` и
отчёт `WRITE-0.md` по чеклисту `PLAYBOOK.md` части 13 и Definition of done 11.2.
Читались сам текст и сами первоисточники, не отчёт автора.

**Вердикт: текст годится как образец при трёх условиях**, снимаемых до письма
следующих восьми тем: снять ложные плюсы в DoD (Ф-07), завести стоп-лист
авторских связок против шаблонности (Ф-08), решить вопрос объёма L2 (Ф-09).

**Сверка фактов.** Открыты и сверены девять первоисточников: RFC 9110, WHATWG
URL Standard, UTS #39 rev. 32, draft-ietf-httpbis-rfc6265bis-22, MDN,
WSTG-CONF-06, ASVS 5.0.0, пять страниц CWE 4.20, статья Claroty. Из примерно
семидесяти проверяемых утверждений неверными оказались два. Все 11 внешних
ссылок открыты вручную — битых нет. Системная находка `FINAL-CHECK.md` С-3
(карточка публикации вместо документа) на этих темах не воспроизводится.

**Исправлено на месте** (git-репозитория в проекте нет, отдельным коммитом
оформить нельзя):
- `http-basics.md` — `Content-Length: 61` при теле в 27 байт → `27`;
- `url-and-encoding.md` — четыре шага были приписаны `skeleton`, тогда как в
  UTS #39 rev. 32 это `internalSkeleton`, а `skeleton(X) = bidiSkeleton(LTR, X)`.

**Что важно для следующей миссии письма:**
1. Норма объёма L2 (800–1200 слов) превышена всеми тремя темами на 26–75 % в
   зависимости от метода счёта. Свод метод счёта не задаёт. Решение за
   оператором: резать объём или поднимать норму.
2. Три темы написаны по одному лексическому шаблону — десять авторских связок
   встречаются ровно по разу в каждой. Стоп-лист нужен до письма, а не после.
3. Снятие поддержки по 5.1 (второй пример с пропусками) не реализовано нигде.
4. Правило двух стеков (часть 4) не выполнено ни в одной теме и не отражено ни
   в DoD, ни в чеклисте части 13 — потеряется молча на остальных 249 темах.
5. Внешние URL продублированы в тексте вопреки 9.1 п. 5, хотя те же адреса
   лежат в `sources.yaml`. Два источника истины уже заведены.
6. Заголовки трёх тем разошлись с планом, и у `t-0-0-01` вместе с заголовком
   ушёл предмет: поля-заголовки как тема практически отсутствуют.
7. CWE-650/172/1004/614/1275 процитированы, но в `sources.yaml` их нет;
   `owasp-asvs-5-document` есть в реестре, но не привязан ни к одной из тем.

**Что подтвердилось как сделанное правильно и менять не надо:** работа с
первоисточниками, маркеры уверенности (точны), честная разметка `[авто]`-пунктов
DoD как «проверить нечем», замер времени (настоящий, из `timing.tsv`), список
мест, где уровень зависит от личного опыта оператора (собран, а не решён за него).

Семь предложений к своду — в `WRITE-REVIEW.md` § 7, они не пересекаются с
одиннадцатью правками из `WRITE-0.md` § 6.
