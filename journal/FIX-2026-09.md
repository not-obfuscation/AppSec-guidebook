# Правки по аудиту 2026-09 — ФИКС-2026-09

Снимок миссии. Дата: 2026-09-04. Повод: решение оператора от 2026-09-03 —
«запускай воркфлоу на фиксы из аудита и найденные при осмотре». Реестр
находок: `journal/AUDIT-2026-09.md` (закоммичен в 692b891).

## Итог одной страницей

Все 18 находок high закрыты, все ~60 medium закрыты или объяснены, low
закрыты по зонам; часть низких стилистических (конвергенции вводных фраз)
разбавлена выборочно — см. «Остаток». Три волны: 25 агентов (контентные
кластеры по файлам + обвязка + сайт + мета + лабы), 5 агентов (реестры,
глоссарий, сквозной стиль, остатки, новые проверки гейта), 2 агента
(добивка хвоста + документирование правил). Итого 32 субагента.

**Гейты на закрытие, всё зелёное** (прогоны 2026-09-04 на финальном
дереве):

| Проверка | Результат |
|---|---|
| `make check` | 0 ошибок, 0 предупреждений, 19 снято исключениями |
| `lint_selftest` | 215/215 (было 210: +5 фикстур на новые правила) |
| `make labs` | 38/38 прогонов, порты свободны |
| `make links` | 0 ошибок, 1 предупреждение (NIST SP двухадресность, известная) |
| `verify_idents.py` | расхождений нет; теперь покрывает и `pilot/semgrep/*.yaml` (20 файлов, 25 правил) |
| `make site` + `check_site.mjs` | 195 страниц (194 + attributions), код 0 |
| `make phone` + `check_phone.mjs` | 11 тем, код 0 |
| `validate.py` | 0 ошибок; предупреждения-фон: ГЛУБИНА ×19, СИРОТА ×2, УРОВЕНЬ ×1 (все обоснованы, ниже) |

## Закрытие находок high (18/18)

1. **A9-S0-01** (cookies, Path/document.cookie) — пример переписан: геттер
   сверяет путь cookie с адресом документа; обход — same-origin кадр на путь
   cookie. Перепроверено эмпирически на chrome-headless-shell 152.
2. **A9-S14-01** (prototype-pollution) — опыт переведён на пару
   `({'__proto__':{}})` vs `JSON.parse(...)`; проверено на Node v26.8.1.
3. **A9-S16-01** (argon2 в Node) — «в обеих стандартных библиотеках
   отсутствует» исправлено: `crypto.argon2()` есть с Node v24.7.0
   (проверено прогоном), в `hashlib` Python 3.14 — нет.
4. **A9-S434-01** (vault-lease-rotation) — механика и самопроверка
   переписаны: аренды отзываются вместе с токеном немедленно (каскад).
5. **A9-X-CONTRA-01/02/07** (DAST-пачка) — числа приведены к артефактам
   `.smgr/`: 10 из 13 (dast-limits), 5 инстансов от 4 правил (zap), строка
   log4j-api возвращена, сумма 47 (severity-assessment).
6. **A9-ASVS-01** (weak-algorithms) — три привязки разведены: 11.3.1
   (дополнения/режимы), 11.4.1/11.4.3 (хеши); frontmatter дополнен.
7. **A9-ASVS-05/13/14/15** (metadata semgrep-правил) — четыре привязки
   исправлены (5.3.2, 9.1.2, 14.1.1, 10.4.4); слепая зона закрыта
   расширением `verify_idents.py` на `pilot/semgrep/*.yaml`.
8. **A9-EXT-SRC-01** (semgrep taint) — тема и реестр обновлены:
   `pattern-sinks` не обязателен с редакции документации 2026-06.
9. **A9-META-NORM-01** — скоуп приведён к факту: 196 в плане, 188 активных,
   8 этапов (PLAYBOOK + SCOPE).
10. **A9-META-NORM-02** — секция «Чем этот гайд не является» + функциональный
    дисклеймер теперь на главной странице сайта (генерируются сборкой).
11. **A9-X-SOURCES-08** — topics.yaml синхронизирован с FM тем: +108 пар
    (90 уникальных id), −11 нецитируемых привязок; `sources/этап-*.md`
    регенерированы и полны; СИРОТА 47 → 5 → 2 (см. «Остаток»).
12. **A9-GLOSS-01** — термин SSRF заведён (defines: ssrf-basics).
13. **A9-GLOSS-02** — канон изменён на «сток» (en: sink); 12 прозаических
    «sink» заменены; HCL-ключ `sink` в vault-integration взят в бэктики.

## Открытые вопросы оператору — статус всех шести

1. Править контент — **выполнено** (эта волна).
2. `pt-application-inspector` — **закрыто**: url на канонический
   `ptsecurity.com/products/ai/`, статус ok, проверено 2026-09-03 (НУЦ
   Минцифры подтверждён). Тема `ru-sast-tools` получила источник в FM и
   сноску; ложный маркер снят.
3. Обвязка — **выполнено целиком**: `verify_idents.py` читает
   `pilot/semgrep/*.yaml`; срез `stoplist.py` починен (общих
   четырёхграмм-служебных 0); предупреждения `validate.py` выведены в
   отчётный слой гейта (`validate-warnings`, без вердикта); новые правила
   гейта: **C-HEAD-L3-SELFCHECK** (шапка L3 без самопроверки) и
   **C-BODY-RETURN** (возврат целит предпосылку) — оба error, оба с
   фикстурами самотеста, задокументированы в SCHEMA.md (52 → 54 правила).
4. Мета-слой — **приведён к факту**: PLAYBOOK (числа, еженедельный CI,
   реальные форматы лаб, часть 14 почищена, рецензент снят, нереализованные
   функции помечены), SCHEMA (C-BODY-IDENT — одна сторона, снимки 188/364/
   37), STYLE, SCOPE, шаблоны (формулировка про блок «Коротко»), INFRA.md,
   workflows (37/20), mkdocs.yml (восемь этапов), docs/numbers.md
   (владелец нормы HMAC, уточнение пробела TTL).
5. Глоссарий — **выполнено**: SSRF + 10 аббревиатур (HMAC, ORM, RBAC, XXE,
   LDAP, SSTI, NVD, CRLF, SAML, JNDI), итого 128 терминов; канон «сток»;
   починены defines (idor/bola/mitm/idempotency/session-fixation), алиас
   «источник» у origin снят, мелочи реестра закрыты. Раскрытия первых
   употреблений добавлены в прозу (13 мест); ORM в H1
   `parameterized-queries` — единственное новое исключение
   (exceptions.yaml, 19 записей, прецедент SAST).
6. topics.yaml ↔ FM — **выполнено** (см. high №11).

## Связанные находки самой волны (вне реестра аудита)

- Сайт: починена деградация сносок `[^N]` (84 сноски на 36 страницах; 40
  определений синтезируются из списка источников при сборке); страница
  `attributions.html` + `LICENSE` в корне (текст CC BY-SA 4.0, код MIT);
  примечание о пропуске этапа 6 на главной; починена mermaid-схема в
  docker-secret-leaks (лексическая ошибка лексера, кавычки на подписях).
- Лабы: 15 README получили секции «Сброс»/«Удаление»; docstring-счётчики
  (quality-gates 8, vault 8); лаба owasp-asvs: 5.2.3 (L2 по факту)
  заменено на 5.2.1 (L1) в key/answers/solution.
- Эмпирические уточнения по ходу: docker-multistage-builds — числа
  179/284 МБ воспроизведены и объяснены (двойной учёт слоёв в content
  store + snapshotter); gosec — 8 записей на 6 местах; trufflehog
  `account=` при Verified:false подтверждён артефактом (гипотеза аудита
  опровергнута, текст не менялся); triage-false-positives про
  deleteGlobalAlerts — утверждение темы подтверждено исходником ZAP v26
  (гипотеза аудита опровергнута).
- Гигиена репозитория: удалён мусорный каталог `--help/` (артefact
  запуска `fetch_idents.py --help`); `fetch_idents.py` получил argparse
  (повтор невозможен); удалён мёртвый `pilot/.venv` (не был ни на что
  подключён).

## Остаток (сознательно не закрыто)

- **validate.py-фон (не роняет гейт, показан в отчётном слое)**: ГЛУБИНА
  ×19 (темы с 4–6 собственными источниками — каждый цитируется, резать
  нечего), СИРОТА ×2 (`cisa-bod-22-01` — сознательно, отозванная директива;
  `semgrep-writing-rules` — цитируется шаблонами templates/tool-L*.md),
  УРОВЕНЬ ×1 (`iana-ipv4-special-registry` — первичный реестр, уровень 1
  оправдан). Из реестра удалены три настоящих осадка: nvd-cve-process,
  spa-textbook, slsa-spec.
- **X-STYLE-05** (конвергенции): «Распространённое представление»
  разбавлено в 14 темах (осталось 31), «Дальше —» в 11 (осталось 47) —
  выборочная дешаблонизация; полная кампания не планировалась аудитом.
- **G-CANON слеп к паре sink/«сток»**: линтер регистрирует каждое
  написание записи как канон; ловить английское «sink» как неканон может
  только правка `glossary_lint.py` (режим «en не законное написание
  прозы»). Отложено: корпус выровнен, правило — при желании отдельно.
- **`.smgr/`**: протухшие status.json (MEM-02/03/04) — внешнее состояние
  супервизора, вне репозитория; не трогали. Расхождение
  `.smgr/appsec-stage2-dast/out/RESULTS.md` («216» против 215 в сыром
  журнале) — артефакт миссии, не правится; контент выровнен по журналу.
- **Не переписываемое по конвенции**: исторические записи HANDOFF/journal
  (MEM-05, X-SOURCES-09/10), датированные снимки в отчётах миссий
  (PAGES.md, PILOT.md, INFRA.md § 6).
- **Кандидаты на будущее** (замечены волной, вне реестра): подтесты WSTG
  (05.6 и т.п.) не выразимы текущим форматом `wstg:`; «три страницы
  разработчиков» во введении ru-sast-tools; повествовательные отсылки к
  удалённым id в notes записей cve-program и slsa-v12-build-track-basics.

ФИКС-2026-09 ЗАВЕРШЁН
