# REORG-2026: реструктуризация репозитория

Дата: 2026-08-30. Фаза 2 плана `PLAN-REORG.md`, решения — в
`docs/adr/0001-repo-layout.md`.

## Что сделано

1. **Единый модуль путей** `tools/paths.py`: все константы путей
   (ROOT, реестры, каталоги) в одном месте; 9 скриптов переведены
   на него. Устранена cwd-зависимость (`mdtext.py`, `linkcheck.py`,
   `lint_selftest.py`, `render_diagrams.py` теперь работают из любого
   каталога — проверено запусками из /tmp). Путь к внешнему учебному
   плану — через `APPSEC_PLAN_PATH` (дефолт прежний; поведение
   «ПРОПУЩЕНО» сохранено). Вывод `make check` после рефакторинга
   побайтово совпал с базовым.
2. **Переезд 41 файла** шестью коммитами чистого `git mv`:
   - `journal/` — 37 снимков миссий: WRITE-*, STAGE0-CLOSE,
     STAGE1-* (14), STAGE2-* (7), STAGE3-* (2), INTROS-* (2),
     FINAL-CHECK, GAP-FIX, WARNINGS, CLEAN-HEADERS, STATUS,
     WRITING-PLAN, SOURCES, AUDIT-2026;
   - `docs/reports/` — 4 живых отчёта: INFRA, PAGES, PILOT,
     PILOT-AUTH.
   История подтверждена: `git log --follow` по переехавшим файлам
   работает (проверено на journal/STAGE1-XSS.md).
3. **Правка ссылок**: HANDOFF.md (120 замен), sources.yaml (17),
   PLAYBOOK/STYLE/SCOPE/PLAN-REORG, mkdocs.yml, Makefile, audit.yaml,
   docstring'и tools/, снимки в journal/ и docs/reports/ — всего
   ~180 замен в 22 файлах. Перевёрстка затронутых абзацев на 79
   колонок с контролем «слова не менялись» (побайтовое равенство
   последовательности слов до/после).
4. **README.md** — карта структуры и команды (файла не было).

## Проверки после каждого шага

- `make check` — 0 ошибок, 0 предупреждений, 6 исключений (после
  каждого коммита; вывод идентичен базовому).
- `make lint-selftest` — 202/202.
- `make site` — 139 страниц, 22 701 внутренняя ссылка, вердикт
  «пройдено»; `make phone` — вердикт «пройдено».
- Контрольный grep по tools/, .github/, mkdocs.yml, Makefile: ссылок
  на старые корневые положения не осталось.
- `make glossary`, `make topics` — без diff (генерируемые файлы
  совпадают).

## Найденное по ходу

- Регрессия шага 2.1: `build_site.py:446` использовал
  `vc.SOURCES`, переименованный в `SOURCES_YAML`; `make check`
  это не ловил (не импортирует build_site в том пути), поймано
  прогоном `make site`. Починено в том же шаге. Урок записан:
  гейт рефакторинга инструментов — `make check && make site &&
  make phone`, а не только check.
- Исторические упоминания старых путей внутри `journal/*.md`
  (снимки ссылаются друг на друга голыми именами) оставлены как есть —
  файлы лежат рядом, ссылки работают.

## Что сознательно не сделано

- Корневые yaml-реестры, `content/stage-N/`, `pilot/` не двигались:
  захардкожены в десятке мест и в данных (`taxonomy.yaml`, `labs.yaml`);
  выигрыш не стоит риска (ADR 0001, пункт 3).
- Числа и устаревшие утверждения в переехавших документах не
  правились: снимки — история; живые (INFRA.md и др.) — обновляются
  в фазе 4 по реестру `journal/AUDIT-2026.md` (слой 6).

РЕСТРУКТУРИЗАЦИЯ СДАНА

---

## Фаза 3: очистка (2026-08-30)

- `.smgr/`: **6,0 ГБ → 131 МБ**. Снесены тяжёлые артефакты завершённых
  миссий (локальные `tools/`, venv'ы, `zaphome`, `nuclei-templates`,
  базы `db-*`); `status.json` всех 40 миссий сохранены. Живые миссии
  (`intros-s1-02` — blocked, `appsec-stage1-toc` — working) не тронуты.
- `__pycache__` удалены из `tools/` и 13 каталогов лаб (16 штук);
  рантайм-логи стендов (`server.log`, `access.log`) убраны (в git
  они и не попадали).
- Сирота `pilot/lab/code.py` удалён (решение оператора).
- Общий стенд `sast-principles/code` ≡ `semgrep-rules/code` —
  сознательное переиспользование, помечено в README обеих лаб.
- `dist/appsec-stage-0.html` пересобран (`make phone`, вердикт
  «пройдено») — был протухшим с 2026-08-24.

ОЧИСТКА ЗАВЕРШЕНА
