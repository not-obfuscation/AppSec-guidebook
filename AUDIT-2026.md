# AUDIT-2026: полномасштабный аудит проекта

Дата: 2026-08-30. Фаза 1 плана `PLAN-REORG.md`. Метод: 8 параллельных
аудиторов по шести слоям, каждая находка проверена фактом (первоисточник
открыт и процитирован, код прогнан, файлы посчитаны). Рабочее дерево
проекта аудитом не изменялось (временные файлы — только в /tmp).

Проверено фактом перед началом: 134 темы (11+83+32+8), 351 источник,
29 лаб, 111 терминов глоссария, `make check` = 0 ошибок / 0
предупреждений / 5 исключений, `lint_selftest` = 202/202, валидатор
реестра = 0 ошибок / 48 предупреждений.

---

## Итог одной страницей

Корпус в хорошей форме: 14 тем фактологической выборки дали **4
фактические ошибки** (все — в паре тем про JWT, одна помечена
«проверено лично» и неверна), 628 листингов синтаксически чисты за
вычетом 16 сознательных фрагментов и **одной усечённой копии
semgrep-правила**, все 29 лаб и 19 semgrep-правил **воспроизводимо
проходят**, мёртвых внешних ссылок в выборке нет, внутренних
противоречий чисел между темами не найдено. Главные проблемы — не в
контенте, а вокруг него: **SCOPE.md противоречит факту публикации**,
INFRA.md и STATUS.md описывают несуществующий мир, глоссарий не
покрывает этапы 2–3 (ASVS упоминается 735 раз без записи), производный
`sources/этап-2.md` отстал от реестра, ~20 открытых долгов миссий
висят без движения.

Сводка находок по слоям: ошибок фактов — 4 (+2 затронутые темы вне
выборки), спорных — 7, устаревших — 1; ошибок кода — 1; лаб упавших —
0; ссылок мёртвых — 0; рассинхрона документации — ~45 мест; открытых
долгов — 20 из 32.

---

## Слой 1. Факты (выборка 14 тем из 134)

Проверены: этап 0 — cookies, security-headers, same-origin-policy,
sessions-vs-tokens; этап 1 — jwt-basics, ssrf-filter-bypass,
role-parameter-tampering, fail-open-vs-fail-closed, path-traversal;
этапы 2–3 — sbom, cve-cvss, dependency-track, quality-gates,
branch-protection. Все проверки — по открытым первоисточникам
(RFC 7519/7518/6797/9110, draft-6265bis-22, HTML Standard, ASVS v5.0.0
по тегу, WSTG, OWASP Cheat Sheets, FIRST CVSS/EPSS, NVD API, KEV JSON,
GitHub Docs) и независимым прогонам (PyJWT 2.13.0, Python 3.14.7).

### Ошибки (требуют правки контента)

| # | Место | Утверждение | Факт |
|---|---|---|---|
| Ф-1 | `jwt-basics.md:27,63–65` + frontmatter; то же `jwt-attacks.md:27,63` | «A02:2025 Cryptographic Failures» | В Top 10:2025 A02 — Security Misconfiguration; Cryptographic Failures — **A04:2025**. Внутреннее противоречие: `xxe.md:55` пишет правильно |
| Ф-2 | `jwt-basics.md:25,359–360,458` | «ASVS v5.0-3.1.1: чувствительные данные в токене сессии не передаются клиенту» | ASVS 5.0-3.1.1 — про документирование браузерных механизмов; такого требования нет. Ближайшее по смыслу — **14.1.1** (данные, «only encoded… such as Base64… plaintext payload inside a JWT») |
| Ф-3 | `jwt-basics.md:26` (+ `jwt-attacks.md:26`) | `wstg: ['WSTG-v42-SESS-10']` | SESS-10 не существует в теге v4.2 (404); страница «Testing JSON Web Tokens» есть только в `latest` |
| Ф-4 | `jwt-basics.md:232–235,561` | «чужой `aud` проходит, пока ожидаемое не объявлено — проверено лично на PyJWT 2.13.0» | Прогон опровергает: токен с `aud` при незаданном `audience` отклоняется (InvalidAudienceError). Про `iss` — верно. Пометка «проверено лично» противоречит факту |

### Спорные (требуют переформулировки)

- С-1 `jwt-basics.md:198–199,381–382` — «энтропия не ниже 256 бит для
  HS256»: RFC 7518 требует **размер ключа** ≥ выхода хеша, Cheat Sheet —
  отдельно «160 бит энтропии»; «256 бит энтропии» никто не требует.
- С-2 `jwt-basics.md:63–64` — CWE-522 для утечки email/скидочного кода:
  CWE-522 строго про учётные данные; ближе CWE-311/200.
- С-3 `ssrf-filter-bypass.md:417–418` — «восемь уязвимостей закрыты»:
  Claroty оговаривает «кроме найденных в неподдерживаемых версиях Flask».
- С-4 `dependency-track.md:76` — «хеши» среди улик Dependency-Check:
  документация прямо: «does not currently use file hashes».
- С-5 `dependency-track.md:114,141` — «на исходниках опознавать нечего»:
  слишком сильно, есть анализаторы lock-файлов.
- С-6 `quality-gates.md:194,513` — «`extra.lines` в CE = requires login»:
  по официальной таблице Semgrep `lines` в CE доступны, недоступен
  `fingerprint`. Замер темы расходится с документацией — перепроверить.
- С-7 `cve-cvss.md:143–145` — «балл EPSS меняется вслед за наблюдаемой
  активностью»: FAQ FIRST — активность влияет на обучение модели,
  не на дневной балл.

### Устарело

- У-1 `dependency-track.md:102,251` — Dependency-Check 12.1.0, хотя
  v13.0.0 вышла 2026-08-03 (до даты ревизии темы).
- У-2 `same-origin-policy.md` (сноски) — дата правки HTML Standard
  «21 августа 2026»; живая страница — 30 августа 2026. Тривиально,
  стандарт живой, тема сама это оговаривает.

### Мелочи

- М-1 `same-origin-policy.md:132–134,748–749` — пример «веб-почта из
  интернета» приписан странице MDN, на которой его нет (он из web.dev).
  Содержательно верно, неверна атрибуция.
- М-2 `security-headers.md:361` — «поле знает только DENY и SAMEORIGIN»:
  есть ещё obsolete `ALLOW-FROM`. Упрощение.
- М-3 `security-headers.md:79,145,225,358` — «Expect-CT помечено
  неиспользуемым»: точнее «Do not use it» / «Deprecated».
- М-4 `same-origin-policy.md:727` — название «§ 9.3.2.1 Security risks»
  не существует (номер верный, название нет).

### Не проверено (честный список)

- «rfc6265bis в очереди RFC Editor» (cookies.md:412–414) — страницы
  очереди недоступны из среды; актуальность -22 подтверждена.
- Все стендовые числа авторов (3330 компонентов в sbom.md; 361 CVE
  в cve-cvss.md; числа стендов fail-open) — выгрузки/скрипты
  в репозиторий не приложены, внешне невоспроизводимы; противоречий
  первоисточникам нет. Артефакты `e01-*.py`…`e04-*.py`, цитируемые
  в «стендовых» темах, в репозитории отсутствуют.
- Node-листинги jwt-basics:304–310 и path-traversal:318–323, поведение
  путей на Windows — темы честно помечают «не воспроизводилось».

### Что подтверждено (для протокола)

Этап 0 — фактологически чист (номера разделов RFC 6797/7519/9110,
draft-6265bis-22, ASVS v5.0.0 сошлись дословно, включая таблицу
префиксов cookie и сценарии SameSite). role-parameter-tampering,
fail-open-vs-fail-closed, path-traversal — чисты полностью (включая
артефакты WSTG-ATHZ-03 и локальные прогоны таблиц путей). Этапы 2–3:
все CVE, баллы CVSS (формулы пересчитаны), EPSS-снимок совпал с живым
API в пределах округления, членство в KEV, коды возврата Semgrep,
механика branch protection — дословно по первоисточникам.

## Слой 2. Код в листингах

628 fenced-блоков: python 133, javascript 54, yaml 35, bash 31,
mermaid 9, http 60, text 281, прочие 19. Прогнаны compile()/ast,
`node --check`, `bash -n`, `yaml.safe_load_all`, `semgrep --validate`.

- **Все самодостаточные блоки (97 python, 14 js, 1 bash по эвристике)
  проходят.** 16 провалов — все сознательные фрагменты (`return` на
  верхнем уровне, срезы), 13 из них уже имеют маркер.
- **К-1 (ошибка): `password-storage.md:470–486`** — листинг подан как
  правило из `pilot/semgrep/password-fast-digest.yaml`, но это усечённая
  копия: нет `message`/`metadata`, `semgrep --validate` по листингу —
  FAIL (реальный файл валиден). Противоречит и `semgrep-rules.md:99–100`
  (message среди обязательных полей). Читатель получит нерабочее правило.
- 3 фрагмента без маркера: `role-parameter-tampering.md:194,213`,
  `ssrf-filter-bypass.md:271` (+2 с косвенными маркерами nosec/nosemgrep:
  `bandit.md:164`, `semgrep-custom-rules.md:158`).
- Предложение (в фазу 4): единый маркер `# ФРАГМЕНТ — …` первой строкой
  блока; чекер обязует компилироваться всё без маркера.

Не проверено: mermaid (9 блоков), text/http/прочие языки, семантика
(воспроизводимость) кода.

## Слой 3. Лабы и semgrep-правила

- **Все 29 лаб прогнаны и проходят** по критерию (tests=0/0, hack=1/0
  или собственные check.py с «зачтено»), включая браузерные (node +
  chrome-headless-shell), zap-scanning, cve-cvss, supply-chain-threats,
  pipeline-*. Даты `checked:` в labs.yaml правдоподобны.
- **Все 19 наборов semgrep-сверки — «ПРАВИЛО СВЕРЕНО»** (rc=0).
- jwt-basics/jwt-attacks/crypto-misuse требуют `.venv-tools` (PyJWT,
  cryptography) — README лаб это честно предписывает; свойство среды,
  не дефект.
- Процессы/порты после прогонов не остались (проверено).
- Дубль `sast-principles/code/` ≡ `semgrep-rules/code/` подтверждён
  побайтово. Стенды developer-communication ≠ zap-scanning — НЕ дубли
  (уточнение к плану).
- **`pilot/semgrep/postmessage-no-origin-check.ts` — не сирота**:
  второй тест правила (check.py:140). Запись П12 в PLAN-REORG.md
  в этой части неверна — исправлено.
- `pilot/lab/code.py` — сирота подтверждён (решение: удалить).
- Не проверено: подлинность сохранённых отчётов ZAP в `runs/`
  (ZAP в среде нет).

## Слой 4. Источники и ссылки

- Мёртвых URL в выборке 26 (каждая 14-я запись) — **ноль**; один
  транзиентный 000 (whatwg, повторы дали 200).
- Не-ok статусов — **1** (`pt-application-inspector`, TLS-отказ среды
  воспроизведён; статус честен). Предпосылка о «4 записях» не
  подтвердилась.
- Политика свежести (fast 30д / medium 180д / stable 365д) формально
  не нарушена, но **«ковёр» дат**: 72 % реестра имеют
  `checked: 2026-08-20` — признак массового проставления, а не
  индивидуальной сверки. Доверие к полю низкое — в фазу 4: протокол
  «checked = реально открыт» и выборочные перепроверки.
- **Дрейф `sources/этап-2.md`**: не регенерировался после трёх коммитов
  в sources.yaml; 10 источников недотянуты (zap-automation,
  zap-alerts-index, zap-automation-env, nuclei-templates-intro полностью
  отсутствуют; у nist-sp-500-268, zap-docs, bandit-docs,
  bandit-test-plugins, defectdojo-docs, defectdojo-dedup недостающие
  повторные включения). Остальные этапы совпадают. Чинится
  `gen_sources_md.py` + гейт `--check` (фаза 4).

## Слой 5. Согласованность, глоссарий, объём

- **Матрица нормативных чисел: жёстких противоречий нет.** Все
  расхождения первоисточников (64 vs 128 бит энтропии сессии, варианты
  KDF) честно оговорены в текстах.
- ГК-1 (серая зона): токен 112–127 бит по ASVS 6.5.2 можно хешировать
  быстрой функцией (password-storage.md:303), но как идентификатор
  сессии он не проходит планку 128 бит (secure-random.md:57) — зазор
  читателю не объяснён.
- ГК-2 (наблюдение): security-headers даёт два «правильных» HSTS
  max-age (год :242 и два года :182) без пояснения выбора.
- ГК-3 (пробел): минимальная длина пароля не названа нигде в корпусе
  (есть только максимум 64 по ASVS 6.2.9).
- **Глоссарий не покрывает этапы 2–3.** Группа `standards`
  (glossary.yaml:47–48) пуста. Кандидаты на добавление (частота по
  корпусу): ASVS 735, CWE 630, WSTG 582, SAST 147, CVE 130, OAuth 119,
  CVSS 115, SBOM 85, IDOR 68, EPSS 64, KEV 49, WAF 38, BOLA 37,
  DAST 31, BFLA 22, OIDC 16, IMDS 15. ASVS/WSTG/CWE используются
  без раскрытия с первой же темы курса (http-basics.md:238,266,270).
  «Триаж», «фаззинг», «пеппер» — не добавлять (общеупотребительны/редки).
- **Объём: нарушений норм нет** (wordcount по 134 темам, exit 0).
  Наблюдение: 23 из 27 тем L1 лежат в пределах 5 % над нижней границей
  (13 — в пределах 1 %): кластеризация «под норму», любая правка,
  удаляющая 3–25 слов, уронит тему в C-VOL-UNDER. Зеркально три темы L2
  в 22 словах от верхней границы.

## Слой 6. Документация и процесс

### Рассинхрон (полный реестр — в журнале миссии, здесь главное)

- **SCOPE.md:16–17,74–75 — действующая «замороженная» норма
  («приватный, наружу не публикуется, CI исключён») противоречит факту
  (публикация с 2026-08-26, pages.yml + links.yml).** Самая тяжёлая
  находка слоя: не число, а конституция проекта, обратная решению
  оператора. Требует правки SCOPE.md отдельным решением.
- INFRA.md — переписывать § 1, § 5–7, § 9 целиком: «гейт красный,
  13 ошибок», «CI-as-a-service нет», «не опубликован», «18 страниц»,
  «47 правил C-*» — всё против факта (0/0/5, 139 страниц, 52 правила,
  CI есть).
- SCHEMA.md — ~18 устаревших мест, все из эпохи «двенадцати тем»;
  двойной дрейф в :630 (четырёхграмм уже 1, не 0).
- STATUS.md — снимок 2026-08-22; долги fastapi/cve.org закрыты,
  ptsecurity и cwe-taxonomy живы; «301 запись» → 351; «УКАЗАТЕЛЬ» 17→15.
- STYLE.md, PLAYBOOK.md, WRITING-PLAN.md, SOURCES.md — устаревшие
  числа и статусы (полный список: STYLE.md:34–37,184–186,374,467–468,
  587–588,610–611,703–707,748–750; PLAYBOOK.md:163,700+,909;
  WRITING-PLAN.md:19,98–99,130–132,40–83; SOURCES.md:3–15,51,297 —
  протокол archived_url противоречит принятому решению).

### Открытые долги (20 из 32 выписанных)

Открыты: Н-11 и У-2 (на них 123 решения трёх этапов), таблица
трудоёмкости 3.4, условно замороженные уровни (8 этапа 1, 8 этапа 2,
3 этапа 3), внешнее ревью L1, слияние 1.12.02→1.8.04, skipped_lines
+ 5 отложенных находок линтера, отчёт INTROS-INTROS_S1_02.md (миссия
остановлена оператором), check_toc.py вне tools/ (три расходящиеся
копии в .smgr), З-06 (поле cicd во frontmatter), З-03 этапа 3,
Н-02 этапа 3 (локальный форж), review_interval у branch-protection,
Н-07 этапа 3, числа «из 196»/«девять подразделов», З-2/З-3 TRIAGE
(L-NBSP/L-BANNED латентные мины), З-1 TRIAGE, ptsecurity, cwe-taxonomy.
Закрыты фактически: вилка L2, Н-01..Н-08 этапа 1 (основная масса),
Н-07 этапа 1, semgrep в .venv-tools (стал оснасткой этапа 2),
fastapi/cve.org, норма L3 против введений.

## Реестр действий (вход для фаз 2–4)

**Правки контента (волна исправлений, сразу после аудита):**
Ф-1…Ф-4 (+jwt-attacks), С-1…С-7, У-1, М-1…М-4, К-1, ГК-1…ГК-3.

**Фаза 4 (укрепление конвейера):**
гейт `--check` для gen_sources_md.py; линтер листингов с маркером
ФРАГМЕНТ; прогон лаб и semgrep-сверки в CI; пополнение глоссария
(17 терминов + группа standards); протокол свежести источников;
timeless-стоп-слова; переписывание INFRA.md и правка SCOPE.md
(решение оператора); сверка чисел SCHEMA/STYLE/PLAYBOOK/STATUS.

**Решения оператора (отдельные):** SCOPE.md ↔ публикация; судьба
SCOPE «заморожен на год»; 20 открытых долгов (по одному решению
на каждый или пакетно).

АУДИТ 2026 ЗАВЕРШЁН
