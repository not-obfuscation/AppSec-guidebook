# Автопубликация сайта на GitHub Pages

Отчёт миссии `appsec-pages`. Что сделано, чем проверено, что осталось за
оператором. Маркер приёмки — последней строкой, и дописывается он только после
того, как публичный адрес проверен запросом, а не предположением.

Статус на 2026-08-26: **гейт секретов пройден, история очищена от личного
адреса, конвейер написан и проверен локально; push отбит защитой GitHub и ждёт
двух действий оператора.** Подробности — в разделе «Что осталось».

## 1. Гейт поиска секретов перед первым push

Репозиторий сделан публичным 2026-08-26, а в `main` накоплено 92 коммита,
не отправленных на origin. Первый push раскрывает всю историю разом, поэтому
до него прогнан поиск и по рабочему дереву, и по всей истории.

Инструменты взяты те же, которыми этап 2 разбирает тему секретов, они уже лежат
на машине: `gitleaks 8.30.1` и `trufflehog 3.97.1`.

| Что просмотрено | Чем | Объём | Находок |
|---|---|---|---|
| Все файлы под контролем версий | `gitleaks dir` | 600 файлов, 9.07 МБ | 14 |
| Вся история, все ссылки | `gitleaks git --log-opts=--all` | 107 коммитов | 14 |
| Вся история, детекторы с провенансом | `trufflehog git` | 107 коммитов | 6 |

Ни одна находка не оказалась настоящим секретом. Все четырнадцать — учебный
материал гайдбука, просмотрены поштучно и без редактирования:

* `AKIA5TQR7WXZ3KLM6NDF` — вымышленный ключ, на котором в темах
  `secret-scanners.md`, `secret-rotation.md` и в отчёте `STAGE2-SECRETS.md`
  показано, как выглядит вывод сканера. Он же нарочно используется в теме как
  пример корректного base32, в отличие от соседнего `AKIA2E0A8F3B244C9986`.
* `wJalrXUtnFEMI/K7MDENG/bPxRfiCYEX` в `content/stage-1/secrets-in-code.md` —
  пример из документации самого AWS.
* `sk_live_51H8kQ2eZvKYlo2C0X9wRzT`, `glpat-STATIC-TOKEN-IN-REPO`,
  `stand-signing-secret-32-bytes-min`, `AT-wiener-7a1c` — придуманные значения
  в лабах `jwt-basics`, `oauth-basics`, `pipeline-security` и в правиле semgrep.
* `postgres://app:hunter2@db:5432`, `postgresql://app:s3cr3t@db:5432`,
  `https://user:pass@example.com:8443` — строки подключения из примеров, с
  паролями-заглушками и доменами `.example` и `.test`.

Отдельно, поверх сканеров, просмотрено то, чего сканеры не ищут, а досье
называет прямо:

* Приватные ключи — нет. Строка `BEGIN PRIVATE KEY` встречается только как
  текст темы «секреты в коде», рассказывающей, что по ней ищут.
* Токен Telegram — нет ни одного совпадения по формату.
* Соседние проекты оператора — `agentrouter` упомянут единственный раз, прозой
  в `STAGE2-SECRETS.md`, в перечислении того, что при работе НЕ использовалось.
  `tatar-keyboard` не упомянут вовсе.
* Адреса электронной почты в содержании — тринадцать штук, все на доменах
  `.example` и `.test`.
* Внутренние адреса — только `127.0.0.1`, `localhost` и лабораторные `10.0.x.x`
  из учебных стендов.
* Файлы `.env`, `.pem`, `.key`, `.netrc`, `.npmrc` — в истории не появлялись ни
  разу, в том числе среди удалённых.
* Двоичные файлы — три `.whl`, содержимое просмотрено: это пакеты-пустышки лабы
  про цепочку поставок, по пять текстовых файлов в каждом.

Лабы с эксплойт-кодом публикации не мешают: это содержание гайдбука.

Отчёты сканеров лежат в `.smgr/appsec-pages/gate/` — каталог супервизора, он
в `.gitignore` и наружу не уезжает.

## 2. Что публикуется

Только собранное содержимое `site/`. Исходники видны в публичном репозитории
сами по себе — это решение оператора, и миссия ни видимость репозитория, ни
состав файлов не меняет. Собранный сайт (~38 МБ) в git не коммитится,
`.gitignore` не тронут.

## 3. Конвейер

Два workflow, оба в `.github/workflows/`.

### `pages.yml` — публикация

Срабатывает на push в `main` и по кнопке. Две job:

* **build** — восстанавливает окружение тем же `tools/setup.sh`, которым оно
  восстанавливается на машине, прогоняет `make check`, собирает `make site`,
  проверяет, что в `site/` есть главная и индекс поиска, и кладёт `site/` в
  артефакт через `actions/upload-pages-artifact@v3`.
* **deploy** — `actions/deploy-pages@v4` в окружении `github-pages`.

Решения, заложенные в файл:

* Прав ровно столько, сколько нужно официальному механизму: `pages: write` и
  `id-token: write` у штатного `GITHUB_TOKEN`. Ни PAT, ни deploy-key, ни одного
  нового секрета репозитория не заведено.
* Ветки `gh-pages` нет и не будет: артефакт публикуется напрямую.
* Пины версий не продублированы в workflow. Они живут в `tools/setup.sh` — в
  одном месте, чтобы им было негде разойтись. Vale 3.18.0, mkdocs-material
  9.7.7 и остальные не тронуты: окружение CI подгоняется под пины, а не наоборот.
* `make check` — гейт: не прошло проверки, не публикуется. Ослаблять проверки
  или дописывать исключения ради зелёного CI миссия не будет.
* Схемы mermaid рисуются в CI по-настоящему: `tools/setup.sh` ставит
  `chrome-headless-shell`, им же работает и проверка собранного сайта в браузере.
* Кэши — по содержимому: pip и Vale по хешу `tools/setup.sh`, пакеты node и
  браузер по хешу `tools/node/package.json`, готовые схемы по хешу текстов тем.
  Изменился пин или текст — ключ другой, кэш пересобирается сам.
* Публикация одна в единицу времени (`concurrency: pages`), начатый деплой не
  отменяется: оборванный деплой оставляет страницу в непонятном состоянии.

### `links.yml` — ссылки, отдельно и без права блокировать

`make links` — единственная проверка проекта, ходящая в сеть. Она зависит от
чужих серверов и флапает: полежал минуту чужой сайт — проверка красная, хотя в
гайдбуке не изменилось ничего. В гейт публикации она не входит, иначе чужой сбой
останавливает публикацию своего. Вынесена в отдельный workflow: раз в неделю по
расписанию и по кнопке. `pages.yml` про него не знает и на него не смотрит.

### Чего в конвейере намеренно нет

* `use_directory_urls: false` оставлено как есть. На нём стоит открытие сайта с
  диска и офлайновый поиск; Pages с таким режимом работают нормально.
* Собранный сайт не коммитится, `.gitignore` не менялся.

## 4. Чем проверено локально

Обе команды гейта прогнаны на машине до того, как попали в workflow:

* `make check` — `ИТОГ: 0 ошибок, 0 предупреждений, 5 снято исключениями —
  пройдено`, код возврата 0.
* `make site` — `ИТОГ: сайт офлайновый, навигация и схемы на месте, ссылки
  живые, поиск находит — пройдено`. Собрано 140 страниц и 10 схем, 38 МБ,
  2 минуты 27 секунд на машине; `site/index.html` и
  `site/search/search_index.json` на месте — их же проверяет отдельный шаг
  workflow, чтобы «зелёно, но пусто» не уехало на страницу.
* Оба файла workflow разобраны парсером YAML.
* Отдельно — восстановление окружения с нуля, как в CI: снесены `node_modules`,
  `build/diagrams` и `site/`, затем `tools/setup.sh` (проходит целиком),
  `tools/setup.sh --check` (код 0), `make check` (0 ошибок) и `make site` (все
  10 схем нарисованы заново, «пройдено»). Три поломки, которые это вскрыло, —
  в § 5.

## 5. Что вскрылось при попытке восстановить окружение с нуля

Пункт был записан как риск, а оказался парой настоящих поломок. Обе не мои и
обе не проявлялись раньше по одной причине: `tools/setup.sh` пропускает
установку пакетов node, если каталог `node_modules` уже есть. На машине он был
всегда. В CI его нет никогда — там каждый прогон начинается с чистого места, и
восстановление окружения с нуля до сих пор просто никто не проверял. Оно не
работало.

**Версии пакетов node не были прибиты.** `tools/node/pnpm-lock.yaml` лежал в
`.gitignore`, поэтому в CI его бы не было и `pnpm install` разрешал бы версии
заново по `^`-диапазонам: чужой минорный релиз `markdownlint-cli2` или
`mermaid-cli` менял бы поведение проверок, не меняя в репозитории ни строчки.
По отдельному разрешению оператора из `.gitignore` убрана ровно одна строка,
лок-файл заведён под контроль версий, а `tools/setup.sh` ставит пакеты с
`--frozen-lockfile`. Остальные строки `.gitignore` не тронуты: `site/`,
`build/`, `dist/` и окружения остаются игнорируемыми, собранный сайт в git
по-прежнему не коммитится.

**`pnpm install` возвращал код 1 на чистой установке.** В
`tools/node/pnpm-workspace.yaml` вместо значения стояла незаполненная заглушка
`puppeteer: set this to true or false`. Пакеты при этом вставали все 270, но
pnpm 10 завершался с `ERR_PNPM_IGNORED_BUILDS`, и `tools/setup.sh` честно
докладывал «pnpm install не прошёл» и выходил с кодом 1 — то есть шаг
восстановления окружения в CI падал бы на ровном месте. Заглушка заменена на
`false`, что совпадает с тем, чего скрипт и хотел: postinstall puppeteer
запрещён, а браузер ставится отдельным явным шагом.

**Бинаря `puppeteer` не появлялось при установке с нуля.** Им `tools/setup.sh`
ставит `chrome-headless-shell`, без которого не рисуются схемы. puppeteer
подтягивался не как зависимость проекта, а как peer-зависимость `mermaid-cli`
через `autoInstallPeers`, и его бинарь в корневой `.bin` не линковался; в старом
`node_modules` он остался от какой-то прежней установки. Теперь puppeteer
записан прямой зависимостью с точной версией `25.8.0` — той же, что и была в
локе. Сверено построчно: в лок-файле изменились только строки самого puppeteer,
версии остальных 270 пакетов не сдвинулись.

После этих трёх правок `tools/setup.sh` на снесённом `node_modules` проходит
целиком и `tools/setup.sh --check` возвращает 0.

## 6. Что осталось

### Вопрос оператору: один личный адрес в истории

Гейт чист по секретам, но нашлось одно personally identifiable: коммит
`d47d5ca` («Семь тем подраздела Cryptographic Failures этапа 1») был подписан
настоящим почтовым адресом оператора. Остальные 106 коммитов подписаны
`agent@localhost`. Push сделал бы адрес публичным необратимо, а исправление —
переписывание истории — прямо в списке границ миссии. По правилу «не
опубликовать лишнего важнее, чем опубликовать быстрее» push был остановлен и
вопрос задан.

**Решение оператора — `rewrite`**, разовое разрешение именно на эту операцию;
на остальное из списка границ оно не распространяется. Сделано: автор и
коммиттер коммита переписаны на `agent@localhost`, `d47d5ca` → `3438719`.
Содержимое коммита совпадает с прежним байт в байт, дата авторства сохранена;
изменились только хеши этого коммита и 32 следующих за ним. `origin/main`
остался предком нового `main`, поэтому push прошёл обычным fast-forward —
force-push не применялся. Проверено после переписи: в `main` не осталось ни
одного коммита с личным адресом.

Единственная ссылка на старый хеш в файлах под контролем версий была в этом
отчёте и в `HANDOFF.md` — обе поправлены. Остальные хеши, упомянутые в отчётах
этапов, старше переписанного коммита и не изменились: проверено перебором всех
хеш-подобных строк в файлах репозитория.

### Push отбит защитой самого GitHub

После переписи push был запущен и не прошёл: его отбила не проверка миссии, а
push protection GitHub. Его сканер опознал как настоящие два вымышленных
значения из темы `content/stage-2/secret-scanners.md` (коммит `b11d2a6`):
ключ `AKIA5TQR7WXZ3KLM6NDF` и пару вида ключ-и-секрет в примере вывода
trufflehog. Это ровно тот материал, на котором тема показывает, как выглядит
вывод сканера, и в ней же этот ключ противопоставлен соседнему
`AKIA2E0A8F3B244C9986` с разбором, почему второй настоящим быть не может.

Забавно, но по делу: гайдбук, объясняющий поиск секретов, споткнулся о
собственные учебные примеры. Ничего наружу при этом не уехало — push отбит
целиком, `origin/main` остался на прежнем коммите.

Все три выхода отсюда лежали за границами миссии, поэтому решал оператор.
**Решение — разблокировать**: оба значения помечены в интерфейсе GitHub как
используемые в примерах. Push protection остаётся включённой и продолжает ловить
настоящие ключи; содержание темы не тронуто, вымышленные значения оставлены как
есть. Полный ответ сервера сохранён в
`.smgr/appsec-pages/gate/push-protection.txt`.

Два отвергнутых варианта записаны здесь потому, что соблазн будет повторяться:
выключить push protection целиком — снять защиту и от настоящих ключей в
репозитории, который учит искать секреты; переписать вымышленные значения — это
правка содержания темы, запрещённая границами, и она ломает предмет темы, где
правдоподобный ключ намеренно противопоставлен невозможному. Если сканер
зацепится за примеры в следующих темах, правильный ход тот же: собрать ссылки
разблокировки в один список и спросить один раз.

### Переключатель, который миссия нажать не может — нажат

`gh` на машине не установлен, настройки репозитория через API миссия не меняет,
поэтому **Settings → Pages → Build and deployment → Source: GitHub Actions**
переключил оператор руками. Без этого `deploy-pages` падает с «Pages site not
found», сколько бы раз ни запускался. Подтверждено: домен
`not-obfuscation.github.io`, Enforce HTTPS включён.

Ожидаемый публичный адрес: `https://not-obfuscation.github.io/AppSec-guidebook/`

### Проверка живого адреса

Пока адрес не отвечает, работа не сдана, даже если workflow зелёный. Проверить
надо: главная открывается, одна тема открывается, глоссарий открывается, поиск
находит слово. Это последний шаг перед маркером приёмки, и до него миссия ещё
не дошла.
