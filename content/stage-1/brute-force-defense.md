---
id: brute-force-defense
plan_id: t-1-6-01
title: Брутфорс, rate limiting и обходы защиты
summary: >
  Где приложение считает неудачные попытки входа, по какому ключу их
  привязывает и какими четырьмя приёмами этот счётчик обходят, не подобрав
  ни одного пароля напрямую.
stage: web-vulns
order: 420
status: published
depth: L2
mode: концепт
time_min: 45
teaches:
  - Назвать три места, где считается неудачная попытка входа, и ключ каждого
  - Отличить блокировку учётной записи от ограничения частоты по адресу
  - Показать на прогоне, чем обходится счётчик, привязанный к адресу или к успеху
  - Собрать проверку так, чтобы блокировку нельзя было навести на чужую учётку
prerequisites: [sessions-vs-tokens]
related: [user-enumeration, credential-stuffing, mfa-bypass, rest-and-graphql]
tags: [auth, logic]
cwe: [CWE-307, CWE-799]
asvs: ['v5.0-6.1.1', 'v5.0-6.3.1', 'v5.0-6.2.4']
wstg: ['WSTG-v42-ATHN-03']
owasp: ['A07:2025']
labs: []
sources: [owasp-cs-authentication, ps-authentication, nist-sp-800-63b, wstg-v42-athn-03-weak-lockout]
reviewed: 2026-09-02
review_interval: 12
---

# Брутфорс, rate limiting и обходы защиты

Уровень **L2** · время 45 мин (теория 27 / задача 12 / самопроверка 6,
оценка)

Что прочитать сначала: `sessions-vs-tokens`.

Защита от перебора стоит и срабатывает: на стенде с порогом в пять
неудач шестая попытка получает отказ 429. Тот же стенд, тот же перебор,
одна добавка — каждая попытка идёт с новым значением заголовка
`X-Forwarded-For`:

```text
а) перебор с одного адреса:          5 отказов, 7 блокировок из 12
б) свой X-Forwarded-For на попытку: 12 отказов, 0 блокировок
```

Двенадцать догадок прошли, счётчик не сработал ни разу. Приложение
считало неудачи по адресу, а адрес в каждой попытке был новый: защита
исправно сторожила дверь, которой никто не пользовался.

Брутфорс — перебор паролей к учётной записи автоматом по словарю, и вся
защита от него сводится к счётчику неудачных попыток. Содержание темы —
два вопроса к этому счётчику: где он стоит и по какому ключу считает. От
ответов зависит, какую атаку он закрывает и какую открывает: счётчик по
учётной записи, например, позволяет запереть снаружи чужую учётку.

Дальше разберём три места, где считают попытки. Потом остальные обходы
из того же прогона: сброс счётчика собственным успешным входом,
распыление одного пароля по многим учёткам, наведённую блокировку. И
починку, при которой ключ устойчив, успех историю не стирает, а
блокировка снимается восстановлением пароля.

## 0. Коротко

Брутфорс — перебор паролей к учётной записи автоматом по словарю. Защита от
него сводится к счётчику неудачных попыток: набралось столько-то — вход
закрывается. Всё содержание темы в том, **где** этот счётчик и **по какому
ключу** он считает. Счётчик по адресу обходится сменой адреса. Счётчик, что
обнуляется при успешном входе, обходится собственным входом атакующего между
попытками. Блокировка по учётной записи, наоборот, даёт новую атаку — навести
её на чужую учётку и запереть человека снаружи.

**Зачем это в работе AppSec-инженера.** «У нас есть rate limiting» — типичный
ответ на вопрос о переборе, и он ничего не говорит. Ревьюер спрашивает три
вещи: где стоит счётчик, по какому ключу считает и что его сбрасывает. Разница
между защитой и её видимостью лежит ровно в этих трёх ответах.

## 1. Цели

После этого раздела вы сможете:

1. Назвать три места, где считается неудачная попытка входа, и ключ каждого.
2. Отличить блокировку учётной записи от ограничения частоты по адресу.
3. Показать на прогоне, чем обходится счётчик по адресу или по успеху.
4. Собрать проверку так, чтобы блокировку нельзя было навести на чужую учётку.

## 3. Механика

**Откуда это взялось.** Пароль — общий секрет, и проверка входа обязана
отвечать «да» или «нет» быстро, иначе ей никто не пользуется. Та же скорость
достаётся автомату: тысяча запросов в минуту угадывает слабый пароль за
разумное время. Единственный рычаг у защиты — сделать так, чтобы попытки
кончались раньше догадок. Отсюда счётчик; отсюда же вопрос, к чему его привязать,
потому что всё, что атакующий меняет между попытками, для привязки не годится.

### Три места и три ключа

Счётчик неудач ставят по одному из трёх ключей, и каждый закрывает свою атаку,
оставляя открытой другую.

- **По адресу источника.** Считаются попытки с одного IP. Закрывает перебор
  одной учётки из одной точки. Не закрывает распределённую атаку: адрес
  атакующий меняет.
- **По учётной записи.** Считаются неудачи по одному логину независимо от
  адреса. Cheat Sheet OWASP советует привязывать счётчик именно к учётной
  записи, а не к адресу. Закрывает распределённый перебор одной учётки. Открывает
  отказ в обслуживании: чужую учётку так запирают снаружи.
- **По паре и глобально.** Считается частота на всём входе разом —
  анти-автоматизация уровня приложения. ASVS v5.0-6.1.1 требует, чтобы
  документация описывала, как настроены rate limiting и адаптивный ответ и как
  они не приводят к злонамеренной блокировке.

Ни один ключ не полон. NIST SP 800-63B-4 § 3.2.2 требует ограничивать подряд
идущие неудачные попытки по одному аутентификатору на учётной записи — не более
ста, с отключением аутентификатора при превышении. Верхнюю границу в сто NIST
называет компромиссом между вероятностью угадать и потребностью в
восстановлении доступа. Cheat Sheet и NIST сходятся в главном: меры ставят
слоями, и лучший отдельный слой — второй фактор, а не счётчик.

### Что обходит счётчик

Прогон на локальном стенде (`.smgr/appsec-stage1-auth/exp/e04-ratelimit.py`,
Python 3.14.7) показывает четыре обхода на одном приложении с порогом в пять
неудач:

```text
а) перебор с одного адреса:                5 отказов, 7 блокировок из 12
б) свой X-Forwarded-For на попытку:       12 отказов,  0 блокировок
в) счётчик обнуляется успехом:            12 отказов,  0 блокировок
г) распыление: один пароль на пять учёток: 2 входа,    0 блокировок
д) законный вход после 5 неудач по чужой учётке: код 429
```

Строка «а» — счётчик работает: на шестой попытке приложение отвечает 429.
Строка «б» — тот же перебор, но каждая попытка приходит с новым значением
заголовка `X-Forwarded-For`, и счётчик по адресу не набирается ни разу.
Строка «в» — атакующий вставляет собственный успешный вход между попытками, и
счётчик, обнуляемый успехом, не доходит до порога. Строка «г» — распыление:
один пароль против пяти учёток, по одной попытке на каждую, — счётчик по
учётной записи не срабатывает, а два входа взяты. Строка «д» — обратная
сторона блокировки по учётке: пять неудач по чужому логину, и его законный
владелец получает 429.

> **Предупреждение.** Заголовок `X-Forwarded-For` ставит клиент. Доверять ему
> можно только после доверенного прокси, который перезаписывает его своим
> значением. Счётчик по «адресу из заголовка» без такого прокси считает по
> значению, которым управляет атакующий, — то есть не считает.

## 5. Как выглядит в коде

Ревьюер ищет ключ счётчика и то, что его сбрасывает.

```python
# УЯЗВИМО — демонстрация, не для продакшена.
def login(request):
    ip = request.headers.get("X-Forwarded-For", request.remote_addr)  # (1)
    if fails[ip] >= LIMIT:
        return deny(429)
    if check_password(request.form["user"], request.form["pw"]):
        fails[ip] = 0                                                  # (2)
        return grant_session(request.form["user"])
    fails[ip] += 1
    return deny(401)
```

1. Ключ — адрес из заголовка, которым управляет клиент. Каждая попытка со
   своим значением обнуляет привязку.
2. Успешный вход обнуляет счётчик. Свой вход между догадками сбрасывает его
   атакующему.

Признаки такие. Ключ счётчика — адрес, тем более из заголовка `X-Forwarded-For`
или `X-Real-IP` без доверенного прокси. Сброс счётчика при успешном входе.
Порог, применённый к учётной записи без защиты от наведённой блокировки.
Проверка кода CAPTCHA по коду ответа, а не по факту решения. Лимит, стоящий в
контроллере входа, но отсутствующий на альтернативном API той же проверки.

## 6. Как чинится

Правило: считать по устойчивому ключу, не сбрасывать счётчик успехом одного
входа и не давать блокировкой запирать чужую учётку.

```python
# Исправлено.
def login(request):
    user = request.form["user"]
    key = ("user", user)                                    # (1)
    if locked(key):                                         # (2)
        return deny(429, "temporarily locked")
    ok = check_password(user, request.form["pw"])
    record_attempt(key, ok)                                 # (3)
    if not ok:
        return deny(401)
    return grant_session(user)
```

1. Ключ — учётная запись, а не адрес. Смена адреса перебор не ускоряет.
2. Блокировка временная, с растущей выдержкой (Cheat Sheet: экспоненциальный
   рост от секунды). Разблокировать умеет и восстановление пароля — тогда
   наведённая блокировка не запирает владельца насовсем.
3. Попытки пишутся всегда, и успех не стирает историю неудач: он лишь пускает
   этот вход.

**Что ставится рядом, а не вместо.** Второй фактор (`mfa-bypass`) — по данным,
которые приводит Cheat Sheet, снимает подавляющее большинство атак на пароль.
Проверка пароля по списку скомпрометированных и топ-3000 частых
(ASVS v5.0-6.2.4) убирает те догадки, которые перебор и пробует первыми.
CAPTCHA Cheat Sheet называет мерой в глубину, а не заменой блокировки. Её
показывают после нескольких неудач, а результат сверяют на сервере.

**Чем платит каждый ключ.** Счётчик по адресу дёшев и не запирает людей, но
слеп к распределённой атаке. Блокировка по учётке ловит распределённый перебор,
но открывает отказ в обслуживании. Глобальная анти-автоматизация видит атаку
целиком, но настраивается тонко и требует документированного порога, иначе сама
становится отказом в обслуживании.

## 9. Ловушка

Частое мнение: раз стоит rate limiting, перебор закрыт.

**Счётчик закрывает ровно ту атаку, под чей ключ он поставлен.** Ограничение
по адресу не мешает распределённому перебору. Блокировка по учётке не мешает
распылению одного пароля по многим учёткам. Ни то ни другое не мешает
подстановке украденных пар, где каждый логин пробуют один раз.

Контрпример из прогона. Приложение с порогом в пять неудач по адресу выдержало
перебор с одной точки (строка «а»). Тот же перебор со сменой `X-Forwarded-For`
на каждую попытку оно пропустило: ноль блокировок на двенадцати неудачах
(строка «б»). Счётчик исправно работал — не по тому ключу.

## 10. Чеклист ревью

1. Verify that счётчик неудачных попыток привязан к устойчивому ключу, а не к
   адресу из заголовка, которым управляет клиент (ASVS v5.0-6.3.1).
2. Verify that успешный вход не обнуляет накопленный счётчик неудач.
3. Verify that блокировка по учётной записи не позволяет запереть чужого
   пользователя снаружи и снимается восстановлением пароля.
4. Verify that проверка пароля идёт против списка топ-3000 частых и списка
   скомпрометированных (ASVS v5.0-6.2.4).
5. Verify that CAPTCHA сверяется на сервере по факту решения, а не по коду
   ответа, и не заменяет блокировку.
6. Verify that тот же лимит стоит на всех путях входа, включая мобильный API и
   альтернативные каналы (WSTG-v42-ATHN-03).
7. Verify that настройки rate limiting и порог блокировки описаны в
   документации приложения (ASVS v5.0-6.1.1).

## 11. Задача

Возьмите вход приложения, с которым работаете, и ответьте письменно на четыре
вопроса. По какому ключу считаются неудачные попытки. Что обнуляет этот
счётчик. Что произойдёт, если каждую попытку слать с новым значением
`X-Forwarded-For`. Что произойдёт с законным владельцем, если пять раз ошибиться
в его логине.

**Наблюдаемый критерий:** четыре ответа со ссылкой на строку кода или настройку
шлюза, где ключ и сброс видны. Плюс вывод, какую из трёх атак — перебор одной
учётки, распыление, наведённую блокировку — эта защита не закрывает.

## 12. Проверь себя

1. По каким трём ключам ставят счётчик неудач и какую атаку каждый оставляет
   открытой?
2. Почему счётчик, обнуляемый успешным входом, обходится, если атакующий может
   войти в свою учётную запись?
3. Чем блокировка по учётной записи опаснее ограничения частоты по адресу?
4. Почему второй фактор Cheat Sheet называет сильнее любого счётчика?
5. Возврат к теме `sessions-vs-tokens`: после успешного входа выдаётся новая
   сессия — почему счётчик неудач нельзя держать в этой сессии?

<details markdown="1">
<summary>Ответы</summary>

1. По адресу источника (оставляет распределённую атаку), по учётной записи
   (оставляет распыление и открывает наведённую блокировку), глобально по
   входу (требует тонкой настройки и документированного порога).
2. Атакующий вставляет собственный успешный вход между догадками; успех
   обнуляет счётчик, и порог никогда не достигается. Прогон: строка «в», ноль
   блокировок на двенадцати неудачах.
3. Она даёт новую атаку — отказ в обслуживании: пять неудач по чужому логину
   запирают его владельца. Ограничение по адресу этого не делает.
4. Счётчик закрывает атаку под свой ключ и слеп к остальным. Второй фактор не
   зависит от того, угадан пароль. По данным Cheat Sheet он снимает
   подавляющее большинство атак на пароль.
5. До входа сессии этого пользователя ещё нет, а неудачные попытки идут именно
   до входа. Счётчик в сессии считал бы только уже вошедших — то есть никого из
   тех, кого надо остановить.

</details>

## 13. Источники

1. OWASP Authentication Cheat Sheet; реестр `owasp-cs-authentication`. Разделы:
   Protect Against Automated Attacks (Login Throttling, Account Lockout,
   CAPTCHA), Authentication and Error Messages.
   <https://cheatsheetseries.owasp.org/cheatsheets/Authentication_Cheat_Sheet.html>
2. PortSwigger Web Security Academy, «Vulnerabilities in password-based login»;
   реестр `ps-authentication`. Разделы: Flawed brute-force protection, Account
   locking, User rate limiting.
   <https://portswigger.net/web-security/authentication>
3. NIST SP 800-63B-4 «Digital Identity Guidelines», Final, июль 2025; реестр
   `nist-sp-800-63b`. Раздел § 3.2.2 Rate Limiting (Throttling): предел в
   сто подряд идущих неудач, растущая выдержка, bot-detection challenge.
   <https://csrc.nist.gov/pubs/sp/800/63/b/4/final>
4. OWASP WSTG-ATHN-03 «Testing for Weak Lock Out Mechanism», WSTG v4.2; реестр
   `wstg-v42-athn-03-weak-lockout`. Разделы: How to Test, Unlock Mechanism,
   Remediation.
   <https://owasp.org/www-project-web-security-testing-guide/v42/4-Web_Application_Security_Testing/04-Authentication_Testing/03-Testing_for_Weak_Lock_Out_Mechanism>

Каркас этапа: `owasp-asvs-5-document` (ASVS v5.0.0), `owasp-wstg-42`,
`owasp-top10-2025`; якоря подраздела: `ps-authentication`,
`owasp-cs-authentication`. Наследуются всеми темами и отдельной строкой не
повторяются.

**Скоропортящийся слой.** Пороги блокировки и выдержки (5–10 попыток, 5–30
минут) — рекомендация Cheat Sheet и WSTG, живых документов без даты; числа
годятся как ориентир, а не норматив. Заголовки `X-Forwarded-For`, `X-Real-IP`
как канал подмены адреса привязаны к настройке конкретного шлюза.

**Маркеры уверенности.** Четыре обхода счётчика и наведённая блокировка —
**проверено лично** 2026-08-24 на Python 3.14.7 локальным стендом
`e04-ratelimit.py` (порог 5, счётчик по адресу и по учётке). Требования к ключу
счётчика, порогам и слоям защиты — **по документации**: Authentication Cheat
Sheet, ASVS v5.0.0, NIST SP 800-63B-4, WSTG v4.2, открыты лично 2026-08-24.
Поведение живого шлюза с доверенным прокси **не воспроизводилось**: стенд
моделирует разбор `X-Forwarded-For` приложением, а не цепочку прокси.
