---
id: csrf-bypass
plan_id: t-1-5-02
title: Обход защиты от CSRF
summary: >
  Пять способов, которыми проверка токена перестаёт проверять, обходы сверки
  заголовков Origin и Referer и то, почему подстановка cookie ломает наивный
  double-submit.
stage: web-vulns
order: 380
status: published
depth: L2
mode: концепт
time_min: 40
teaches:
  - Назвать пять дефектов проверки токена и отличить их по признаку в коде
  - Показать, почему сверка Referer как единственный рубеж обходится
  - Объяснить, чем подстановка cookie ломает наивный double-submit
  - Назвать проверки, которые не обходятся перечисленными приёмами
prerequisites: [csrf-mechanics]
related: [csrf-mechanics, csrf-tokens, samesite-cookies, cookies]
tags: [csrf, session, headers]
cwe: [CWE-352]
asvs: ['v5.0-3.5.1', 'v5.0-3.5.2', 'v5.0-3.3.3']
wstg: ['WSTG-v42-SESS-05']
owasp: ['A01:2025']
labs: []
sources: [ps-csrf, owasp-cs-csrf-prevention]
reviewed: 2026-09-02
review_interval: 24
---

# Обход защиты от CSRF

Уровень **L2** · время 40 мин (теория 24 / задача 10 / самопроверка 6,
оценка)

Что прочитать сначала: `csrf-mechanics`.

Защита стоит: токен в форме есть, он длинный и меняется от входа ко
входу. Прогон на стенде ломает её, не трогая токен. Узел
`blog.bank.example` ставит cookie на весь сайт, и заголовок `Cookie`,
пришедший к кабинету, выглядит так:

```text
csrf=INJECTED; session=S1; csrf=REAL
```

Две cookie с одним именем живут рядом, и первой стоит подставленная:
у неё длиннее `Path`, а разбор по первому совпадению читает значение
атакующего. Приложение сверяет присланный токен с ним же — и запрос
проходит.

Это обход защиты от подделки запроса: мера на месте, а дефект остался,
потому что сломано одно звено проверки.

Дальше разберём пять дефектов проверки токена и признак каждого в коде.
Покажем, почему сверка заголовков `Origin` и `Referer` обходится одной
строкой разметки. И назовём меры, которые этим набором не снимаются.

Механику подделки запроса разбирает тема-предпосылка. Здесь речь о том, что
защита уже стоит, а дефект остался: набор способов конечный и проверяется
чеклистом.

## 0. Коротко

Найти подделку запроса на приложении без защиты вовсе получается редко.
Академия PortSwigger говорит об этом прямо: работа сводится к обходу уже
поставленных мер. Мер этих три — токен, атрибут `SameSite` и сверка
заголовков, — и у каждой свой набор способов не сработать.

Общее у всех способов одно. Защита от подделки отвечает на вопрос «затеяла
ли этот запрос страница приложения». Каждый обход находит путь, на котором
этот вопрос не задан вовсе или задан не так.

**Зачем это в работе AppSec-инженера.** Набор конечный, и потому ревью
изменяющего действия укладывается в семь вопросов чеклиста. Дороже всего
обходятся два дефекта: проверка, которую пропускают при отсутствии значения,
и токен, не привязанный к сессии. Оба выглядят как работающая защита и оба
снимаются одним запросом.

## 1. Цели

После этого раздела вы сможете:

1. Назвать пять дефектов проверки токена и отличить их по признаку в коде.
2. Показать, почему сверка Referer как единственный рубеж обходится.
3. Объяснить, чем подстановка cookie ломает наивный double-submit.
4. Назвать проверки, которые не обходятся перечисленными приёмами.

## 3. Механика

**Откуда это взялось.** Синхронизирующий токен требует места, где приложение
держит выданное значение рядом с сессией. Это состояние на сервере, и оно
мешает: горизонтальное масштабирование, общий кеш сессий, лишняя запись при
каждом входе. Каждая следующая мера — попытка получить тот же результат
дешевле: без хранения, без правки форм, одной строкой в промежуточном слое.
Дешевле получается, а вопрос при этом подменяется. Отсюда и набор обходов:
все они бьют не в токен, а в подмену вопроса.

### Пять дефектов проверки токена

Академия перечисляет их по одному, и различаются они местом, где проверка
теряется.

| Дефект | Что делает атакующий |
|---|---|
| Проверка зависит от метода | шлёт то же действие методом GET |
| Проверка зависит от наличия значения | убирает параметр целиком |
| Ожидаемое значение берут из общего запаса | берёт свой токен из своего кабинета |
| Ожидаемое значение привязано к несессионной cookie | ставит жертве пару cookie и токена |
| Ожидаемым значением служит одноимённая cookie | придумывает значение и ставит его cookie |

Первые два — про поток управления. Проверка стоит внутри ветки, и мимо ветки
есть путь. Убрать параметр целиком, а не подставить в него пустое значение, —
приём, который срабатывает потому, что код часто написан как «если токен
прислан, сверить».

Третий дефект — про то, откуда берётся ожидаемое значение. Приложение держит
общий запас выданных токенов и принимает любой из него. Атакующий входит
своей учётной записью, получает валидный токен и вкладывает его в свою
страницу.

Четвёртый и пятый требуют от атакующего лишнего: возможности поставить жертве
cookie на этот сайт. Академия оговаривает, что ставить её может и другой узел
того же домена — например, витрина на поддомене.

### Что видно в заголовках

Сверку заголовков ставят вместо токена или рядом с ним. Прогон показывает,
что приложение получает при разных способах отправки запроса.

```text
способ отправки          Origin                   Referer
свой POST-формой         bank.example:8131        bank.example:8131/
чужой POST-формой        evil.test:8132           evil.test:8132/
родственный POST-формой  blog.bank.example:8133   blog.bank.example:8133/
чужой GET-навигацией     —                        evil.test:8132/
чужой POST, referrer=no  null                     —
```

Схема `https://` у всех значений опущена, чтобы строки помещались по ширине.

Три строки отсюда — готовый список условий, при которых сверка не сработает.

Поле `Origin` при переходе по ссылке отсутствует вовсе. Проверка «если
`Origin` есть — сверить» пропускает изменяющее действие на методе GET.

Поле `Referer` атакующий убирает со своей стороны: элемент `meta` с политикой
`no-referrer` на его странице снимает поле у исходящего запроса. Академия
называет этот приём и его причину: сверка, написанная как «если заголовок
есть», пропускает запрос без заголовка.

Последняя строка показывает то, чего в академии нет. Убрав `Referer`,
атакующий получает `Origin` со значением `null`. Cheat sheet OWASP описывает
распространённую настройку — принимать запрос, если `Origin` совпал со списком
**или** равен `null`, — и оговаривает, что атакующий этим пользуется. Прогон
показывает, как именно он это делает: одной строкой разметки.

Отдельно стоит запомнить, что кросс-сайтовый `Referer` приходит без пути и
строки запроса — только origin. Сверка, написанная на полный адрес, не
сработает не у атакующего, а у законного пользователя.

### Наивные способы сверки

Академия называет два способа сверки, которые обходятся без всякой правки
заголовков. Первый — проверка на начало строки: адрес
`https://bank.example.evil.test/` начинается с ожидаемого домена. Второй —
проверка на вхождение подстроки: адрес `https://evil.test/?bank.example`
ожидаемый домен содержит.

### Подстановка cookie против наивного double-submit

Наивный вариант double-submit сверяет значение из тела запроса с одноимённой
cookie. Своей записи о выданных токенах приложение не держит.

Прогон показывает, что происходит, когда cookie на весь сайт ставит другой
его узел:

```text
своя cookie на bank.example         session=S1; csrf=REAL
после подстановки с blog.bank       session=S1; csrf=REAL; csrf=INJECTED
заголовок Cookie на /transfer       csrf=INJECTED; session=S1; csrf=REAL
```

Первая строка — обычное состояние. Вторая показывает, что подстановка не
затирает cookie приложения: в браузере живут обе, с одним именем и разной
областью действия. Третья строка объясняет, почему это работает: атакующий
поставил свою cookie с более длинным значением `Path`, и браузер поставил её
в заголовок **первой**. Разбор заголовка, который берёт первое совпадение по
имени, читает значение атакующего.

Cheat sheet называет наивный вариант нерекомендуемым именно из-за подстановки
cookie и перечисляет пути: захват поддомена, уязвимый родственный узел,
подстановка по незашифрованному HTTP. Разбор рекомендуемого варианта — тема
`csrf-tokens`.

Префикс `__Host-` в имени cookie эту подстановку отбивает: браузер не даёт
поставить такую cookie с другого узла и требует `Path=/`. Проверено прогоном:
попытка поставить `__Host-csrf` с родственного узла не изменила ничего.

### Что этими приёмами не обходится

Три меры перечисленным набором не снимаются.

Токен, привязанный к сессии и проверяемый до действия независимо от метода и
наличия значения, закрывает первые пять дефектов целиком. Свой заголовок
запроса кросс-сайтово не отправляется без предварительного разрешения:
проверено прогоном, запрос со своим заголовком до приложения не доходит вовсе.
Поле `Origin` подделать со страницы нельзя — cheat sheet относит его к списку
запрещённых для скрипта.

Единственный общий обход у всего перечисленного — XSS в приложении или на
родственном узле того же сайта. Скрипт в origin приложения достаёт токен со
страницы сам.

## 5. Как выглядит в коде

Ревьюер ищет проверку, у которой есть путь мимо.

```javascript
// УЯЗВИМО — демонстрация, не для продакшена
app.post('/email/change', (req, res) => {
  const sent = req.body.csrf;
  if (sent && sent !== issuedTokens.get(sent)) {        // (1)
    return res.status(403).end('токен не сошёлся');
  }
  changeEmail(req.session.user, req.body.email);        // (2)
  res.end('ок');
});
```

1. Два дефекта в одном условии. Проверка выполняется, только когда значение
   прислано: без параметра ветка не срабатывает. И ожидаемое значение берётся
   из общего запаса выданных, а не из сессии этого пользователя.
2. Действие выполняется после обеих дыр.

Признаки такие. Условие вида «если значение прислано». Поиск ожидаемого
значения в общем словаре, а не в сессии. Проверка внутри обработчика одного
метода, когда действие достижимо и другим. Сверка заголовка, написанная через
`startsWith` или `includes`. Сравнение с cookie того же имени без записи на
стороне сервера.

**Тот же дефект на другом стеке.** В Django общая проверка стоит промежуточным
слоем, и дыра появляется там, где его снимают:

```python
# УЯЗВИМО — демонстрация, не для продакшена
@csrf_exempt
def change_email(request):
    ref = request.headers.get("Referer", "")
    if ref.startswith("https://bank.example"):
        request.user.email = request.POST["email"]
        request.user.save()
    return HttpResponse("ок")
```

Проверка на начало строки пропускает `https://bank.example.evil.test/`.
Порядок условия здесь важнее самой сверки. Записанное так, оно при
отсутствии заголовка действие не выполнит. Обратная запись — «отказать,
если заголовок есть и не совпал» — пропустит запрос без заголовка целиком.

## 6. Как чинится

Правило одно: проверка ставится до действия, выполняется всегда и сравнивает
присланное значение со значением этой сессии.

```javascript
// Исправлено.
import { timingSafeEqual } from 'node:crypto';

app.all('/email/change', (req, res) => {
  if (req.method !== 'POST') return res.status(405).end();     // (1)
  const expected = req.session.csrfToken;                      // (2)
  const sent = String(req.body.csrf ?? '');
  if (!expected || sent.length !== expected.length ||          // (3)
      !timingSafeEqual(Buffer.from(sent), Buffer.from(expected))) {
    return res.status(403).end('токен не сошёлся');
  }
  changeEmail(req.session.user, req.body.email);
  res.end('ок');
});
```

1. Метод ограничен до действия, а не внутри ветки проверки.
2. Ожидаемое значение берётся из сессии, а не из общего запаса.
3. Отсутствие значения — отказ. Сравнение с постоянным временем не даёт
   отличить ответ по длительности.

**Что остаётся за пределами правила.** Сверка заголовков токен не заменяет.
Cheat sheet допускает её как второй рубеж и для запросов до входа, когда
сессии ещё нет. Третий случай описывает требование ASVS v5.0-3.5.2. Оно
касается приложений, которые полагаются на предварительный запрос
совместного использования ресурсов (Cross-Origin Resource Sharing, CORS).
Такое приложение обязано убедиться, что действие нельзя вызвать запросом,
который предварительного не вызывает.

**Что чинится не в коде.** Подстановка cookie с родственного узла закрывается
двумя мерами вне обработчика: префиксом `__Host-` в имени cookie
(ASVS v5.0-3.3.3) и разнесением ненадёжного содержимого на отдельный сайт.
Вторую меру академия называет прямо: пользовательские файлы и витрины держат
не на том сайте, где стоит кабинет.

## 9. Ловушка

Часто слышно: токен в форме есть, значит защита от подделки запроса стоит.
Дальше проверять нечего — остаётся убедиться, что поле не пустое.

**Наличие токена и проверка токена — разные утверждения.** Токен виден в
разметке страницы, а проверка живёт на сервере, и увидеть её можно только по
поведению. Три из пяти дефектов академии выглядят на странице одинаково
хорошо: токен есть, он длинный, он меняется от входа к входу.

Случай, который это ломает. Приложение выдаёт токен, кладёт его в скрытое
поле и сверяет — но с общим запасом выданных, а не с сессией. Атакующий
входит своей учётной записью, забирает свой токен и вкладывает его в форму на
своей странице. Токен настоящий, проверка проходит, действие выполняется от
имени жертвы. Проверяется это одним запросом: подставить в форму токен другой
сессии и посмотреть, откажет ли приложение.

## 10. Чеклист ревью

1. Verify that проверка токена выполняется при любом методе запроса, а не
   только у того, которым форма отправляется штатно (ASVS v5.0-3.5.1).
2. Verify that отсутствие токена в запросе даёт отказ, а не пропуск проверки.
3. Verify that ожидаемое значение берётся из сессии этого пользователя, а не
   из общего запаса выданных.
4. Verify that сверка заголовка `Origin` или `Referer` сравнивает origin
   целиком, а не начало строки и не вхождение подстроки.
5. Verify that отсутствие обоих заголовков и значение `null` в `Origin`
   считаются отказом, а не разрешением.
6. Verify that cookie с токеном несёт префикс `__Host-`, если защита сверяет
   присланное значение с cookie (ASVS v5.0-3.3.3).
7. Verify that ненадёжное содержимое вынесено с того сайта, где стоят
   изменяющие действия (WSTG-v42-SESS-05).

## 11. Задача

Возьмите приложение, с которым работаете, и выберите одно изменяющее
действие. Проведите над ним четыре запроса. Первый — без параметра с
токеном. Второй — методом GET. Третий — с токеном другой сессии. Четвёртый —
без заголовка `Referer`.

**Наблюдаемый критерий:** четыре строки с кодом ответа и с тем, изменилось ли
состояние. Плюс одно предложение о том, какой из четырёх результатов вы бы
завели дефектом и почему остальные три — нет.

## 12. Проверь себя

1. Чем «убрать параметр с токеном» отличается от «прислать пустой токен» и
   почему первое иногда срабатывает?
2. Что нужно атакующему, чтобы обойти наивный double-submit, и почему префикс
   `__Host-` это закрывает?
3. Почему сверка `Referer` как единственный рубеж обходится двумя разными
   способами?
4. Токен в форме есть и он длинный. Какой одиночный запрос покажет, привязан
   ли он к сессии?
5. Возврат к теме `csrf-mechanics`: почему XSS снимает все перечисленные меры
   сразу?

<details markdown="1">
<summary>Ответы</summary>

1. Пустой токен попадает в ветку сравнения и не совпадает с ожидаемым.
   Отсутствующий параметр не попадает в ветку вовсе, если условие написано как
   «если токен прислан, сверить».
2. Возможность поставить жертве cookie на этот сайт — с родственного узла, при
   захвате поддомена или по незашифрованному HTTP. Префикс `__Host-` запрещает
   ставить такую cookie с другого узла и требует `Path=/`.
3. Первый способ — убрать заголовок элементом `meta` с политикой
   `no-referrer`: сверка, написанная как «если заголовок есть», пропустит
   запрос. Второй — подобрать адрес, который проходит наивное сравнение по
   началу строки или по вхождению подстроки.
4. Войти второй учётной записью, забрать её токен и подставить в форму первой.
   Если действие выполняется, токен к сессии не привязан.
5. Скрипт исполняется в origin приложения и читает страницу. Токен он берёт
   оттуда же, откуда его берёт форма, а заголовки браузер ставит сам как для
   своего запроса.

</details>

## 13. Источники

1. PortSwigger Web Security Academy, «Cross-site request forgery (CSRF)» и
   её подстраницы «Bypassing CSRF token validation» и «Bypassing
   Referer-based CSRF defenses»; реестр `ps-csrf`. Разделы: What is a CSRF
   token, Common flaws in CSRF token validation, Validation of Referer depends
   on header being present, Validation of Referer can be circumvented.
   <https://portswigger.net/web-security/csrf>
2. OWASP Cross-Site Request Forgery Prevention Cheat Sheet; реестр
   `owasp-cs-csrf-prevention`. Разделы: Naive Double-Submit Cookie Pattern,
   Using Standard Headers to Verify Origin, Identifying the Target Origin,
   Using Cookies with Host Prefixes to Identify Origins.
   <https://cheatsheetseries.owasp.org/cheatsheets/Cross-Site_Request_Forgery_Prevention_Cheat_Sheet.html>

Каркас этапа: `owasp-asvs-5-document` (ASVS v5.0.0), `owasp-wstg-42`,
`owasp-top10-2025`. Наследуются всеми темами и отдельной строкой не
повторяются.

**Скоропортящийся слой.** Политика браузеров по заголовку `Referer` менялась
и продолжает меняться: объём того, что уходит кросс-сайтово, сокращают.
Страницы академии и cheat sheet — живые площадки без даты публикации.

**Маркеры уверенности.** Заголовки, которые приложение получает при разных
способах отправки, подстановка cookie с родственного узла и порядок значений
в заголовке `Cookie` — **проверено лично** 2026-08-24 в
`HeadlessChrome/152.0.7977.42` на Node.js v26.7.0 на стенде из трёх сайтов по
HTTPS на петле. Проверено: при кросс-сайтовом переходе по ссылке поле `Origin`
отсутствует; элемент `meta` с политикой `no-referrer` убирает `Referer` и
переводит `Origin` в значение `null`; кросс-сайтовый `Referer` приходит без
пути и строки запроса; cookie, поставленная родственным узлом на весь сайт, не
затирает одноимённую cookie приложения, а встаёт в заголовок первой при более
длинном значении `Path`; попытка поставить cookie с префиксом `__Host-` с
родственного узла не проходит; кросс-сайтовый запрос со своим заголовком до
приложения не доходит. Пять дефектов проверки токена и два наивных способа
сверки адреса — **по документации** (Web Security Academy, открыта лично
2026-08-24). Разбор наивного double-submit, приём с принятием значения `null`
и запрещённые для скрипта заголовки — **по документации** (Cross-Site Request
Forgery Prevention Cheat Sheet, открыт лично 2026-08-24). Требования 3.5.1,
3.5.2 и 3.3.3 сверены с текстом ASVS v5.0.0 (открыт лично 2026-08-24).
Захват поддомена и подстановка cookie по незашифрованному HTTP лично **не
воспроизводились**. Поведение Django **не воспроизводилось**: листинг
показывает форму дефекта, а не прогон.
