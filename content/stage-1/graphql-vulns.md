---
id: graphql-vulns
plan_id: t-1-10-04
title: Уязвимости GraphQL
summary: >
  Почему интроспекция раскрывает схему целиком, как псевдонимы упаковывают
  сотни операций в один HTTP-запрос и обходят счётчик частоты, и почему
  авторизацию в GraphQL проверяют на резолвере, а не на эндпоинте.
stage: web-vulns
order: 720
status: draft
depth: L2
mode: концепт
time_min: 40
teaches:
  - Объяснить, что раскрывает интроспекция и почему её отключают в проде
  - Собрать обход счётчика частоты псевдонимами в одном HTTP-запросе
  - Назвать, почему авторизация на эндпоинте в GraphQL недостаточна
  - Свести защиту к резолверной авторизации, лимитам и отключённой интроспекции
prerequisites: [rest-and-graphql]
related: [rest-and-graphql, bola, information-disclosure, csrf-mechanics]
tags: [api, access-control]
cwe: [CWE-863, CWE-200, CWE-799]
asvs: ['v5.0-4.3.1', 'v5.0-4.3.2']
wstg: []
owasp: ['A01:2025', 'A06:2025']
labs: []
sources: [ps-graphql, owasp-cs-graphql, graphql-learn]
reviewed: 2026-08-24
review_interval: 12
---

# Уязвимости GraphQL

Уровень **L2** · время 40 мин (теория 24 / задача 10 / самопроверка 6,
оценка)

Что прочитать сначала: `rest-and-graphql`.

На учебном стенде лимит задан честно: не больше пяти запросов в минуту.
Вот прогон перебора промокодов:

```text
HTTP-запросов отправлено:      1
проверок промокода выполнено:  10
найден рабочий код:            ['SAVE20']
счётчик 'не больше 5 запросов в минуту' увидел: 1 запрос
```

Счётчик не сломался: запрос действительно один. Внутри него — десять
операций, повторённых под разными именами.

Так устроен GraphQL: язык запросов к API, где вместо множества адресов
один эндпоинт, а клиент сам описывает в запросе, какие поля нужны в
ответе. Псевдоним даёт полю своё имя и позволяет повторить одну операцию
в запросе много раз; для счётчика, считающего HTTP-запросы, это единица.
Та же свобода «опиши ответ сам» сдвигает и остальные границы. Схема,
которую в REST никто не отдаёт целиком, здесь отдаётся по встроенному
запросу. А проверка права на входе в единственный эндпоинт не различает
поля.

Дальше разбираем три свойства этой модели и три класса дефектов из них.
Интроспекция отдаёт схему любому спросившему, и у её отключения есть
обходы. Псевдонимы упаковывают перебор в один запрос. А авторизация в
GraphQL живёт не на эндпоинте, а в резолвере поля.

## 0. Коротко

GraphQL — язык запросов к API с одним эндпоинтом. Вместо множества адресов
клиент шлёт на него запрос с деревом нужных полей, а сервер выполняет каждое
поле своим **резолвером** — функцией, которая достаёт его значение. Три свойства
этой модели дают три класса дефектов.

**Интроспекция.** GraphQL самоописываем: по встроенному запросу к полю
`__schema` сервер отдаёт всю схему — типы, поля, аргументы. Инструментам это
нужно, а в проде раскрывает устройство API целиком.

**Псевдонимы и batching.** Обычно два поля с одним именем в ответе несовместимы;
псевдоним даёт полю своё имя и позволяет повторить одну операцию в запросе
много раз. Счётчик частоты, считающий HTTP-запросы, видит один запрос, а внутри
него — сотни операций.

**Авторизация на резолвере.** Проверка прав на входе в эндпоинт ничего не решает:
за одним эндпоинтом лежат все операции. Право надо проверять там, где данные
достаются, — в резолвере поля.

**Зачем это в работе AppSec-инженера.** Дефекты доступа здесь те же, что в REST,
но проверять их надо на другом уровне. Издание OWASP 2025 года относит нарушение
авторизации (CWE-863) и раскрытие сведений (CWE-200) к категории `A01:2025`
Broken Access Control. Обход ограничения частоты (CWE-799) OWASP относит к
`A06:2025` Insecure Design. На ревью тема даёт вопрос к каждому GraphQL-API: отключена ли
интроспекция, где проверяется право и считает ли лимит операции, а не запросы.

## 1. Цели

После этого раздела вы сможете:

1. Объяснить, что раскрывает интроспекция и почему её отключают в проде.
2. Собрать обход счётчика частоты псевдонимами в одном HTTP-запросе.
3. Назвать, почему авторизация на эндпоинте в GraphQL недостаточна.
4. Свести защиту к резолверной авторизации, лимитам и отключённой интроспекции.

## 3. Механика

**Откуда это взялось.** Самоописываемость — часть замысла GraphQL: по схеме
строятся клиенты, редакторы запросов, документация. Интроспекция для этого и
встроена. В разработке она нужна, в проде — отдаёт карту API любому, кто
спросит. Стенд поднимает учебный эндпоинт на несколько десятков строк и
показывает все три свойства.

```text
$ python e02-graphql.py
=== А. интроспекция раскрывает схему ===
  включена:  {'__schema': {'Query': [...], 'User': [...]}}
  отключена: {'errors': ['интроспекция отключена']}
```

С включённой интроспекцией запрос к `__schema` возвращает список типов и полей;
с отключённой — отказ. Даже при отключённой интроспекции схему иногда
восстанавливают по подсказкам. Платформа Apollo в сообщении об ошибке
предлагает похожее правильное имя поля, и по таким подсказкам инструменты
собирают схему по частям. Отдельные обходы отключённой интроспекции — спецсимвол
после `__schema`, который проходит мимо грубого фильтра по ключевому слову, или
запрос через `GET`, если интроспекция закрыта только на `POST`.

### Обход счётчика частоты псевдонимами

Псевдоним задан в GraphQL как способ дать полю своё имя в ответе. Побочное
следствие — одну операцию можно повторить в запросе сколько угодно раз под
разными именами. Счётчик частоты, считающий HTTP-запросы, засчитывает один
запрос.

```text
=== Б. один HTTP-запрос перебирает промокоды псевдонимами ===
  HTTP-запросов отправлено:      1
  проверок промокода выполнено:  10
  найден рабочий код:            ['SAVE20']
  счётчик 'не больше 5 запросов в минуту' увидел: 1 запрос
```

Один запрос проверил десять промокодов и нашёл рабочий; лимит «не больше пяти
запросов в минуту» увидел единицу. Так псевдонимами перебирают коды, пароли,
одноразовые токены — всё, что защищено счётчиком запросов, а не операций.

### Авторизация на резолвере

За одним GraphQL-эндпоинтом лежат все запросы и мутации. Проверка права на входе
в эндпоинт пропустит либо всё, либо ничего. Стенд закрывает поле `email` на
резолвере: чужой пользователь получает имя, но не почту.

```text
=== В. поле email закрыто авторизацией на резолвере ===
  carlos (id=1) читает user(id=2): {'name': 'admin', 'email': '[скрыто…]'}
  сам admin (id=2) читает себя:    {'name': 'admin', 'email': 'root@…'}
```

Право проверяется в резолвере поля, а не на эндпоинте: `email` отдаётся только
владельцу. Это главное, чем контроль доступа в GraphQL отличается от REST, где
у каждой операции свой адрес.

## 5. Как выглядит в коде

Ревьюер ищет резолвер, который достаёт данные, не спросив о праве, и проверку
доступа, привязанную к эндпоинту вместо поля.

```python
# УЯЗВИМО — демонстрация, не для продакшена.
def resolve_user(obj, info, id):
    return db.get_user(id)            # (1) любой аутентифицированный

# Авторизация на входе в эндпоинт, одна на все операции:
def graphql_endpoint(request):
    require_login(request)            # (2) пустил внутрь — пустил ко всему
    return schema.execute(request.body, context=request)
```

1. Резолвер отдаёт пользователя по `id` без проверки, кто спрашивает и на что
   имеет право.
2. Право проверено на эндпоинте: кто прошёл вход, тот дотянулся до всех полей и
   операций схемы.

## 6. Как чинится

- **Авторизация на резолвере.** Право на объект и на поле проверяется в
  резолвере, а не на эндпоинте: там, где данные достаются, известно, кто и что
  запросил.
- **Интроспекция отключается в проде.** Если API не предназначен для сторонних,
  интроспекцию выключают, а подсказки об именах полей в ошибках — тоже
  (ASVS v5.0-4.3.2).
- **Лимит считает операции, а не запросы.** Ограничивают число псевдонимов,
  глубину и сложность запроса, а не только частоту HTTP-запросов
  (ASVS v5.0-4.3.1).
- **Схема не выставляет приватные поля.** Публичная схема проверяется на поля,
  которые не должны быть видны: внутренние идентификаторы, адреса, флаги.
- **Стоимость запроса оценивается.** Тяжёлый вложенный запрос отклоняется по
  оценке стоимости до выполнения — против отказа в обслуживании.

## 9. Ловушка

Ходовое мнение: интроспекция отключена — схема скрыта, а стоит лимит частоты —
перебор закрыт.

**Отключённая интроспекция не прячет схему полностью.** Подсказки об именах
полей в сообщениях об ошибках выдают её по частям, а грубый фильтр по ключевому
слову обходят спецсимволом или сменой метода на `GET`. Схему приходится не
столько прятать, сколько считать известной и защищать доступ к данным, а не к
её описанию.

**Лимит частоты по HTTP-запросам перебор не закрывает.** Псевдонимы упаковывают
сотни операций в один запрос, и счётчик запросов их не видит. Считать надо
операции: число псевдонимов, глубину, стоимость.

## 10. Чеклист ревью

1. Verify that право на объект и на поле проверяется в резолвере, а не только на
   входе в GraphQL-эндпоинт.
2. Verify that интроспекция отключена в проде, если API не предназначен для
   сторонних потребителей.
3. Verify that подсказки об именах полей в сообщениях об ошибках отключены.
4. Verify that лимит ограничивает число псевдонимов, глубину и стоимость
   запроса, а не только частоту HTTP-запросов.
5. Verify that публичная схема не выставляет приватные поля объектов.
6. Verify that тяжёлые вложенные запросы отклоняются по оценке стоимости до
   выполнения.

## 11. Задача

Поднимите или возьмите учебный GraphQL-эндпоинт. Отправьте запрос интроспекции к
`__schema` и посмотрите, отдаётся ли схема. Затем составьте один запрос с
десятком псевдонимов одной операции и посмотрите, засчитает ли лимит частоты его
как один запрос.

**Критерий:** записано, раскрыла ли интроспекция схему и сколько операций
выполнил один запрос с псевдонимами при одном засчитанном запросе. Названо, где
проверяется право на поле — на резолвере или на эндпоинте.

## 12. Проверь себя

1. Что возвращает запрос интроспекции к полю `__schema` и почему это выключают
   в проде?
2. Как псевдонимы позволяют обойти счётчик частоты по HTTP-запросам?
3. Почему проверка права на входе в GraphQL-эндпоинт недостаточна?
4. Чем помогает восстановить схему платформа, отдающая подсказки об именах полей
   в ошибках?
5. Возврат к теме `rest-and-graphql`: чем один эндпоинт GraphQL отличается от
   множества адресов REST для контроля доступа?
6. Возврат к теме `bola`: почему резолвер, отдающий объект по `id` без проверки,
   — та же дыра, что в REST-эндпоинте?

<details>
<summary>Ответы</summary>

1. Всю схему: типы, поля и их аргументы. В проде это раскрывает устройство API
   любому, кто спросит, поэтому интроспекцию отключают, если API не для сторонних.
2. Псевдоним даёт полю своё имя, и одну операцию повторяют в запросе много раз
   под разными именами. Счётчик по HTTP-запросам засчитывает один запрос, а
   внутри выполняются десятки или сотни операций.
3. За одним эндпоинтом лежат все запросы и мутации. Проверка на входе пропускает
   либо ко всему, либо ни к чему; различать право по объекту и полю она не может.
4. Подсказка предлагает похожее правильное имя поля в сообщении об ошибке. По
   таким подсказкам инструмент собирает схему по частям, даже когда интроспекция
   отключена.
5. В REST у каждой операции свой адрес, и право проверяют на маршруте. В GraphQL
   адрес один, поэтому право проверяют не на эндпоинте, а в резолвере поля.
6. Потому что дефект тот же — объект отдаётся без проверки, кто и на что имеет
   право. Меняется лишь место: не маршрут REST, а резолвер поля.

</details>

## 13. Дальше

- `bola` — тот же дефект авторизации объекта; в GraphQL он живёт в резолвере, а
  не в маршруте.
- `information-disclosure` — интроспекция и подсказки об именах полей как
  раскрытие внутреннего устройства API.
- `csrf-mechanics` — GraphQL уязвим к подделке запроса, если принимает операции
  не только JSON-кодированным `POST`.

## 14. Источники

1. PortSwigger Web Security Academy, «GraphQL API vulnerabilities»; реестр
   `ps-graphql`. Разделы: интроспекция и её обходы (спецсимвол после `__schema`,
   смена метода на `GET`), подсказки Apollo, обход ограничения частоты
   псевдонимами, GraphQL-CSRF.
   <https://portswigger.net/web-security/graphql>
2. OWASP, «GraphQL Cheat Sheet»; реестр `owasp-cs-graphql`. Разделы: отключение
   интроспекции в проде, лимит глубины и сложности, лимит на batching,
   авторизация на уровне резолвера, а не эндпоинта.
   <https://cheatsheetseries.owasp.org/cheatsheets/GraphQL_Cheat_Sheet.html>
3. GraphQL Foundation, «Learn GraphQL»; реестр `graphql-learn`. Разделы:
   резолверы и исполнение запроса, псевдонимы полей, интроспекция как встроенная
   функция.
   <https://graphql.org/learn/>

Каркас этапа: `owasp-asvs-5-document` (ASVS v5.0.0), `owasp-wstg-42`,
`owasp-top10-2025`. Наследуются всеми темами и отдельной строкой не
повторяются.

**Скоропортящийся слой.** Обходы отключённой интроспекции и поведение подсказок
— свойство конкретных серверов (Apollo и других) и меняются с их версиями;
модель GraphQL (один эндпоинт, резолверы, псевдонимы, интроспекция) стабильна и
задана спецификацией.

**Маркеры уверенности.** Раскрытие схемы интроспекцией и отказ при отключённой,
перебор десяти промокодов псевдонимами в одном запросе при одном засчитанном и
закрытие поля `email` авторизацией на резолвере — **проверено лично** 2026-08-24
на Python 3.14.7 (стенд `e02-graphql.py`, учебный эндпоинт без внешних пакетов):
запрос к `__schema` возвращает типы и поля; один запрос выполняет десять
проверок кода; чужой пользователь получает имя без почты. Модель GraphQL, роль
резолверов и псевдонимов — **по документации** (страница graphql.org, открыта
лично 2026-08-24). Обходы интроспекции, подсказки Apollo и меры защиты — **по
документации** (страницы PortSwigger и OWASP, открыты лично 2026-08-24).
Поведение живых серверов Apollo на подсказках и обходах фильтра
**не воспроизводилось**: в стенде учебный эндпоинт, а не Apollo.
