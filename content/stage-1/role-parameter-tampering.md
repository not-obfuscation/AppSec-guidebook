---
id: role-parameter-tampering
plan_id: t-1-1-10
title: 'Манипуляции с токенами и параметрами роли'
summary: >
  Что происходит, когда решение о правах принимается по значению, которое
  приложение само отдало клиенту, — в поле формы, в cookie или в токене.
stage: web-vulns
order: 530
status: draft
depth: L2
mode: концепт
time_min: 30
teaches:
  - Найти признак права, который приложение отдаёт клиенту и принимает обратно
  - Назвать, что защищает подпись токена и чего она не защищает
  - Отличить подмену параметра роли от эскалации через отсутствующую проверку
  - Составить ретест подмены роли по трём местам из WSTG
prerequisites: [access-control-models]
related: [access-control-models, privilege-escalation-vertical, client-side-access-checks]
tags: [access-control, jwt]
cwe: [CWE-285, CWE-565]
asvs: ['v5.0-8.3.1', 'v5.0-8.3.2', 'v5.0-9.1.1']
wstg: ['WSTG-v42-ATHZ-03']
owasp: ['A01:2025']
labs: []
sources: [wstg-v42-athz-03-privilege-escalation, ps-access-control, owasp-top10-2025-a01]
reviewed: 2026-08-23
review_interval: 24
---

# Манипуляции с токенами и параметрами роли

**AG-AC-10** · уровень **L2** · время 30 мин (теория 17 / задача 8 /
самопроверка 5, оценка) · reviewed: 2026-08-23 · проверено на: WSTG v4.2,
OWASP Top 10 издания 2025, Web Security Academy (живая платформа),
ASVS v5.0.0, CWE 4.20
пререквизиты: `access-control-models` · маппинг: CWE-285 · CWE-565 ·
ASVS v5.0-8.3.1 · WSTG-v42-ATHZ-03 · A01:2025

Состав блоков — по уровню L2 (`PLAYBOOK.md` 3.1). Блоки 2, 4, 7 и 8 удалены,
блок 11 сведён к задаче без лабы, порядок остальных сохранён. Тяжёлая половина
темы — атаки на подпись и разбор JWT — вынесена в тему 1.7.02 уровнем выше;
здесь только решение о правах, принятое по значению от клиента.

## 0. Коротко

Приложение определяет права при входе и кладёт результат туда, откуда его
читают на каждом запросе. Это скрытое поле формы, cookie, параметр адреса или
утверждение токена. Дальше решение принимается по присланному значению. Атакующему остаётся
это значение изменить. Описание A01:2025 называет приём отдельным пунктом —
манипуляция метаданными. Чинится тем, что права вычисляются на сервере по
личности субъекта, а не читаются из запроса.

**Зачем это в работе AppSec-инженера.** Дефект держится на разнице между «данные
подписаны» и «данные не влияют на решение». Подпись закрывает подделку и не
закрывает устаревание: роль, отозванная минуту назад, в подписанном токене
остаётся действительной. Разговор об этом упирается в требования, которые надо
уметь назвать.

## 1. Цели

После этого раздела вы сможете:

1. Найти признак права, который приложение отдаёт клиенту и принимает обратно.
2. Назвать, что защищает подпись токена и чего она не защищает.
3. Отличить подмену параметра роли от эскалации через отсутствующую проверку.
4. Составить ретест подмены роли по трём местам из WSTG.

## 3. Механика

**Откуда это взялось.** Права дороги в вычислении: их собирают из ролей,
групп и признаков учётной записи, а нужны они на каждом запросе. Первым
решением стало посчитать один раз при входе и носить результат с собой. Пока
результат лежал в серверной сессии, вопрос не стоял: клиент видел только
непрозрачный идентификатор. Дальше состояние ушло к клиенту. Причины назывались разные: снять нагрузку с
хранилища, развести узлы без общей памяти, упростить внешний интерфейс. Вместе
с состоянием туда уехало и решение о правах.

PortSwigger называет три места, куда такой результат кладут: скрытое поле,
cookie и заранее заданный параметр строки запроса. Приложение принимает решение
по присланному значению, и адрес вида `home.jsp?admin=true` работает ровно так,
как выглядит.

### Три места по процедуре WSTG

WSTG-v42-ATHZ-03 разбирает подмену роли по трём точкам, и они покрывают почти
все встречающиеся случаи.

**Параметр группы или объекта в теле запроса.** Запрос `POST /user/viewOrder.jsp`
несёт `groupID=grp001&orderID=0001`. Проверяется, сможет ли пользователь вне
`grp001` подставить чужие значения и получить чужой заказ.

**Скрытое поле профиля в ответе после входа.** Сервер возвращает форму со
скрытым полем `profile=SysAdmin`, а страница отправляет её автоматически. Значение
меняется в перехвате, и вопрос ровно один: станет ли отправитель администратором.

**Значение условия в ответе сервера.** Приложение отдаёт клиенту служебную строку
состояния и доверяет тому, что вернётся. Клиент здесь не сообщает данные, а
управляет ходом обработки.

### Подписанный токен

Токен с подписью снимает подделку: ASVS v5.0-9.1.1 требует проверять подпись или
код аутентичности до того, как содержимое принято. Остаётся вторая половина
задачи — устаревание.

ASVS v5.0-8.3.2 формулирует её как требование: изменения значений, на которых
делается решение о доступе, применяются немедленно. Там, где немедленно не
получается — а самодостаточный токен именно такой случай, — нужны
компенсирующие меры. Это предупреждение о действии субъекта, который уже не
авторизован, и откат такого действия. Отдельно оговорено, что утечку сведений
такая мера не закрывает.

Отсюда практический вывод для ревью: подпись переводит вопрос из «можно ли
подменить роль» в «за сколько минут отзыв прав доходит до решения».

## 5. Как выглядит в коде

Ревьюер ищет чтение признака права **из запроса** — в любой его части.

```python
# УЯЗВИМО — демонстрация, не для продакшена.
role = request.form.get("profile") or request.cookies.get("role")  # (1)
if role == "SysAdmin":                                             # (2)
    return admin_console()
```

1. Признак права читается из тела запроса и из cookie. И то и другое ставит
   отправитель.
2. Решение принимается по прочитанному значению.

Тот же дефект в подписанном виде выглядит иначе и опаснее для ревью, потому что
подпись создаёт впечатление закрытого вопроса:

```python
claims = jwt.decode(token, key, algorithms=["RS256"])   # подпись проверена
if claims["role"] == "admin":                           # (3)
    return admin_console()
```

3. Подделка закрыта, устаревание — нет. Роль внутри токена отражает состояние
   на момент выпуска.

Обёртки, которые выглядят защитой: шифрование значения роли в cookie (закрывает
чтение, а не подмену, если целостность не проверяется) и сверка контрольной
суммы, вычисленной без секрета. Ложное срабатывание: чтение роли из запроса ради
отрисовки интерфейса, когда решение о доступе принято отдельно и на сервере.

## 6. Как чинится

Права вычисляются на сервере по личности субъекта, а из запроса берётся только
она.

```python
user = load_user(session_subject())   # личность — из состояния сессии
if authz.can(user, "admin_console"):  # права — вычислены на сервере
    return admin_console()
```

**Границы применимости.** Приём переносит вычисление прав на каждый запрос, и
это стоит обращения к хранилищу. Там, где отказаться от самодостаточного токена
нельзя, задача не решается, а разменивается. Размен такой: короткий срок жизни и
отдельная проверка чувствительных действий по свежему состоянию. К ним
добавляется одна из мер отзыва — список завершённых токенов, отсечение по дате
выпуска или ротация ключа. Цена известна: возвращается состояние на сервере,
ради отказа от которого модель и выбиралась.

## 9. Ловушка

Распространённое представление: роль внутри подписанного токена подменить
нельзя, поэтому решение по ней безопасно.

**Подпись закрывает подделку, а не устаревание.** Проверка подписи отвечает на
вопрос, кто выпустил токен. О том, верны ли утверждения на момент проверки, она
не говорит ничего.

Контрпример. Сотрудника переводят на другую должность, роль `admin` снимают в
десять утра. Токен с этой ролью выпущен в девять и живёт восемь часов. До шести
вечера подпись верна, утверждение `role: admin` верно с точки зрения проверки, и
административная консоль открывается. Требование v5.0-8.3.2 говорит ровно об
этом случае.

## 10. Чеклист ревью

1. Verify that решение о правах не принимается по значению, пришедшему от
   клиента, — полю формы, cookie, параметру адреса (ASVS v5.0-8.3.1).
2. Verify that признак права, отданный клиенту для отрисовки интерфейса, не
   читается обратно при решении о доступе.
3. Verify that подпись или код аутентичности самодостаточного токена
   проверяются до того, как содержимое принято (ASVS v5.0-9.1.1).
4. Verify that изменение прав применяется немедленно либо заведены
   компенсирующие меры с предупреждением и откатом (ASVS v5.0-8.3.2).
5. Verify that чувствительные действия проверяются по свежему состоянию прав, а
   не по утверждениям токена.
6. Verify that целостность значений в cookie обеспечена ключом сервера, а не
   контрольной суммой без секрета.

## 11. Задача

Соберите список всех мест, где знакомое приложение отдаёт клиенту признак его
прав: поля форм, cookie, параметры адреса, утверждения токена. Для каждого
проверьте, читается ли значение обратно при решении о доступе.

Одна подсказка: начните с ответа точки профиля и со скрытых полей форм на
страницах, где меняются права, — там признак чаще всего и лежит.

**Критерий выполнения** наблюдаемый: таблица «где лежит признак — читается ли
обратно — чем это проверено». Для подписанных токенов дополнительно записано
время между снятием права и отказом в доступе.

## 12. Проверь себя

1. Какие три места называет PortSwigger для хранения признака прав на стороне
   клиента?
2. Что защищает подпись самодостаточного токена и чего она не защищает?
3. Какие три точки подмены роли разбирает WSTG-v42-ATHZ-03?
4. Что требует ASVS v5.0-8.3.2 там, где изменение прав не применяется
   немедленно?
5. Возврат к теме `access-control-models`: почему подмена параметра роли
   опаснее в RBAC, чем в модели, где правило написано про объект?

<details>
<summary>Ответы</summary>

1. Скрытое поле формы, cookie и заранее заданный параметр строки запроса.
2. Закрывает подделку содержимого: изменить утверждение без ключа нельзя. Не
   закрывает устаревание — утверждения отражают состояние на момент выпуска.
3. Параметр группы или объекта в теле запроса; скрытое поле профиля в ответе
   после входа; значение условия в ответе сервера, которому приложение доверяет.
4. Компенсирующие меры: предупреждение о действии субъекта, который больше не
   авторизован, и откат этого действия. Утечку сведений это не закрывает.
5. В RBAC роль — единственный вход в решение, и подмена роли меняет весь набор
   прав сразу. Правило про объект требует ещё и владения объектом, поэтому
   подменённая роль сама по себе доступа не даёт.

</details>

## 13. Дальше

- Тема 1.7.02 — атаки на JWT: подмена алгоритма, путаница ключей, разбор
  подписи.
- `privilege-escalation-vertical` — эскалация там, где проверки нет вовсе.
- `client-side-access-checks` — почему признак прав вообще уезжает клиенту.

## 14. Источники

1. OWASP WSTG-ATHZ-03 «Testing for Privilege Escalation», WSTG v4.2; реестр
   `wstg-v42-athz-03-privilege-escalation`. Разделы: Testing for Role/Privilege
   Manipulation, Manipulation of User Group, Manipulation of User Profile,
   Manipulation of Condition Value.
   <https://owasp.org/www-project-web-security-testing-guide/v42/4-Web_Application_Security_Testing/05-Authorization_Testing/03-Testing_for_Privilege_Escalation>
2. PortSwigger Web Security Academy, «Access control vulnerabilities and
   privilege escalation»; реестр `ps-access-control`. Раздел: Parameter-based
   access control methods.
   <https://portswigger.net/web-security/access-control>
3. OWASP Top 10:2025, «A01:2025 Broken Access Control»; реестр
   `owasp-top10-2025-a01`. Раздел: Description (пункт про манипуляцию
   метаданными).
   <https://owasp.org/Top10/2025/A01_2025-Broken_Access_Control/>

Каркас этапа: `owasp-asvs-5-document` (ASVS v5.0.0), `owasp-wstg-42`,
`owasp-top10-2025`; якорь подраздела: `owasp-cs-authorization`. Наследуются
всеми темами и отдельной строкой не повторяются: третья сноска ведёт на
отдельную запись реестра — главу A01:2025, а не на оглавление издания.

**Скоропортящийся слой.** Формулировка компенсирующих мер в v5.0-8.3.2
привязана к редакции ASVS 5.0.0. Страница Web Security Academy — живая
платформа без даты.

**Маркеры уверенности.** Три точки подмены роли и их разбор — **по
документации** (WSTG-ATHZ-03, открыт лично 2026-08-23). Три места хранения
признака прав на клиенте — **по документации** (Web Security Academy, открыта
лично 2026-08-23). Пункт про манипуляцию метаданными — **по документации**
(страница A01:2025, открыта лично 2026-08-23). Требования 8.3.1, 8.3.2 и 9.1.1
сверены с текстом ASVS v5.0.0 (открыт лично 2026-08-23). Листинги **не
запускались**.
