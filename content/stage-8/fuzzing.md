---
id: fuzzing
plan_id: t-8-0-01
title: 'Фаззинг в web2: go test, Atheris, Hypothesis, AFL++'
summary: >
  Метод, который подаёт программе сгенерированные входы и учится на покрытии
  кода: корпус, мутации, минимизация падающего ввода. На встроенном фаззинге
  Go, Atheris, Hypothesis и AFL++.
stage: optional
order: 10
status: published
depth: L2
mode: концепт
skeleton: инструмент
time_min: 45
teaches:
  - Объяснить, как покрытие кода становится обратной связью фаззера
  - Запустить фаззинг в go test и прочитать находку: вход, минимизация, регресс
  - Отличить фаззинг от property-based тестирования и назвать место каждого
prerequisites: [url-and-encoding, unhandled-exceptions-dos]
related: [url-and-encoding, unhandled-exceptions-dos, go-race-detector]
fixes_in: [unhandled-exceptions-dos]
tags: [fuzzing, tooling, availability]
cwe: [CWE-20]
asvs: []
wstg: []
owasp: []
labs: []
sources: [go-fuzzing, atheris, hypothesis-docs, aflplusplus]
reviewed: 2026-09-02
review_interval: 12
---

# Фаззинг в web2: go test, Atheris, Hypothesis, AFL++

Уровень **L2** · время 45 мин (теория 27 / задача 13 / самопроверка 5,
оценка)

Что прочитать сначала: `url-and-encoding`, `unhandled-exceptions-dos`.

Функция считает последовательности вида `%XX` в строке — ту самую
разметку, которую разбирает тема `url-and-encoding`. Три входа заданы
руками как семена, дальше работает фаззер:

```text
$ go test -fuzz=FuzzCountEncoded -fuzztime=30s .
fuzz: elapsed: 0s, gathering baseline coverage: 5/5 completed,
    now fuzzing with 16 workers
fuzz: minimizing 33-byte failing input file
panic: runtime error: index out of range [1] with length 1
    Failing input written to testdata/fuzz/FuzzCountEncoded/14d3f119d0…
FAIL  fuzzlab  0.077s
```

Прогон занял долю секунды. Найденный вход уместился в один байт: файл в
`testdata` содержит `string("%")` — фаззер сам укоротил находку с 33
байт до одного. А функция упала там, куда руками написанные тесты не
смотрели: на знаке процента в самом конце строки, где следующего байта
нет.

Это и есть фаззинг: программе подают сгенерированные входы, а какие из
них интересны, подсказывает покрытие кода. Ниже — как устроена эта
обратная связь, как читается вывод и чем фаззинг отличается от
property-based тестирования. К чему приводит найденное падение и как оно
чинится — тема `unhandled-exceptions-dos`, сюда это не переезжает.
Теперь поменяйте условие: функция не падает, а молча неверно кодирует
половину входов — что фаззер запишет в `testdata` за час прогона?

## 0. Коротко

Фаззинг (fuzzing) —
автоматическая проверка программы сгенерированными входами: фаззер
мутирует входы из корпуса, следит за покрытием кода и оставляет те, что
открывают новые ветки. Находка — наблюдаемый сбой: паника, переполнение,
зависание; класс дефектов у необработанного входа — CWE-20 (каталог
Common Weakness Enumeration). Встроенный фаззинг Go запускается флагом `-fuzz` у
`go test`. Падающий вход записывается в `testdata` и живёт там как
обычный регрессионный тест. То же в терминах, которыми это обсуждают.

**Зачем это в работе AppSec-инженера.** Парсеры и обработчики ввода —
первое, что получает чужие данные, и их падение это отказ в обслуживании
или хуже. Фаззер доходит до сочетаний, которые ревьюер не перебирает
глазами, а найденный вход остаётся в репозитории как доказательство и
как тест на регресс после починки.

## 1. Цели

После этого раздела вы сможете:

1. Объяснить, как покрытие кода становится обратной связью фаззера.
2. Запустить фаззинг в go test и прочитать находку: вход, минимизация,
   регресс.
3. Отличить фаззинг от property-based тестирования и назвать место
   каждого.

## 3. Механика

**Откуда это взялось.** Входы к тестам писали руками, и проверялись те
случаи, о которых автор догадался. Случайные входы находили падения, но
блуждали вслепую. Поворот сделал AFL: вход, открывший новое ребро
покрытия, попадает в корпус и мутируется дальше — фаззер ползёт по коду,
а не по случайности. Для Go первым такой перенос сделал go-fuzz; после
того как в Go 1.18 фаззинг вошёл в стандартный набор, разработка go-fuzz
остановилась [^1].

В механике три части:

- **корпус** — набор входов: семена от автора плюс накопленные
  интересные;
- **мутатор** — случайные изменения входа из корпуса: биты, вставки,
  обрезки;
- **обратная связь по покрытию** — вход остаётся в корпусе, только если
  открыл покрытие, недоступное имеющимся входам.

Находкой считается наблюдаемый сбой: паника, таймаут (в Go у цели одна
секунда на вызов) или явный провал теста через `t.Error`. Падающий вход
фаззер минимизирует: откусывает куски, пока сбой сохраняется, — отсюда
однобайтовый `%` вместо найденных 33 байт [^1]. Отлаживать такой вход
заметно проще.

Семена из введения — строки с разметкой `%XX`; её норма разобрана в теме
`url-and-encoding`. Фаззер по существу перебирает нарушения этой нормы:
чем точнее семена, тем быстрее он выходит на края.

## 4. Что умеет и чего не умеет

**Что умеет.** Находить сбои на входах, которые не перебрать руками:
граничные длины, пустые строки, обрезанные маркеры, сочетания веток.
Дёшев там, где встроен: в Go это обычный тестовый файл, и ставить ничего
не нужно. Найденное не теряется: вход уходит в корпус и в `testdata`.

**Чего не умеет.** Судить о правильности ответа: фаззер видит сбой, а не
неверный результат. Парсер, который молча неверно кодирует половину
входов, прогон без проверки свойства пройдёт чисто — это и разделяет
фаззинг с property-based тестированием. Не умеет и структурные входы без
подготовки: мутировать JSON побайтно долго, нужны структурные мутаторы
или хороший корпус. И не доказывает отсутствие дефектов: час прогона без
падений говорит только про этот час.

## 5. Как запустить

Фазз-тест живёт в обычном файле `*_test.go` и начинается как тест.
При чтении найдите в нём три части: объявление фазз-цели, семена и
вызов проверяемой функции:

```go
package fuzzlab

import "testing"

func FuzzCountEncoded(f *testing.F) {
  f.Add("%41")
  f.Add("plain")
  f.Add("a%20b")
  f.Fuzz(func(t *testing.T, s string) {
    CountEncoded(s)
  })
}
```

Запуск из каталога пакета — одной командой:

```bash
# Go 1.27: фаззинг с окном в 30 секунд.
go test -fuzz=FuzzCountEncoded -fuzztime=30s .
```

Без ключа `-fuzztime` фаззинг идёт бесконечно, до находки или остановки
руками. Без флага `-fuzz` тот же файл работает обычным тестом по
семенам — этим и пользуется конвейер, где бесконечному прогону не место.

К самой цели два требования. Она должна быть быстрой: фаззер прогоняет
её тысячи раз, и медленная цель съедает окно прогона. И она не должна
держать состояние между вызовами: глобальные переменные связывают входы,
и находка перестаёт воспроизводиться одним сохранённым значением.

## 6. Как читать вывод

Строки из введения читаются по порядку. `gathering baseline coverage` —
прогон семян и корпуса: фаззер меряет, что уже покрыто. `minimizing
33-byte failing input file` — вход найден и укорачивается. `panic: index
out of range` — сама находка: строка из одного знака процента, обращение
за границу. `Failing input written to testdata/…` — вход сохранён, и
следующий `go test` поднимет его без всякого фаззинга.

В ходящем прогоне смотрят ещё на счётчик `new interesting` — темп, с
которым корпус находит новые ветки. Он обычно затухает: корпус покрыл
доступное, и дальнейший прогон дешевле перевести на другую цель. Спад
темпа — штатный конец прогона, а не его поломка.

С найденным падением дальше идут в тему `unhandled-exceptions-dos`: там
разобрано, как необработанный сбой на границе превращается в отказ в
обслуживании и как он чинится. После починки прогон `go test` обязан
пройти по тому же входу — это ретест и регресс одновременно.

## 7. Границы метода

**Граница первая: оракул.** Фаззеру нужен наблюдаемый сбой. Паника
видна, неверный ответ — нет. Закрывается это свойствами: Hypothesis идёт
с другого конца — автор задаёт свойство («раскодирование обратно
кодированию»), а библиотека генерирует входы и минимизирует контрпример
[^3]. Это родственная техника, а не тот же метод: критерий сбоя там
пишет человек.

**Граница вторая: язык и код.** Встроенный фаззинг есть у Go; для Python
тот же принцип даёт Atheris — движок с обратной связью по покрытию поверх
libFuzzer, умеющий и нативные расширения [^2]. Для нативного кода
рабочей лошадкой остаётся AFL++ с инструментированием при компиляции —
от него и пришли идеи, которые затем встроили в Go и Python [^4].

**Граница третья: время.** Фаззинг — это бюджет: чем дольше прогон, тем
глубже входы. В конвейере ему дают короткое окно, а длинные прогоны
выносят в отдельные джобы или во внешние системы вроде OSS-Fuzz.

## 8. Ловушка

Если тесты зелёные и покрытие высокое, что ещё может дать фаззинг?
Вывод напрашивается: раз тесты зелёные и покрытие высокое, фаззинг
ничего не добавит.

**Фаззер меряет покрытие, чтобы искать, а не чтобы отчитаться.** Тест
покрывает ветку одним входом; фаззер идёт по той же ветке тысячами
вариантов и находит сочетание, на котором ветка ломается. Счётчик из
введения — тому пример: три семени проходили, покрытие по ним было
полным, а падающий вход нашёлся за долю секунды.

Встречная крайность — «фаззинг что-то нашёл, значит, под каждый вход
нужна своя проверка». Найденное чинится как один дефект границы, а не
как тысяча условий под мутации: починка из темы
`unhandled-exceptions-dos` закрывает весь класс входов разом.

## 9. Чеклист ревью

1. Verify that у парсеров и обработчиков внешнего ввода есть фазз-цели,
   а не только примеры из тестов (CWE-20).
2. Verify that фаззинг запускается с ограничением по времени, и это окно
   записано в конвейере.
3. Verify that падающие входы сохранены в репозитории и после починки
   проходят обычным прогоном тестов.
4. Verify that у фазз-цели есть наблюдаемый сбой: паника, таймаут или
   явная проверка свойства.
5. Verify that цель быстрая и не держит глобальное состояние между
   вызовами.
6. Verify that найденные сбои заведены как дефекты и закрыты правкой,
   а не отключением цели.

## 10. Задача

Возьмите свой разборщик внешних данных — параметров запроса, поля
заголовка, загружаемого файла — и напишите к нему фазз-цель с двумя-тремя
семенами. Прогоните с окном в одну минуту.

Одна подсказка: начните с функции, которая принимает строку или байты и
не должна падать ни на каком входе.

**Критерий выполнения** наблюдаемый: прогон завершён, и вы можете
назвать, чем он кончился — находкой с сохранённым входом или окном без
падений. Если вход найден, он лежит в `testdata` и воспроизводится
обычным `go test`.

## 11. Проверь себя

1. Из каких трёх частей устроен фаззер с обратной связью и что делает
   каждая?
2. Чем находка фаззера отличается от неверного ответа программы и почему
   это граница метода?
3. Что фаззер делает с падающим входом после минимизации и где этот вход
   живёт дальше?
4. Кто задаёт критерий сбоя в Hypothesis и чем это отличает её от
   фаззера?
5. Возврат к теме `url-and-encoding`: почему разметка `%XX` — благодарный
   объект для фаззинга?
6. Возврат к теме `unhandled-exceptions-dos`: во что превращается
   необработанная паника на границе ввода?

<details markdown="1">
<summary>Ответы</summary>

1. Корпус (семена и накопленные интересные входы), мутатор (случайные
   изменения входа) и обратная связь по покрытию (вход остаётся в
   корпусе, если открыл новое покрытие).
2. Фаззер видит наблюдаемый сбой — панику, таймаут, провал теста, — а
   неверный ответ без сбоя прогоняет молча. Поэтому без проверки
   свойства фаззинг не судит о правильности.
3. Минимизирует до наименьшего входа с тем же сбоем и записывает в
   `testdata`; оттуда вход поднимается обычным `go test` как
   регрессионный тест.
4. В Hypothesis критерий пишет человек в виде свойства; библиотека
   генерирует входы и минимизирует контрпример. У фаззера критерий
   встроенный — сбой программы.
5. Разметка короткая, строгая и разбирается побайтно: граница `%` в
   конце строки, неверные цифры, вложенные кодирования — всё это малые
   мутации от осмысленных семян.
6. В отказ в обслуживании: запрос, на котором код падает без обработки,
   роняет обработчик, а повторение таких запросов держит службу лежачей.

</details>

## 12. Источники

1. The Go Authors, «Go Fuzzing»; реестр `go-fuzzing`. Разделы:
   требования к фазз-тестам, запуск с `-fuzz` и `-fuzztime`, формат
   вывода, минимизация и формат файлов корпуса, Suggestions (быстрая
   цель без глобального состояния), глоссарий.
   <https://go.dev/doc/security/fuzz/>
2. Google, «Atheris — a coverage-guided Python fuzzing engine»; реестр
   `atheris`. Разделы README: устройство движка поверх libFuzzer,
   фаззинг нативных расширений.
   <https://github.com/google/atheris>
3. Hypothesis project, «Hypothesis documentation»; реестр
   `hypothesis-docs`. Разделы: property-based testing как постановка
   задачи, генерация входов и минимизация контрпримера.
   <https://hypothesis.readthedocs.io/en/latest/>
4. AFLplusplus project, «The AFL++ fuzzing framework»; реестр
   `aflplusplus`. Разделы: инструментирование при компиляции, мутации,
   режимы работы.
   <https://aflplus.plus/>

Каркас этапа: у этапа 8 общих унаследованных источников нет; каждая тема
опирается на свои первоисточники.

**Скоропортящийся слой.** Флаги и формат вывода `go test`, список
платформ с покрытием, версии Atheris, Hypothesis и AFL++ — это стареет
первым и при ревизии перечитывается по документации. Механика — корпус,
покрытие как обратная связь, минимизация — стареет медленнее флагов.

**Маркеры уверенности.** Прогон из введения — **проверено лично**
2026-09-02 на go1.27.0 (linux/amd64): заведомо падающий счётчик
последовательностей `%XX`, фаззер нашёл вход за долю секунды,
минимизировал 33-байтовую находку до `string("%")` и записал её в
`testdata`; после починки `go test` проходит по тому же входу.
Таймаут цели в одну секунду, бесконечный прогон без `-fuzztime`, счётчик
`new interesting` и формат корпуса — **по документации** (страница Go
Fuzzing, открыта лично 2026-09-02). Atheris, Hypothesis и AFL++ в этой
работе **не запускались**: их описание — по документации из реестра,
открытой 2026-08-20.

[^1]: The Go Authors, «Go Fuzzing», реестр `go-fuzzing`.
[^2]: Google, «Atheris», реестр `atheris`.
[^3]: Hypothesis project, «Hypothesis documentation», реестр
      `hypothesis-docs`.
[^4]: AFLplusplus project, «The AFL++ fuzzing framework», реестр
      `aflplusplus`.
