---
id: semgrep-custom-rules
plan_id: t-2-1-04
title: Свои правила Semgrep под конкретный стек
summary: >
  Порядок действий от разобранной находки до правила в сборке: разметка
  ожиданий, минимальный образец, прогон по всему репозиторию и сужение по
  измеренному шуму.
stage: appsec-tooling
order: 40
status: draft
depth: L2
mode: рецепт
skeleton: инструмент
time_min: 45
teaches:
  - Превратить разобранную находку в разметку ожиданий для правила
  - Довести правило до схождения разметки прогоном, а не чтением
  - Измерить шум правила на своём репозитории до постановки в сборку
  - Сузить правило подавлением по имени и полем области, а не выключением
prerequisites: [semgrep-rules, sast-false-positives]
related: [semgrep-rules, sast-false-positives, sast-principles, bandit]
fixes_in: [sqli-basics]
tags: [sast, tooling, injection]
cwe: [CWE-89]
asvs: []
wstg: []
owasp: ['A05:2025']
labs: []
sources: [semgrep-testing-rules, semgrep-rule-syntax, semgrep-ignore, semgrep-customize-ci-job]
reviewed: 2026-08-25
review_interval: 12
---

# Свои правила Semgrep под конкретный стек

Уровень **L2** · время 45 мин (теория 25 / задача 15 / самопроверка 5,
оценка)

Что прочитать сначала: `semgrep-rules`, `sast-false-positives`.

Правило написано, и первый же прогон его проверки красный:

```text
0/1: 1 unit tests did not pass:
  x sql-string-built
  missed lines: [], incorrect lines: [19]
```

Строка 19 в разметке — запрос без подстановки вовсе, над ней стоит
пометка «здесь молчать». Правило сработало. `missed lines` — это
пропуски, `incorrect lines` — ложные срабатывания, и оба списка считает
не ревьюер, а сам Semgrep. Считает по файлу разметки ожиданий, лежащему
рядом с правилом: комментарий `ruleid:` обещает срабатывание, `ok:` —
молчание.

Тема начинается там, где кончается язык правил: с разобранной находки,
которую готовые наборы не берут. Обёртка над базой, свой клиент HTTP,
договорённость «через эту функцию не ходим» — всё это готовое правило не
знает. Из такой находки за шесть шагов получается правило в сборке.
Дальше — сам порядок: разметка первой, образец доводится до схождения
прогоном, шум измеряется на всём репозитории до постановки, а снимается
подавлением по имени с причиной. Цена обратного порядка известна:
правило, поставленное без прогона по дереву, выключают целиком на первом
же большом слиянии.

Язык правил разобран в `semgrep-rules`. Здесь порядок действий: что делать с
разобранной находкой, чтобы через час в сборке стояло правило, которое не
выключат через неделю.

## 0. Коротко

Правило пишется в шесть шагов, и первый из них — не образец. Сначала находка
превращается в разметку ожиданий: файл рядом с правилом, где отмечено, где
правило обязано сработать и где обязано промолчать. Дальше образец доводится
до схождения с разметкой, прогоняется по всему репозиторию, сужается по
измеренному шуму и ставится в сборку с сообщением и владельцем.

Порядок именно такой, потому что дорого стоит не написать правило, а оставить
его в сборке. Правило, которое шумит, выключают целиком, и вместе с шумом
уходит весь класс дефектов.

**Зачем это в работе AppSec-инженера.** Готовые наборы правил не знают про
внутренние библиотеки: обёртка над базой, свой клиент HTTP, свой способ
собирать шаблон. Класс дефектов при этом никуда не девается, и единственный,
кто может его описать, — тот, кто разобрал первую находку. Второй повод —
договорённость команды: «через эту функцию не ходим» держится памятью
ревьюера ровно до его отпуска.

## 1. Цели

После этого раздела вы сможете:

1. Превратить разобранную находку в разметку ожиданий для правила.
2. Довести правило до схождения разметки прогоном, а не чтением.
3. Измерить шум правила на своём репозитории до постановки в сборку.
4. Сузить правило подавлением по имени и полем области, а не выключением.

## 3. Механика

**Откуда это взялось.** Раньше договорённости про опасные вызовы жили в
документе для ревью. Ломалось это на масштабе: документ читают при выходе на
работу, а код пишут каждый день. Правило переводит договорённость в проверку,
которая срабатывает в момент правки.

Механики тут ровно столько, сколько нужно, чтобы понять вывод. Образец
сопоставляется с деревом разбора, метапеременная совпадает с любым выражением,
режим метки ведёт значение от источника к стоку. Устройство всего этого
разобрано в `semgrep-rules`, а разбор самого дефекта — в `sqli-basics`; сюда
ни то ни другое не переписывается.

## 4. Что умеет и чего не умеет

Свой набор правил умеет закрывать то, чего нет в готовых: внутренние обёртки,
собственные имена функций, договорённости команды. Умеет он и обратное —
сужать чужое правило до своего стека полем области и подавлением по имени.

Чего свой набор не умеет: заменить готовые. Правило под свою обёртку знает
одну функцию и не знает ста библиотечных вызовов того же класса. Свои правила
живут рядом с набором из реестра, а не вместо него.

## 5. Как запустить

Шесть шагов на разобранной находке. Разбор находки уже сделан — иначе писать
нечего.

```bash
# Semgrep 1.174.0. Шаг 1: разметка рядом с правилом.
# rules/sql.yaml — правило, rules/sql.py — разметка ожиданий.
```

В разметку кладутся два случая срабатывания и два случая молчания. Аннотация
ставится комментарием над строкой: `ruleid: <id>` там, где правило обязано
сработать, `ok: <id>` там, где обязано промолчать [^1].

```python
def get_order_safe(conn, order_id):
    # ok: sql-string-built
    return conn.execute("SELECT id FROM orders WHERE id = ?", (order_id,))
```

```bash
# Шаг 2 и 3: минимальный образец и прогон разметки до схождения.
semgrep --test --metrics=off rules/

# Шаг 4: прогон по всему репозиторию, а не по стенду.
semgrep --config=rules/sql.yaml --metrics=off --no-git-ignore .
```

Шаг 5 — сужение по измеренному шуму. Три средства, от узкого к широкому:
комментарий `nosemgrep: <id>` над строкой, поле `paths` в самом правиле и файл
`.semgrepignore` для каталогов [^2]. Шаг 6 — сообщение, `metadata` с классом
дефекта и владельцем, и постановка в сборку.

## 6. Как читать вывод

Вывод прогона разметки читается первым, и в нём интересны две строки.

```text
0/1: 1 unit tests did not pass:
  x sql-string-built
  missed lines: [], incorrect lines: [19]
```

`missed lines` — правило промолчало там, где размечено `ruleid:`: пропуск.
`incorrect lines` — сработало там, где размечено `ok:`: ложное срабатывание.
В этом прогоне строка 19 — запрос без подстановки вовсе, и правило сработало
на нём. После вычитания такого вызова из стока разметка сходится:

```text
1/1: ✓ All tests passed
```

Дальше читается прогон по репозиторию. На стенде из шести файлов правило дало
три находки: два подставленных дефекта и одно срабатывание на имени таблицы,
проверенном по замкнутому множеству. На семнадцати файлах инструментария этого
проекта — ноль находок.

Ложное срабатывание закрывается точечно, вместе с причиной в самом
комментарии:

```python
# ФРАГМЕНТ — срез, самостоятельно не компилируется
    # nosemgrep: sql-string-built -- имя таблицы сверено с TABLES выше
    return conn.execute(f"SELECT count(*) FROM {table}").fetchone()[0]
```

После этого прогон даёт две находки вместо трёх, и обе — настоящие дефекты.
Что с ними делать, разобрано в теме `sqli-basics`.

## 7. Границы метода

**Граница первая: правило не отличит намерение.** Всё, что делает похожий код
законным, лежит вне текста выражения, и закрывается это подавлением с
причиной, а не образцом.

**Граница вторая: шум меряется на своём репозитории.** Число, полученное на
шести файлах стенда, о монолите не говорит ничего. Прогон по всему дереву —
обязательный шаг, а не проверка впрок.

**Цена.** Первая версия правила — минуты. Версия, пережившая прогон по
репозиторию и сужение, — часы, и большая часть уходит на разбор находок, а не
на образец. Прогон правила на шести файлах — около двух секунд, на пятидесяти
шести — около четырёх, оценка по одному прогону.

## 8. Ловушка

Распространённое мнение: чтобы правило не мешало, его прогоняют только на
изменённых файлах, и тогда старый код шума не даёт.

**Прогон только по изменённому — это про порядок разбора, а не про шум.**
Средство для этого есть: показывать находки относительно магистральной
ветки [^3]. Но старые находки при этом не исчезают, а откладываются, и правило
с высокой долей шума даст ту же долю на первом же большом изменении.

Контрпример: правило поставлено в сборку без прогона по всему дереву, потому
что «в новых файлах его не будет». Первое же слияние крупной ветки приносит
сорок срабатываний, разобрать их некому, и правило выключают целиком — вместе
с двумя настоящими находками, которые в этих сорока были.

## 9. Чеклист ревью

1. Verify that у правила есть файл разметки и прогон `--test` проходит.
2. Verify that в разметке есть случаи `ok:`, а не только `ruleid:`.
3. Verify that правило прогнано по всему дереву, и число находок записано.
4. Verify that каждое подавление названо по идентификатору правила и снабжено
   причиной в том же комментарии.
5. Verify that сообщение правила говорит, что не так и что делать, а не
   называет класс дефекта.
6. Verify that в `metadata` стоит владелец правила: у выключения должен быть
   адресат.

## 10. Задача

Взять одну разобранную находку из своего репозитория и пройти по ней все шесть
шагов. Правило должно сойтись с разметкой и быть прогнано по всему дереву.

Одна подсказка: разметку пишите до образца — образец, написанный первым,
подгоняет ожидания под себя.

**Критерий выполнения** наблюдаемый: `semgrep --test` печатает «All tests
passed», а прогон по репозиторию даёт записанное число находок. Для каждого
ложного срабатывания в коде стоит подавление по имени правила с причиной.

## 11. Проверь себя

1. Почему первым шагом идёт разметка, а не образец?
2. Что означают строки `missed lines` и `incorrect lines` и какая из них про
   ложное срабатывание?
3. Чем подавление по имени правила лучше выключения правила в наборе?
4. Зачем прогонять новое правило по всему дереву, если в сборке оно будет
   смотреть на изменённые файлы?
5. Что стоит положить в `metadata` кроме класса дефекта и почему?
6. Возврат к теме `sast-false-positives`: к какой причине шума относится
   срабатывание на имени таблицы, сверенном с замкнутым множеством?

<details>
<summary>Ответы</summary>

1. Потому что образец, написанный первым, задаёт ожидания под себя. Разметка,
   собранная из разобранных находок, описывает нужное поведение независимо от
   того, каким получится образец.
2. `missed lines` — правило промолчало там, где ожидалось срабатывание, это
   пропуск. `incorrect lines` — сработало там, где ожидалось молчание, это
   ложное срабатывание.
3. Потому что подавление по имени оставляет остальные правила работать на той
   же строке и хранит причину рядом с кодом. Выключение правила в наборе
   убирает весь класс дефектов из всех файлов молча.
4. Чтобы измерить шум до постановки. Прогон по изменённому откладывает старые
   находки, а не убирает их: первое крупное слияние принесёт их все сразу.
5. Владельца правила и адрес внутреннего документа. У выключения правила
   должен быть адресат, а у сообщения — продолжение, которое можно прочитать.
6. К неизвестному правилу санитайзеру: проверка написана своя, и правило про
   неё не знает. Признак — значение сверяется с замкнутым множеством до
   подстановки.

</details>

## 12. Дальше

- `semgrep-rules` — язык правил целиком: операторы, метапеременные, режим
  метки.
- `sast-false-positives` — причины шума, по которым разбираются находки шага
  четыре.
- `bandit` — готовый набор правил под Python и работа с базовым отчётом.
- `triage-false-positives` — что делать с находкой, которую не закрывает ни
  правило, ни подавление.

## 13. Источники

1. Semgrep, «Test rules»; реестр `semgrep-testing-rules`. Разделы: аннотации
   `ruleid:`, `ok:`, `todoruleid:`, `todook:`, поиск тестового файла по имени
   правила, запуск `semgrep --test` и вид вывода.
   <https://docs.semgrep.dev/writing-rules/testing-rules>
2. Semgrep, «Ignore files, folders, and code»; реестр `semgrep-ignore`.
   Разделы: комментарий `nosemgrep` и подавление конкретного правила по имени,
   файл `.semgrepignore`, поле `paths` в правиле, флаги `--exclude` и
   `--include`.
   <https://docs.semgrep.dev/ignoring-files-folders-code>
3. Semgrep, «Customize your CI job»; реестр `semgrep-customize-ci-job`.
   Разделы: прогон относительно магистральной ветки и переменная
   `SEMGREP_BASELINE_REF`, расписание полных прогонов, таймауты.
   <https://docs.semgrep.dev/deployment/customize-ci-jobs>
4. Semgrep, «Rule structure syntax»; реестр `semgrep-rule-syntax`. Разделы:
   поля `metadata`, `paths`, `options`, `min-version` и `max-version`.
   <https://docs.semgrep.dev/writing-rules/rule-syntax>

Каркас этапа: `cwe-taxonomy`. Наследуется всеми темами и отдельной строкой не
повторяется.

**Скоропортящийся слой.** Ключи командной строки, имя переменной для прогона
относительно ветки, вид вывода прогона разметки и синтаксис файла исключений.
Порядок из шести шагов от находки к правилу держится дольше.

**Маркеры уверенности.** Схождение разметки после вычитания вызова с одним
литералом, три находки на стенде и две после точечного подавления, ноль
находок на семнадцати файлах инструментария — **проверено лично** 2026-08-25
на Semgrep 1.174.0 и Python 3.14.7. Время прогонов — **оценка по одному
прогону**. Аннотации разметки, механика подавления и прогон относительно
магистральной ветки — **по документации** Semgrep, открытой лично 2026-08-25.
Прогон относительно ветки **не воспроизводился**: стенд лежит вне индекса git.

[^1]: Semgrep, «Test rules», реестр `semgrep-testing-rules`.
[^2]: Semgrep, «Ignore files, folders, and code», реестр `semgrep-ignore`.
[^3]: Semgrep, «Customize your CI job», реестр `semgrep-customize-ci-job`.
