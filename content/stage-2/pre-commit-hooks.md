---
id: pre-commit-hooks
plan_id: t-2-3-04
title: Pre-commit и серверные проверки на секреты
summary: >
  Клиентский хук отключается одним флагом и потому не граница, а удобство;
  серверная проверка выполняется на приёмной стороне и обходится только с
  записью в журнал. Как поставить обе и чем они отличаются на прогоне.
stage: appsec-tooling
order: 210
status: published
depth: L2
mode: рецепт
skeleton: инструмент
time_min: 40
teaches:
  - Поставить клиентский хук на поиск секретов и воспроизвести его обход
  - Поставить серверную проверку и показать, что тот же обход на неё не действует
  - Разделить, что проверка экономит разработчику и что она гарантирует команде
prerequisites: [secret-scanners]
related: [secret-scanners, secret-history-scan, secret-rotation, secrets-in-code]
fixes_in: [secret-rotation, secrets-in-code]
tags: [secrets, tooling]
cwe: [CWE-798]
asvs: []
wstg: []
owasp: ['A04:2025', 'A07:2025']
labs: []
sources: [pre-commit, gh-push-protection]
reviewed: 2026-09-02
review_interval: 12
---

# Pre-commit и серверные проверки на секреты

Уровень **L2** · время 40 мин (теория 24 / задача 11 / самопроверка 5,
оценка)

Что прочитать сначала: `secret-scanners`.

Две команды, один коммит с секретом. Клиентский хук отклонил его кодом 1;
та же команда с одним флагом создала коммит кодом 0 — без предупреждения и
без записи. А на отправку того же коммита приёмная сторона ответила отказом,
и тот же флаг её не спас:

```text
$ git push --no-verify origin main
 ! [remote rejected] main -> main (pre-receive hook declined)
```

Разница между этими двумя ответами — предмет темы. Проверка на секреты
ставится в двух местах. Клиентский хук исполняет команда разработчика и
подчиняется ему; серверная работает на приёмной стороне, и локальные флаги
до неё не долетают. Первый — удобство, вторая — граница, и путать их роли
дорого.

Дальше — как поставить обе: конфигурация хука в корне репозитория, серверный
хук с диапазоном приходящих коммитов. И чем обход на платформе отличается от
флага: записью в журнале, которую видно.

## 0. Коротко

Проверку на секреты можно поставить в двух местах, и разница между ними не в
скорости, а в том, кому подчиняется проверка.

**Клиентский хук** выполняется на машине разработчика, его же командой `git
commit`. Отключается флагом `--no-verify` и ещё несколькими способами, следа не
оставляет. Это удобство: разработчик узнаёт о секрете за секунду до того, как
его закоммитит, и не тратит день на разбор инцидента.

**Серверная проверка** выполняется на приёмной стороне, до того как коммиты
станут частью репозитория. Клиентскими флагами не отключается вообще — они до
неё не долетают. Это граница.

Обе нужны, и путать их роли дорого: конвейер, где стоит только клиентский хук,
защищённым не становится. Что делать, если секрет всё-таки прошёл, — тема
`secret-rotation`; чинится сам класс `CWE-798` в `secrets-in-code`. Распределение
проверок по стадиям конвейера целиком — этап 3.

**Зачем это в работе AppSec-инженера.** Хук на секреты ставят почти везде, где
вообще ищут секреты, и почти везде считают его защитой. Инженера зовут ровно
тогда, когда секрет прошёл сквозь установленный хук, — и объяснять, почему это
было ожидаемо, приходится задним числом.

## 1. Цели

После этого раздела вы сможете:

1. Поставить клиентский хук на поиск секретов и воспроизвести его обход.
2. Поставить серверную проверку и показать, что тот же обход на неё не действует.
3. Разделить, что проверка экономит разработчику и что она гарантирует команде.

## 3. Механика

**Откуда это взялось.** Хуки в git появились как способ автоматизировать
рутину. Клиентские хуки при этом намеренно сделаны необязательными: они лежат
в `.git/hooks` и не передаются при клонировании. Отключаться они тоже обязаны —
иначе работать с чужим репозиторием стало бы невозможно. Раздать их команде помогает
отдельный слой — конфигурация в корне репозитория, по которой хуки
устанавливаются каждым у себя. Свойство «отключается» этот слой не меняет и
менять не пытается.

Тема отвечает на «как настроить», поэтому устройство хуков дано ровно в той
мере, в какой оно влияет на настройку. Клиентский хук `pre-commit` вызывается
перед созданием коммита; ненулевой код возврата отменяет коммит. Серверный хук `pre-receive` вызывается на принимающей стороне
один раз за отправку. На вход он получает строки вида «старая ссылка, новая
ссылка, имя ссылки». Ненулевой код отклоняет отправку целиком. Разница в том, что
первый исполняет команда разработчика, а второй — приёмная сторона, и её
поведение разработчик не выбирает.

## 4. Что умеет и чего не умеет

**Что умеет клиентский хук.** Остановить коммит до его создания, то есть до того,
как секрет вообще попадёт в историю, — и тем избавить от всей работы по
`secret-rotation`. Работать быстро: проверяются только подготовленные изменения,
а не репозиторий.

**Чего не умеет клиентский хук.** Быть гарантией. Способов пройти мимо несколько,
и все документированные: флаг `--no-verify` у `git commit`, переменная окружения
`SKIP` с именем хука. Наконец, хук можно не установить у себя вовсе:
конфигурация лежит в репозитории, а установка выполняется вручную. Ни один из
этих способов не оставляет следа в репозитории.

**Что умеет серверная проверка.** Отклонить отправку до того, как коммиты станут
частью репозитория. У платформенного варианта — защиты от отправки — охват шире
командной строки: она блокирует и коммиты, сделанные в веб-интерфейсе, и загрузку
файлов, и обращения к API.

**Чего не умеет серверная проверка.** Быть незаметной и мгновенной: она добавляет
время к каждой отправке и требует места, где её выполнять. И она не отменяет
обхода вовсе — но делает его **именным и записанным**. У платформенной защиты
обход требует указать причину. После этого создаётся оповещение в разделе
безопасности, запись в журнале аудита и письмо владельцам. Разница с
`--no-verify` не в том, что обойти нельзя, а в том, что обход виден.

## 5. Как запустить

**Клиентский хук.** Конфигурация кладётся в корень репозитория, чтобы её видела
вся команда:

```yaml
# .pre-commit-config.yaml
repos:
  - repo: local
    hooks:
      - id: gitleaks
        name: gitleaks — поиск секретов
        entry: /путь/к/gitleaks
        args: [git, --pre-commit, --staged, --no-banner, --redact]
        language: system
        pass_filenames: false
```

Дальше каждый разработчик ставит хук у себя — одной командой, и это ручной шаг,
который никто за него не сделает:

```bash
pre-commit install     # -> pre-commit installed at .git/hooks/pre-commit
```

Флаг `--redact` здесь не косметика: без него значение секрета попадёт в вывод
терминала и в журналы сборки, то есть утечёт ещё раз.

**Серверная проверка.** Собственный хук в принимающем репозитории — по одной
строке на каждую обновляемую ссылку:

```bash
#!/usr/bin/env bash
# hooks/pre-receive в принимающем репозитории
while read -r oldrev newrev refname; do
  [ "$newrev" = "0000000000000000000000000000000000000000" ] && continue
  if [ "$oldrev" = "0000000000000000000000000000000000000000" ]; then
    range="$newrev --not --all"      # новая ветка: только свои коммиты
  else
    range="$oldrev..$newrev"
  fi
  gitleaks git . --no-banner --redact --log-opts="$range" || exit 1
done
```

Диапазон здесь — весь смысл. Проверять надо приходящие коммиты, а не репозиторий
целиком: иначе каждая отправка будет спотыкаться о старые находки, и проверку
отключат через неделю.

На хостинге тот же результат даёт встроенная защита от отправки, включаемая
настройкой репозитория или организации, — своего кода она не требует.

## 6. Как читать вывод

Прогон на стенде: Gitleaks 8.30.1, pre-commit 4.6.2, git 2.55.0.

### Хук срабатывает

```text
$ git add config.py && git commit -m "настройка бота"
gitleaks — поиск секретов...............................Failed
- hook id: gitleaks
- exit code: 1
WRN leaks found: 1
$ echo $?
1
```

Читается по трём строкам: какой хук, с каким кодом возврата, сколько находок.
Коммита не создано — это и есть результат.

### Тот же коммит проходит с одним флагом

```text
$ git commit --no-verify -m "настройка бота"
[main 08fdd61] настройка бота
 1 file changed, 1 insertion(+)
$ echo $?
0
```

Ни предупреждения, ни записи. В репозитории есть коммит с секретом, и по
репозиторию невозможно узнать, что хук был обойдён, — узнать это можно только
следующим прогоном сканера.

Второй документированный способ — переменная окружения с именем хука. Она
честнее в терминале и ровно так же нема в репозитории:

```text
$ SKIP=gitleaks git commit -m "проба"
gitleaks — поиск секретов..............................Skipped
[main be3a34e] проба
$ echo $?
0
```

### Серверная проверка тот же коммит не принимает

```text
$ git push origin main
remote: ОТКАЗ: в refs/heads/main найдены секреты — push отклонён
 ! [remote rejected] main -> main (pre-receive hook declined)
error: failed to push some refs to '../server.git'
$ echo $?
1
```

`remote:` в начале строки — признак того, что решение принято не здесь. Отказ
пришёл с приёмной стороны, и локальные флаги на него не влияют:

```text
$ git push --no-verify origin main
 ! [remote rejected] main -> main (pre-receive hook declined)
```

Флаг `--no-verify` у `git push` отключает **клиентский** хук `pre-push` —
серверного он не касается. Это самая частая путаница в теме. У одного и того же
флага на разных командах разные адресаты — и ни на одной он не адресован
серверу.

### Чистая отправка проходит

```text
$ git push origin main
gitleaks — поиск секретов...............................Passed
 * [new branch]      main -> main
```

## 7. Границы метода

**Граница первая: клиентский хук не гарантия, и это не дефект.** Он по замыслу
подчиняется тому, кто его запускает. Считать его защитой — то же самое, что
считать защитой подсказку в редакторе.

**Граница вторая: подготовленные изменения — не история.** Хук смотрит то, что
идёт в коммит. Секрет, уже лежащий в истории, он не найдёт: для этого есть
`secret-history-scan`, и прогон по истории нужен отдельно.

**Граница третья: обход остаётся и на сервере.** Платформенная защита позволяет
провести отправку с указанием причины. Разница с клиентским обходом — в
оповещении, записи в журнале аудита и письме владельцам, то есть в
восстановимости события, а не в его невозможности.

**Граница четвёртая: проверка не покрывает то, чего не знает.** Хук — тот же
сканер, с теми же правилами и теми же пропусками, что разобраны в
`secret-scanners`. Внутренний пароль без узнаваемой формы пройдёт обе проверки.

**Цена.** Клиентский хук — доли секунды на коммит, и это его главное достоинство:
дорогую проверку разработчики отключат. Серверная — время на каждую отправку,
поэтому диапазон коммитов ограничивают приходящими. Проверка секретов по всей
истории на каждую отправку не выдерживается ни в одном живом репозитории.

## 8. Ловушка

Распространённое мнение: хук на секреты стоит — значит, секреты в репозиторий не
попадут.

**Клиентский хук отключается одним флагом, и в репозитории не остаётся следа.**
На стенде это ровно одна дополнительная команда: коммит, отклонённый хуком с
кодом 1, создаётся с `--no-verify` с кодом 0. Обход не требует прав, не требует
знаний и не отличим потом от обычного коммита. А ещё хук можно не установить
вовсе: конфигурация лежит в репозитории, но `pre-commit install` каждый
выполняет сам.

Контрпример к обратному выводу — «значит, клиентские хуки не нужны»: нужны, и
именно из-за цены следующего шага. Секрет, остановленный хуком, стоит секунды;
секрет, доехавший до репозитория, стоит отзыва ключа, разбора журналов
поставщика и оценки срока доступности (`secret-rotation`). Хук окупается на
честных ошибках, которых большинство. Он не про злой умысел и не про
гарантию — гарантию даёт только проверка на той стороне, которой разработчик не
управляет.

## 9. Чеклист ревью

1. Verify that серверная проверка существует, а не только клиентский хук.
2. Verify that серверная проверка смотрит диапазон приходящих коммитов, а не
   репозиторий целиком.
3. Verify that вывод хука не печатает значение секрета: флаг сокрытия включён.
4. Verify that обходы серверной проверки где-то записываются и кем-то
   просматриваются.
5. Verify that установка клиентского хука проверяется, а не предполагается: сам
   по себе файл конфигурации в репозитории ничего не включает.
6. Verify that версия сканера в хуке зафиксирована и обновляется осознанно.
7. Verify that прогон по всей истории существует отдельно от хука и идёт по
   расписанию.

## 10. Задача

Поставить в своём репозитории клиентский хук на поиск секретов и убедиться, что
он останавливает коммит. Затем обойти его двумя разными способами. Затем поднять
локальный принимающий репозиторий с хуком `pre-receive` и попробовать отправить
в него тот же коммит.

Одна подсказка: принимающий репозиторий делается командой `git init --bare`, а
хук в нём должен быть исполняемым — забытый бит прав выглядит как отсутствие
проверки.

**Критерий выполнения** наблюдаемый: у вас есть коммит с выдуманным секретом,
созданный вопреки установленному хуку, и отказ приёмной стороны на попытку его
отправить. Вы можете назвать два способа обойти клиентский хук и объяснить,
почему ни один не действует на серверную проверку.

## 11. Проверь себя

1. Почему клиентский хук нельзя считать границей?
2. Какие способы обойти клиентский хук вы знаете и какой след они оставляют?
3. Что получает на вход хук `pre-receive` и почему важен диапазон?
4. Кому адресован флаг `--no-verify` у команды `git push`?
5. Чем обход платформенной защиты от отправки отличается от `--no-verify`?
6. Возврат к теме `secret-scanners`: почему хук не находит секрет, который уже
   лежит в истории репозитория?

<details markdown="1">
<summary>Ответы</summary>

1. Он выполняется командой разработчика на его машине и по замыслу отключается —
   флагом, переменной окружения или отказом его установить.
2. Флаг `--no-verify` у `git commit`, переменная `SKIP` с именем хука, отказ от
   установки хука; следа в репозитории не остаётся ни от одного.
3. Строки «старая ссылка, новая ссылка, имя ссылки» по одной на обновляемую
   ссылку; диапазон нужен, чтобы проверять приходящие коммиты, а не всю историю
   на каждой отправке.
4. Клиентскому хуку `pre-push` на той же машине; серверной проверки он не
   касается.
5. Обход на платформе требует указать причину и порождает оповещение, запись в
   журнале аудита и письмо владельцам; `--no-verify` не оставляет ничего.
6. Хук проверяет подготовленные изменения — то, что идёт в создаваемый коммит;
   старые коммиты в этот набор не входят и требуют отдельного прогона по истории.

</details>

## 12. Источники

1. pre-commit; реестр `pre-commit`. Разделы: формат `.pre-commit-config.yaml`,
   локальные хуки и `language: system`, установка хука командой `pre-commit
   install`, пропуск отдельного хука переменной `SKIP`.
   <https://pre-commit.com/>
2. GitHub, «About push protection»; реестр `gh-push-protection`. Разделы: что
   блокирует защита от отправки (командная строка, веб-интерфейс, загрузка
   файлов, API), два вида защиты и их умолчания, обход с указанием причины и
   что при этом создаётся — оповещение, запись в журнале аудита, письмо
   владельцам.
   <https://docs.github.com/en/code-security/concepts/secret-security/push-protection>

Каркас этапа: `cwe-taxonomy`. Наследуется всеми темами и отдельной строкой не
повторяется.

**Скоропортящийся слой.** Имена флагов и хуков, вид вывода, настройки
платформенной защиты и её умолчания. Устройство — клиентская проверка
подчиняется разработчику, приёмная не подчиняется — не меняется вовсе.

**Маркеры уверенности.** Все четыре состояния воспроизведены **лично**
2026-08-25: хук отклонил коммит с кодом 1, `git commit --no-verify` создал тот же
коммит с кодом 0, серверный `pre-receive` отклонил отправку, и `git push
--no-verify` её тоже не спас; чистая отправка прошла. Стенд — локальный
принимающий репозиторий `git init --bare`, Gitleaks 8.30.1, pre-commit 4.6.2,
git 2.55.0. Переменная `SKIP` как второй способ обхода проверена **лично** тем
же прогоном: хук печатает `Skipped`, коммит создаётся с кодом 0. Состав того, что
блокирует платформенная защита от отправки, и последствия её обхода —
**по документации** GitHub, открытой лично 2026-08-25; сама платформенная защита
не запускалась, стендом служил собственный серверный хук.
