# Подсказка

`--extra-index-url` не понижает приоритет второго индекса — он объединяет
кандидатов из обоих и берёт версию побольше. Значит, доверенный источник нельзя
задавать «дополнительным». Их два рычага: убрать публичный индекс из уравнения и
прибить версию к известной хорошей.
